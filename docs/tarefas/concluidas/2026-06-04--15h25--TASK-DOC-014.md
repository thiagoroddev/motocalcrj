# TASK-DOC-014 - Sincronizar docs/dominio + docs/arquitetura com o código pós REF-31/32.x

- **Status:** Concluída
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 04/06/26 00:00
- **Data-hora início:** 04/06/26 00:00
- **Data-hora conclusão:** 04/06/26 15:25
- **Dependências:** TASK-REF-31, TASK-REF-32.1..32.6 (concluídas)
- **REQ/ADR/DT:** ADR-011, ADR-012, ADR-013, ADR-014, DT-19; padrão do módulo 23 (modelagem)
- **Observações:** Pacote REF-32 (MVP manutenção) mudou bastante o domínio (preset fonte única, `servicosManutencao`, `statusPrecoAutorizada`, `concessionariaIncluiPeca`, estimativa de M.O., fusão item-componente). Revisão geral de docs para refletir o código e adicionar o que falta, antes de outra IA dar continuidade.

## Planejamento Aprovado

Doc-sync (sem tocar código): atualizar glossário, `bloco-perfil-manutencao`, `overrides`, `invariantes`, `calculos-visao`; **criar** `entidade-preset-moto.md`; atualizar índice da modelagem.

## Execução

- 00:00: Plano aprovado. Investigação: lidos `bloco-perfil-manutencao`, `aggregate-preset`, `entidade-preset`, `invariantes`, `calculos-visao`, `overrides`, `_glossario`; tipos confirmados (`StatusPrecoAutorizada`, `ServicoIndependente`, `PresetMoto`).
- `_glossario.md` (v9): `ServicoIndependente` corrigido; **8 termos novos** (Serviços de Manutenção do Preset, Status do Preço Autorizado, Concessionária Inclui Peça, Estimativa de M.O. `~`, Fator de M.O., Item-componente, MAPA_PECA_PARA_SERVICO, Custo Incompleto); Modo de Revisão (MVP) e Vida Útil (convenção ADR-014 + DT-19).
- `bloco-perfil-manutencao.md`: campos de estimativa, default `autorizadas`, MVP (`normalizarPerfilMvp`), 2 actions novas, snippet de cálculo atualizado.
- `overrides.md`: `ServicoIndependente` real (`precoIndependente` + campos de concessionária); `resolverIntervaloPeca` passo 2 "só quando editado" (REF-29) + DT-19; defaults (12+2); removida seção duplicada de `revisaoAutorizadaOverrides`.
- `invariantes.md`: INV-MANUT-2 (estimativa opt-in `~`), INV-MANUT-3 (anti-duplicação/composição); nota DT-19 em INV-VIDA-UTIL-1; INV-CALC-2 contagem → ponteiro.
- `arquitetura/calculos-visao.md`: assinaturas 3-arg; pipeline com ETAPA 5 (visão); III.5 avulsos+estimativa+pendências; III.8 view-model (`montarItensManutencao`, `maoDeObraEstimada`, `resolverServicosManutencaoPerfil`); IV tipos novos; VI riscos; ponteiros de teste.
- **Criado** `entidade-preset-moto.md`: shape do JSON `PresetMoto` (peças/pneus/revisão/`servicosManutencao`/`fatorMaoDeObra`), distinção vs `PresetEntry`, invariantes INV-PRESETMOTO-1..4, presets existentes.
- `modelagem/README.md`: índice (novo doc + `manutencao-estimativas.md`) e histórico (v5).
- 15:23: `npm run test` → 272 verdes (código intacto, doc-only).

## Decisões Tomadas

- **`_glossario.md` mantido com underscore** (fixa no topo da pasta) em vez de renomear para `glossario.md` — evita ação destrutiva/quebra de links; o README já referencia `_glossario.md`.
- **Não documentar `PerfilUso` casual nem mexer em requisitos/PWA** — fora do escopo (DT-18 e TASK-CHORE-015 já tratam).
- **Contagens de teste viram ponteiro** (`npm run test`) em vez de número fixo (política DOC-012).

## O Que NÃO Foi Feito (e Por Quê)

- `value-objects.md`, `contexto-projeto-ai.md`, `requisitos/*`: não tinham drift causado pela REF-32 relevante ao escopo; a parte de `contexto-projeto-ai.md` (ref. morto a `vite-plugin-pwa`) é da TASK-CHORE-015 — não dupliquei.
- Restruturar o diagrama do README (mistura `dominio/` e `dominio/modelagem/`): inconsistência pré-existente, fora do escopo desta task.

## Testes

- `npm run test`: **272 verdes** (antes e depois — mudança é doc-only, nenhum arquivo `src/` tocado).

## Revisão

### Modo
Auto-revisão IA — doc-sync. Sem revisão de código (nenhum `src/` alterado).

### ✅ Bom
- Cada doc tocado lista "validado contra `src/...`" e o que mudou; rastreável.
- Criação de `entidade-preset-moto.md` fecha lacuna real: a entidade técnica do preset (fonte única da REF-31) não tinha doc — só o envelope `PresetEntry`.
- Glossário e invariantes agora cobrem os conceitos da REF-32, base para outras IAs continuarem.

### 🟡 Importante
- **Inconsistência de numeração de invariantes** entre `invariantes.md` (INV-PRESET-1 = kmAnual derivada) e `aggregate-preset.md` (INV-PRESET-1 = consistência do preset ativo): colisão pré-existente, não introduzida aqui. Candidata a TASK-DOC futura (não gerei tarefa para não inflar; registrar se incomodar).

### 🟢 Sugestão
- Unificar a localização do README da modelagem (descreve `dominio/` estando em `dominio/modelagem/`).

### Veredito
**APROVADO.** Docs refletem o código pós REF-31/32.x; lacuna do PresetMoto preenchida.

### Tarefas Geradas pela Revisão
- Nenhuma (a colisão INV-PRESET-* fica registrada aqui como nota, sem tarefa).

### Requisitos Gerados / ADRs Geradas
- Nenhum.

## Aprendizados Para o Projeto
- Doc de domínio defasa rápido quando o cálculo muda — vale sincronizar glossário + invariantes ao fim de cada pacote grande (como REF-32), não só no fim.
- Há **dois** conceitos "preset" (técnico `PresetMoto` × envelope `PresetEntry`); a documentação agora separa explicitamente — fonte recorrente de confusão.
