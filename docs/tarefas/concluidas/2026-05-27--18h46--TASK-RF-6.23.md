# TASK-RF-6.23 - Seção Manutenção: subgrupos "Ancorado / Amortizado" + popover de detalhes por peça

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Esforço-H/IA:** M/M
- **Data origem:** 27/05/26
- **Data início:** 27/05/26 18:39
- **Data conclusão:** 27/05/26 18:46
- **Dependências:** TASK-RF-6.13
- **REQ/ADR/DT:** RF-6.3.3, ADR-006
- **Observações:** Agrupar peças da categoria Manutenção em Ancoradas e Amortizadas sem mudar os itens nem o cálculo total. Ancoradas mantêm `1x/2x`; amortizadas exibem `≈0,30x/≈1,52x`. Todos os itens ganham ícone de olho que abre dialog shadcn com tabelas individuais de variáveis e impacto no cálculo.

## Planejamento Aprovado

**O que muda:**
- `src/types/calculos.ts`: estender `CustoPeca` com `modo`, `kmUltimaTroca`, `kmDasProximasTrocas` e `intervaloMeses` opcional.
- `src/utils/calculos.ts`: preencher metadados de modo e próximas trocas em `calcularCpkPorPeca`, sem alterar `custoAnual` nem `cpk`.
- `src/components/detalhamento/SecaoManutencao.tsx`: manter as linhas atuais, mas separadas em containers "Ancorados" e "Amortizados"; ancorados seguem com contador inteiro, amortizados usam `≈` + decimal real.
- `src/components/detalhamento/PopoverDetalhesPeca.tsx`: novo dialog shadcn reutilizável para detalhes do item, com duas tabelas: "Item" e "Manutenções previstas" ou "Provisão no período".
- `src/utils/calculos.test.ts`: cobrir modo ancorado/amortizado, lista de próximas trocas e preservação de custo.

**Critérios de aceite:**
- ✅ Peças com km informado e mapeadas aparecem em "Ancorados".
- ✅ Peças sem km informado, não mapeadas ou temporais aparecem em "Amortizados".
- ✅ `kit_revisao` fica amortizado/automático e não volta para Ajustes.
- ✅ Ancorados mostram `1x`, `2x`, `4x`; amortizados mostram `≈0,30x`, `≈1,52x`.
- ✅ Cada peça tem botão de olho com `aria-label` e dialog acessível.
- ✅ Dialog ancorado mostra tabela "Item" e tabela "Manutenções previstas" com ordem, quilometragem e custo.
- ✅ Dialog amortizado mostra tabela "Item" e tabela "Provisão no período" com km no período, fórmula e custo, sem inventar eventos reais.
- ✅ Total da categoria Manutenção permanece idêntico antes/depois.
- ✅ `npm run test` e `npm run lint` verdes.

**Impacto:** Detalhamento, tipos/saída de cálculo e testes unitários. Sem migration de schema.
**Riscos:** mudar cálculo sem querer; mitigado por testes de preservação de custo. Risco visual em 375px com labels longos; mitigado com containers simples, truncamento existente e botões compactos com ícones lucide.
**Dependências novas:** nenhuma.

## Execução

- 27/05/26 18:39: Plano aprovado pelo humano.
- 27/05/26 18:39: Baseline `npm run test`: 159 testes verdes.
- 27/05/26 18:41: `CustoPeca` passou a expor `modo`, `kmUltimaTroca`, `kmDasProximasTrocas` e `intervaloMeses`.
- 27/05/26 18:42: `SecaoManutencao` refatorada para containers Ancorados/Amortizados, com olho por peça e ajuda por grupo.
- 27/05/26 18:43: Criado `PopoverDetalhesPeca` com dialog shadcn e duas tabelas por item.
- 27/05/26 18:44: Testes de cálculo atualizados para próximos eventos e modos de peça.
- 27/05/26 18:45: `npm run test` e `npm run lint` verdes após ajustes finais de formatação.

## Decisões Tomadas

- **Dialog shadcn em vez de popover livre:** mantém consistência com pop-ups existentes e garante focus trap/ESC.
- **Amortizado não lista manutenções previstas:** sem km da última manutenção, o app não tem âncora real. A segunda tabela mostra "Provisão no período".
- **Serviços de revisão permanecem fora dos subgrupos:** a separação Ancorado/Amortizado se aplica às peças de manutenção, que têm `CustoPeca`.

## O que NÃO foi feito (e por quê)

- **Tabela global de todas as próximas trocas:** fora do escopo aprovado; o foco é tabela individual por item.
- **Editar km da última manutenção pelo dialog:** fora do escopo; continua em Ajustes.
- **Build e dev server:** não executaram por falha ambiental WSL/Node antes de iniciar Vite/TypeScript.

## Testes

- `1o npm run test`: 159 verdes.
- `2o npm run test`: 160 verdes.
- `npm run lint`: verde.
- `npm run build`: não executou no ambiente atual (`WSL 1 is not supported. Could not determine Node.js install directory`).
- `npm run dev -- --host 0.0.0.0`: não iniciou pelo mesmo erro de ambiente WSL/Node.

## Revisão

- **Resultado:** APROVADO com ressalva ambiental.
- **Achados bloqueantes:** nenhum.
- **Risco residual:** validação visual em navegador local não foi possível porque o dev server não inicia neste ambiente WSL/Node. A implementação segue os padrões dos dialogs existentes e passou em lint/testes.
