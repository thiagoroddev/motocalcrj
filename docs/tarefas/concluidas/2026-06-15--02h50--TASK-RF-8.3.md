# TASK-RF-8.3 — Bateria: CardBateria (Ajustes + onboarding)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante · **Urgência:** Normal · **Esforço-H/IA:** M/M
- **Data-hora origem:** 15/06/26 02:15 · **Data-hora início:** 15/06/26 · **Data-hora conclusão:** 15/06/26 02h50
- **Dependências:** TASK-RF-8.1 (motor), TASK-RF-8.2 (presets) · **REQ/ADR/DT:** ADR-016; parte 3 do épico **TASK-RF-8**
- **Observações:** A RF-8.3 planejada incluía também a Mão de Obra + Detalhamento; durante a execução isso se mostrou um refactor próprio (CardServico é km-cêntrico) → **dividido em RF-8.4** (decisão alinhada com o humano de dividir XG para preservar evolução).

## Planejamento Aprovado

Card próprio de bateria, editável, em Ajustes e no onboarding: data da última troca + vida útil em anos, com a próxima troca/atraso visível.

## Execução

- **`src/components/ajustes/CardBateria.tsx`** (novo): `<input type="month">` para a última troca (AAAA-MM) + `Select` de vida útil (2/3/4/5 anos) + linha de **próxima troca / atraso** via `proximaTrocaBateria`. Dispara `SET_BATERIA_ULTIMA_TROCA` / `SET_BATERIA_VIDA_UTIL`.
- Wired em **`PaginaAjustes.tsx`** (após "KM - últimas trocas") e no onboarding **`Passo5Manutencoes.tsx`**.

## Decisões Tomadas

- **Vida útil (select de anos) fica na `CardBateria`** (Ajustes/onboarding), junto da data e da próxima-troca — card temporal coeso. A aparição da bateria em "Serviços Extras" (Mão de Obra) com a M.O. é **RF-8.4**.
- **`proximaTrocaBateria` exibida no próprio card** (não só no Detalhamento): cobre o requisito "mostrar próxima troca/atraso" já aqui.

## O Que NÃO Foi Feito (e Por Quê)

- **Bateria em "Serviços Extras" (Mão de Obra) com seletor de anos** + **Detalhamento** (próxima troca/atraso no item) + **ADR-014/016**: viraram **TASK-RF-8.4** (fecha o épico). Motivo: o `CardServico` filtra `intervalKm > 0` (bateria, temporal, fica de fora) e é km-cêntrico — incluir a bateria exige threadar a config de vida útil por `SecaoServicosExtras`→`ListaServicos`→`CardServico` + ramo temporal; é um refactor próprio, melhor isolado.

## Testes

- **`CardBateria.test.tsx`** (novo, 3 testes): despacha a data (AAAA-MM); limpar despacha `null`; sem data estima a próxima troca pelo ano da moto (jan + vida útil = 01/2025 p/ moto 2022, 3 anos).
- **Suíte: 445 verdes** (39 arquivos). `tsc`/`lint`/`build`: **APROVADO**. Smoke do onboarding passa pelo card novo.

## Revisão

Auto-revisão (Standard): componente isolado reusando primitivos (`Input`, `Select`, `TituloSecao`) + o helper já testado `proximaTrocaBateria`. Sem lógica de cálculo nova.

## Tarefas Geradas

- **TASK-RF-8.4** (pendentes): bateria em "Serviços Extras" (Mão de Obra) com seletor de anos + Detalhamento (próxima troca/atraso) + addendum ADR-014/016. Fecha o épico RF-8.

## Aprendizados Para o Projeto

- **`<input type="month">`** casa direto com o formato `AAAA-MM` da config — sem máscara/calendário custom.
- **Dividir na hora certa:** o que parecia 1 tarefa (RF-8.3 com Mão de Obra) era 2 — o CardServico temporal é refactor próprio. Isolar (RF-8.4) preserva a `CardBateria` entregue.
