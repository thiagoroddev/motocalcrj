# TASK-RF-6.31.3 - Reordenar rotas, progresso e wiring do onboarding (Strict)

- **Status:** Concluído
- **Modo:** Strict
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** G
- **Data início:** 07/06/26 22:45
- **Data conclusão:** 07/06/26 23:26
- **Dependências:** RF-6.31.1 (ADR-020), RF-6.31.2 (PassoModelo)
- **REQ/ADR/DT:** ADR-020, registro mestre RF-6.31
- **Observações:** Implementa a ordem canônica/rotas semânticas do ADR-020. Reescreve `onboardingUtils`
  (CONFIG_PASSOS/MAPA_PROXIMO/MAPA_ANTERIOR) e `FluxoOnboarding`; roteia `PassoModelo` e `PassoMaoDeObra`;
  remove `Passo1`/`Passo2` e o legado `5trocas`; ajusta `RotaProtegida`/`PaginaPerfil` (→ `/onboarding/modelo`)
  e os destinos de "Editar" da confirmação; atualiza o happy-path do smoke (matriz de navegação + docs na 6.31.5).

## Execução

- 07/06/26: `onboardingUtils.ts` reescrito — `CONFIG_PASSOS`, `MAPA_PROXIMO`, `MAPA_ANTERIOR` com as
  rotas semânticas e ramificações do ADR-020. `parsePasso` inalterado.
- `FluxoOnboarding.tsx`: rotas semânticas; `PassoModelo` e `PassoMaoDeObra` roteados; `Passo1`/`Passo2`
  e `5trocas` removidos; fallback do `config` e guard/catch-all → `/onboarding/modelo`.
- `RotaProtegida.tsx` e `PaginaPerfil.tsx` → `/onboarding/modelo`.
- `PassoConfirmacao.tsx`: destinos de "Editar" atualizados (`modelo`/`km`/`situacao`/`seguro`/
  `internet`/`alimentacao`); fallback do `concluir` → `/onboarding/modelo`. (Reordenação do resumo +
  regra "último passo" do PassoLayout ficam na 6.31.4.)
- **Arquivos deletados:** `Passo1.tsx`, `Passo2.tsx`.
- `App.smoke.test.tsx`: happy-path reescrito para a nova ordem (Modelo→Ano→Km→Situação→Seguro→
  Alimentação→Internet→Vida útil→**Mão de obra**→Últimas manutenções→Confirmação).
- **2 bugs de rota legada corrigidos durante a execução** (a reordenação os exporia):
  - `Passo6.tsx` navegava com `getProximoPasso('6', …)` (chave antiga) → `'situacao'`. Sem isso o fluxo
    travava na Situação (pego pelo smoke).
  - `PassoConfirmacao` `concluir` (caminho inválido) caía em `/onboarding/1` → `/onboarding/modelo`.

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 33 arquivos / 384 testes verdes na
  conclusão da reordenação (386 após a RF-6.32.1, fechada junto). O smoke percorre o fluxo novo ponta a ponta.

## Revisão

### Modo
Auto-revisão IA + validação humana. Strict (navegação inteira).

### ✅ Bom
- **Navegação data-driven:** a ordem/ramos vivem só em `onboardingUtils` (mapas) — telas não sabem a
  ordem, exceto o `Passo6` (branch local por `situacao`), agora corrigido para a chave semântica.
- **Rotas semânticas** eliminam o legado `5trocas` e os labels duplicados.
- **Smoke como rede de segurança:** pegou na hora o literal `'6'` esquecido no `Passo6` — o gate não
  passou até corrigir. Coerente com a regra de "gate verde sustenta o pronto".
- **`typecheck` confirma** que a remoção de `Passo1`/`Passo2` não deixou referências.

### 🔴 Bloqueante / 🟡 Importante
Nenhum.

### 🟢 Sugestão
- `PassoLayout` ainda tem o literal `passo === '9'` (inócuo após rotas semânticas; confirmação usa
  `textoBotao` próprio). Será trocado por "detectar último passo" na **6.31.4**.
- A reordenação das seções de resumo da confirmação fica na **6.31.4**; a matriz de navegação (3 ramos +
  voltar) e a sincronização de docs, na **6.31.5**.

### Veredito
APROVADO. Código, gate e validação visual humana OK.

## Validação Visual

- **APROVADO (07/06/26):** humano percorreu o fluxo novo e confirmou ("tudo ok").

## Relacionado

- Próximas: **TASK-RF-6.31.4** (resumo da confirmação + regra "último passo" do PassoLayout) e
  **TASK-RF-6.31.5** (matriz de navegação + docs).
