# TASK-REF-06 — PassoLayout — shadcn Button + padrão de altura (min-h-touch)

- **Status:** Concluída ✅
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data origem:** 18/05/26
- **Início:** 18/05/26 20:40
- **Conclusão:** 18/05/26 20:44
- **Dependências:** —
- **REQ/ADR/DT:** —
- **Responsável:** IA + Humano

---

## Planejamento Aprovado

**O que muda:**
- `src/pages/onboarding/PassoLayout.tsx`: substituir os dois `<button>` nativos por shadcn `<Button>`.
  - Voltar: `variant="outline"` + `className="flex-1 min-h-touch rounded-btn border-muted font-medium"`
  - Próximo/Concluir: variante default + `className="flex-1 min-h-touch rounded-btn font-semibold text-foreground disabled:opacity-40"`

**Critérios de aceite:**
- Aparência idêntica à atual (altura 48px, cores, estado disabled)
- `npm run lint` → 0 erros
- `npm run test` → 92 testes passam
- `npm run build` → passa

---

## Execução

- 20:40: Tarefa promovida a em-andamento. Plano aprovado pelo humano.
- 20:41: Import de `Button` adicionado em `PassoLayout.tsx`.
- 20:42: `<button>` Voltar e Próximo/Concluir substituídos por `<Button>`. Override `text-foreground` aplicado para contornar `--primary-foreground` escuro do tema.
- 20:43: `npm run lint --fix` executado — corrigiu também formatação pendente em `PaginaDetalhamento.tsx` (linha 117, espaço faltando — bug anterior, não relacionado a esta task).
- 20:44: lint (0 erros), 92 testes, build ok.

---

## Arquivos Alterados

| Arquivo | Mudança |
|---|---|
| `src/pages/onboarding/PassoLayout.tsx` | `<button>` → `<Button>` (Voltar + Próximo/Concluir); import `Button` adicionado |
| `src/pages/PaginaDetalhamento.tsx` | Formatação prettier automática (linha 117) — não relacionado à task |

---

## Decisões Tomadas

- **`text-foreground` no botão primário:** `--primary-foreground` do tema é `rgb(13 19 33)` (escuro), não branco. Para manter o texto branco no botão azul, foi necessário override com `text-foreground`. Padrão adotado para todos os botões primários de ação no onboarding.
- **`disabled:opacity-40` mantido:** o shadcn padrão usa `opacity-50`; manteve-se `40` para preservar a aparência visual atual exata.

## O que NÃO foi feito e por quê

- **Atualizar `--primary-foreground` para branco no tema:** seria uma mudança com impacto em outros componentes shadcn fora do onboarding. Fora do escopo desta task.

---

## Testes

| Comando | Resultado |
|---|---|
| `npm run lint` | 0 erros |
| `npm run test` | 92 testes passaram |
| `npm run build` | CSS: 40.16 kB, JS: 344.70 kB |

---

## Tarefas Geradas

Nenhuma.
