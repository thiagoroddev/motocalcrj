# TASK-REF-31 - Preset/modelo como fonte única da verdade

- **Status:** Concluído
- **Modo:** Strict
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** G/G
- **Data origem:** 01/06/26 19:13
- **Data início:** 02/06/26 10:36
- **Data conclusão:** 02/06/26 20:16
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A01; ADR-011 (Aceita)
- **Observações:** `src/presets/*.json` passou a ser a fonte única dos metadados de catálogo. `catalogoModelos.ts` continua existindo como fachada compatível, mas não monta mais lista manual.

## Planejamento Aprovado

**O que muda:**
- `src/presets/pop110i.json`: adicionar `nomeFipe` e `aceitaEtanol`.
- `src/types/calculos.ts`: expandir o contrato de preset com metadados de catálogo.
- `src/data/repositorioPresets.ts`: criar módulo único com `import.meta.glob`, mapa de presets por id e helpers.
- `src/data/catalogoModelos.ts`: derivar `CATALOGO` e helpers a partir do repositório.
- `src/hooks/useCustos.ts`, `src/pages/PaginaInsumos.tsx`, `src/pages/PaginaMaoDeObra.tsx`, `src/components/detalhamento/DialogEdicaoCusto.tsx`: remover globs locais.
- `src/data/catalogoPresets.test.ts`: reforçar contrato catálogo/preset.

**Critérios de aceite:**
- Novo preset JSON aparece no catálogo/onboarding sem editar `.ts`. OK
- Nenhum consumidor remonta `import.meta.glob('../presets/*.json')` fora do repositório. OK
- `CATALOGO` continua disponível para consumidores atuais, mas derivado do JSON. OK
- `npm run lint` e `npm run test` verdes. OK

## Execução

- 02/06/26 10:35: Plano inicial aprovado pelo humano.
- 02/06/26 10:36: Baseline inicial `npm run test`: 239 testes verdes.
- 02/06/26 10:36: Tarefa movida para `em-andamento.md`; ADR-011 criada como Proposta.
- 02/06/26 19:42: Preset `src/presets/factor125i.json`, criado pelo humano, alinhado como fixture preparatória com dados locais FIPE/peças/revisões.
- 02/06/26 20:03: DT-18 registrada para evitar campos editáveis por modo (`vidaUtilKmEntrega`, `intervaloMesesEntrega`, etc.) e preferir vida útil base + fatores de uso.
- 02/06/26 20:10: Humano aprovou replanejamento, aceitou ADR-011 e autorizou executar TASK-REF-31.
- 02/06/26 20:10: Baseline pós-aprovação: `npm run test` 239 verdes; `npm run lint` verde.
- 02/06/26 20:11: ADR-011 marcada como Aceita.
- 02/06/26 20:14: Criado `repositorioPresets.ts`; catálogo passou a ser derivado dos presets; globs locais removidos dos consumidores.
- 02/06/26 20:14: Teste de contrato reforçado para garantir que todos os presets aparecem no catálogo e que os metadados derivados batem.
- 02/06/26 20:16: Validação final com lint/testes verdes. `npm run build` bloqueado pelo ambiente WSL 1/Node antes do TypeScript.

## Decisões Tomadas

- `catalogoModelos.ts` permanece como API pública/fachada para reduzir impacto no onboarding e nas telas existentes.
- O id canônico do modelo continua derivado do nome do arquivo JSON (`pop110i`, `factor125i`).
- `PresetMoto` recebeu metadados opcionais para não inflar mocks de cálculo; `PresetMotoCatalogo` representa o contrato exigido pelo repositório real.
- TASK-REF-31 não altera `src/utils/calculos.ts`; manutenção data-driven fica para TASK-REF-32.x.
- Factor 125i ficou como preset experimental/preparatório, agora automaticamente visível no catálogo por estar em `src/presets/factor125i.json`.

## O Que NÃO Foi Feito (e Por Quê)

- Não foi criado schema Zod de preset: escopo coordenado com TASK-RNF-013.
- Não foi modificada a modelagem de manutenção, `KmUltimaTrocas`, mapas peça↔serviço ou ciclo de revisão: escopo de TASK-REF-32.x.
- Não foi removido `intervaloKmEntrega`: permanece por compatibilidade atual; DT-18 registra a direção futura de fatores de uso.
- Não foi renomeada `LinhaRevisaoHonda`: vazamento de marca fica para TASK-REF-32.x.
- Não foi alterado `src/utils/calculos.ts`, conforme planejamento e restrição do projeto.

## Testes

- `1º npm run test`: 239 verdes antes da implementação.
- `2º npm run lint`: verde antes da implementação.
- `3º npm run test -- src/data/catalogoPresets.test.ts`: 1 teste verde após reforço do contrato.
- `4º npm run lint`: verde após implementação.
- `5º npm run test`: 239 verdes após implementação.
- `6º npm run test -- src/App.smoke.test.tsx src/data/catalogoPresets.test.ts`: 4 testes verdes, cobrindo smoke de onboarding e contrato de catálogo.
- `npm run build`: não executou por limitação do ambiente (`WSL 1 is not supported` / `Could not determine Node.js install directory`).

## Revisão

### Checklist essencial
- [x] ADR Strict aceita antes da implementação.
- [x] Fonte única de carregamento em `src/data/repositorioPresets.ts`.
- [x] `CATALOGO` derivado de JSON e compatível com API anterior.
- [x] Consumidores sem `import.meta.glob` local.
- [x] Segundo preset (`factor125i.json`) aparece pelo contrato sem editar catálogo TS.
- [x] Lint e testes verdes.
- [x] Sem alteração em cálculo/manutenção.

### Riscos residuais
- `ADR-011.md` e este arquivo de conclusão podem estar ignorados pela regra local `docs` em `.gitignore`; se for versionar, usar `git add -f`.
- O repositório ainda usa cast de JSON para `PresetMotoCatalogo`; validação runtime fica para TASK-RNF-013.
- A Factor 125i aparece no catálogo, mas cálculo/manutenção ainda usa mapas legados da Pop até TASK-REF-32.x.

### Segunda passada - revisão independente (02/06/26)

**Modo:** Auto-revisão IA (segunda passada a pedido do humano). Strict.

**Gate revalidado de forma independente:** `npx tsc --noEmit` verde; `npm run lint` verde; `npx vitest run` = 239 testes verdes (14 arquivos, inclui contrato de catálogo e smoke de onboarding).

**✅ Bom**
- Fonte única real: `repositorioPresets.ts` concentra o `import.meta.glob`; os 4 consumidores (`useCustos`, `PaginaInsumos`, `PaginaMaoDeObra`, `DialogEdicaoCusto`) chamam `obterPreset()` sem glob duplicado (resolve REV-001-A01).
- `catalogoModelos.ts` vira fachada derivada preservando a API pública.
- Contrato `PresetMoto` (metadados opcionais p/ mocks) vs `PresetMotoCatalogo` (exige metadados) bem desenhado.
- Teste de contrato garante `idsCatalogo === idsPresets` e metadados derivados batendo - a própria Factor comprova "novo JSON = aparece sozinho".
- `factor125i.json` com dados de alta rastreabilidade: `tabelaFipe` bate 100% com a FIPE; preços de peças/pneus batem com as médias das Tabelas Resumo; nas 7 revisões autorizadas `precoPecas + precoMaoDeObra === precoTotal` e o total bate com a fonte; coerência mecânica (disco+pastilha na frente, tambor atrás).

**🔴 Bloqueante:** nenhum.

**🟡 Importante**
1. Peças específicas por modelo ainda não suportadas: ids novos da Factor (`filtro_oleo`, `filtro_combustivel`, `disco_freio_dianteiro`, `pastilha_freio_dianteiro`) não estão em `MAPA_PECA_PARA_SERVICO` / `MAPA_PECA_PARA_KM_ULTIMA_TROCA` nem em `KmUltimaTrocas` -> caem em modo amortizado, sem ancoragem por km nem serviço independente. Não é regressão (REF-31 não devia tocar `calculos.ts`); é o que falta para "novo modelo = novo JSON" valer na manutenção. Já coberto por TASK-REF-32.x + DT-18. Confirmado pelo humano como necessidade real (marcas diferentes têm peças diferentes).
2. Factor exposta no catálogo parcialmente funcional, sem flag de "experimental". **Decisão do humano (02/06/26): manter visível como está; não será selecionada até a hora de testá-la. Sem ação.**
3. `filtro_oleo` com preços sem fonte (35,00 / 15,00). **RESOLVIDO nesta passada:** humano adicionou as cotações em `precos-pecas.md`; preço atualizado para a média rastreável R$ 29,43 (original) / R$ 19,08 (paralela), com notas de fonte.

**🟢 Sugestão**
- `consumoKmL` 42 / `comBau` 38 é estimativa não validada (a própria nota admite) - validar com telemetria quando houver.
- Split peças/MO das revisões 10k+ é rateio estimado, mas consistente (soma = total) e de baixo impacto (cálculo usa `precoTotal`).
- `PneuPreset` só tem `vidaUtilKm`; valor de entrega da fonte (22.500/15.000) não cabe no modelo atual - implícito em DT-18.
- `codigoFipe` da Pop segue vazio; alinhar quando houver.
- Nomenclatura: campo JSON `modelo` = nome de exibição, mas `perfil.moto.modelo` = id de arquivo; candidato a `modeloId` no futuro.

**Risco residual reavaliado:** o item do `.gitignore` (regra `docs`) foi corrigido pelo humano - `ADR-011.md` e este arquivo não estão mais ignorados.

**Veredito:** APROVADO COM RESSALVAS (sem bloqueantes; ressalva estrutural 1 já enfileirada em REF-32.x; ressalvas 2 e 3 tratadas/decididas).

## Tarefas Geradas pela Revisão

- Nenhuma nova. Permanecem TASK-REF-32.x e TASK-RNF-013 planejadas.

## ADRs Geradas

- ADR-011 aceita nesta tarefa.

## Aprendizados Para o Projeto

- O segundo preset foi útil para forçar a fundação correta: se um JSON real entra em `src/presets`, o catálogo precisa enxergá-lo automaticamente.
- Dados por modo de uso devem evitar campos paralelos editáveis; DT-18 documenta a direção de vida útil base + fatores.
