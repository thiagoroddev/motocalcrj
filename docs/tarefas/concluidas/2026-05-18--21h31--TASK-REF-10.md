# TASK-REF-10 Onboarding Passo7, 8, 9 e PassoConfirmacao shadcn + min-h-touch

- **Status:** Concluída ✅
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** M/M
- **Data origem:** 18/05/26
- **Início:** 18/05/26 ~21h28
- **Conclusão:** 18/05/26 21:31
- **Dependências:** TASK-REF-06 ✅
- **REQ/ADR/DT:**
- **Responsável:** IA + Humano

---

## Execução

- `Passo7.tsx`: 4 `<button>` raw → shadcn `<Button>`; 2 `<input>` → shadcn `<Input>`; helper `toggleClassName(ativo)` extraído para evitar repetição.
- `Passo8.tsx`: 1 `<input>` → shadcn `<Input>` com `min-h-touch`.
- `Passo9.tsx`: 2 card-buttons com emoji → shadcn `<Button>` com `h-auto flex-col items-center`; 1 `<input>` → shadcn `<Input>`.
- `PassoConfirmacao.tsx`: botão "Editar" → `<Button variant="link" className="h-auto p-0 text-xs font-medium text-primary">`.
- `npm run lint -- --fix`: auto-corrigiu 1 erro de formatação prettier em Passo7 (linha muito longa).

---

## Arquivos Alterados

| Arquivo | Mudança |
|---|---|
| `src/pages/onboarding/passos/Passo7.tsx` | 4× `<button>` → `<Button>`; 2× `<input>` → `<Input>`; helper `toggleClassName` |
| `src/pages/onboarding/passos/Passo8.tsx` | 1× `<input>` → `<Input>` |
| `src/pages/onboarding/passos/Passo9.tsx` | 2 card-buttons com emoji → `<Button>`; 1× `<input>` → `<Input>` |
| `src/pages/onboarding/passos/PassoConfirmacao.tsx` | Botão "Editar" → `<Button variant="link">` |

---

## Decisões Tomadas

- **Helper `toggleClassName(ativo)`:** Passo7 tinha 4 botões com o mesmo padrão ativo/inativo extraído como função local para evitar duplicação de string longa de className.
- **`PassoConfirmacao` "Editar":** `variant="link"` do shadcn com `h-auto p-0` mantém o comportamento de link inline sem afetar o layout da sessão.
- **Passo9 cards com emoji:** `h-auto flex-col items-center` sobrepõem o `h-10 flex-row` padrão do shadcn Button. Padrão consistente com `min-h-touch` garantindo área de toque mínima.

---

## Testes

| Comando | Resultado |
|---|---|
| `npm run lint` | 0 erros (1 corrigido por --fix) |
| `npm run test` | 92 testes passaram |
| `npm run build` | CSS: 41.07 kB, JS: 345.47 kB |

---

## Revisão

> Revisão geral da bateria TASK-REF-06 → TASK-REF-10 aplicada aqui por ser a última tarefa concluída do ciclo.

### ✅ Bom

- Todo o onboarding migrado para shadcn `<Button>` / `<Input>` zero `<button>` / `<input>` raw restantes (confirmado por grep).
- Padrão `min-h-touch` (48 px) aplicado consistentemente em todos os elementos interativos.
- Override `text-foreground` no botão primário resolve o conflito `--primary-foreground: rgb(13 19 33)` do tema sem alterar o design token global.
- Helper functions (`toggleClassName`, `cardClassName`, `inputClassName`) eliminam repetição de strings longas de className em Passo6Aluguel, Passo6Responsabilidade e Passo7.
- FIPE dual-track (fast path `codigoFipe` + fallback 4 chamadas) reduz latência quando o código está disponível.
- Estado `'fallback'` com `tabelaFipe` garante que IPVA nunca retorne R$0 por falha da API.
- `tsc`, `lint` e 92 testes verdes em todas as tarefas da série.

### 🔴 Bloqueante

Nenhum.

### 🟡 Importante

Nenhum.

### 🟢 Sugestões

- `tabelaFipe: Record<number, number>` → `Record<string, number>`: chaves JSON são sempre string; o cast anterior funcionava por coerção do JS mas era uma mentira de tipo. **Corrigido inline nesta revisão** em `catalogoModelos.ts` e `Passo3.tsx`.

### ⚪ Observações

- `README.md` continha texto sem formatação markdown ficou para o humano corrigir manualmente.
- `codigoFipe: ""` no `SET_FIPE_CACHE` do caminho fallback é correto: `cacheValida()` não valida `codigoFipe`, apenas `marca`, `modelo` e `anoModelo`.

---

### Veredito: APROVADO ✅

Série concluída sem débito técnico relevante. Única correção de tipo aplicada diretamente nesta revisão.

### Tarefas Geradas pela Revisão

Nenhuma. Correção de tipo `tabelaFipe` aplicada inline.

---

## Tarefas Geradas

Nenhuma. **Série de refatoração do onboarding concluída** (TASK-REF-06 → TASK-REF-10). Todo o onboarding usa shadcn Button/Input com `min-h-touch`.
