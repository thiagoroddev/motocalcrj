# TASK-CHORE-015 - Alinhar constantes de negócio e documentação às fontes canônicas

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Esforço-H/IA:** P/M
- **Data-hora origem:** 01/06/26 19:13
- **Data-hora início:** 04/06/26 19:30
- **Data-hora conclusão:** 04/06/26 20:08
- **Dependências:** —
- **REQ/ADR/DT:** REV-001-A06; fonte canônica `src/data/dados_rj.json`
- **Observações:** Constantes de negócio duplicadas (literais onde já há fonte em `dados_rj.json`) + drifts doc↔código. Executado em sub-itens A/B/C/D. **D revelou um bug de modelagem** (Revisão Geral sempre amortizada) → extraído para TASK-BG-022.

## Planejamento Aprovado

Aprovado em 2 etapas: **A + B (Opção A)** e depois **C + D**.

- **A — Alíquota de IPVA:** novo `src/data/dadosRJ.ts` (singleton validado); `Passo3` lê `dadosRJ.ipva.aliquotaMotos` no lugar do literal `* 0.02` / label "2% a.a.".
- **B — Fator etanol `0.78`:** estender `dadosLocaisSchema` com `autonomiaEtanolFatorReducao`; trocar `* 0.78` por `dadosRJ.autonomiaEtanolFatorReducao` nos 3 consumidores (`PerfilContext`, `PaginaInsumos`, `DialogEdicaoCusto`); `useCustos` consome o módulo compartilhado.
- **C — Doc:** corrigir referência morta a `vite-plugin-pwa` em `contexto-projeto-ai.md` + wording "fallback" da FIPE (resíduo REF-36).
- **D — `Math.ceil(eventosRevisaoNoAno)`:** revalidar a representação da frequência da Revisão Geral.

## Execução
- 04/06/26 19:30: Plano detalhado escrito (aterrou 0.78 em 3 arquivos; schema descartava o fator; D mapeado).
- 04/06/26 19:35: Humano aprovou **A + B, Opção A**. Criado `src/data/dadosRJ.ts`.
- 04/06/26 19:45: Schema estendido com `autonomiaEtanolFatorReducao`. O contrato schema↔tipo (RNF-013, `presetSchema.test` `expectTypeOf`) exigiu espelhar o campo no tipo **`DadosRJ`** (`types/calculos.ts`), o que forçou atualizar `catalogoPresets.test` (`toEqual`) e **5 mocks** de `calculos.test.ts` (fixtures; fator não usado pelo cálculo, valor `0.78`). `useCustos` passou a consumir o módulo (removeu o parse local).
- 04/06/26 19:46: **A** — `Passo3` lê `dadosRJ.ipva.aliquotaMotos` (valor e label derivados). **B** — `0.78` → `dadosRJ.autonomiaEtanolFatorReducao` em `PerfilContext`, `PaginaInsumos`, `DialogEdicaoCusto`.
- 04/06/26 19:50: A+B verde — `tsc` 0, `test` 288, lint limpo. `rg "0.78|0.02" src --glob '!*.test.*'` → só `dados_rj.json`.
- 04/06/26 19:55: Humano aprovou **C + D**. **C** aplicado: `contexto-projeto-ai.md` sem `vite-plugin-pwa` morto (PWA marcado como planejado) e FIPE sem "fallback" (fonte única, ADR-015).
- 04/06/26 19:58: **D** — investigada a origem de `eventosRevisaoNoAno` (= `custos.revisao.detalhes.eventosNoAno`). Aplicado provisoriamente `≈X,XX×` (consistente com amortizados) após confirmação do humano.
- 04/06/26 20:02: Humano corrigiu o modelo: **Revisão Geral segue agenda fixa (6.000 km) → deve ser sempre ancorada**; amortização é só para itens de desgaste sem km da última troca. Confirmado no código que a Revisão Geral roda **sempre proporcional** (`base = (ciclo/KM_CICLO)*kmAnual`) e **nunca recebe `kmUltimaRevisao`/`kmAtual`** para ancorar — bug de modelagem no `calculos.ts` (gated). **Revertido o D provisório** ao status quo (`Math.ceil`); a correção real foi extraída para **TASK-BG-022** (toca o cálculo, fora do escopo de alinhamento de constantes).

## Decisões Tomadas
- **Opção A (módulo `dadosRJ.ts` + estender schema)**, não leitura crua do JSON: elimina a duplicação real (REV-001-A06) e mantém o fator validado.
- **Espelhar `autonomiaEtanolFatorReducao` em `DadosRJ` (tipo):** o contrato schema↔tipo da RNF-013 exige paridade; arquivo de tipos, **não** o `calculos.ts` gated.
- **D não virou mudança de display:** a representação correta (ancorado vs amortizado) depende de corrigir o modelo de custo da Revisão Geral. Resolver no display sem corrigir o cálculo seria maquiar o bug. Revertido e extraído para BG-022.

## O Que NÃO Foi Feito (e Por Quê)
- **Não alterei `src/utils/calculos.ts`:** gated; a correção da Revisão Geral (anchoring) é a TASK-BG-022, com plano/aprovação próprios.
- **Não reescrevi o monólito `Requisitos_..._v6.md`:** snapshot versionado; a verdade ativa (`contexto-projeto-ai.md`, glossário) foi alinhada.
- **Display da frequência da Revisão Geral fica no status quo (`Math.ceil`)** até a BG-022 — quando o cálculo ancorar, a contagem vira inteira naturalmente.

## Testes
- `1º npm run test`: 288 verdes (baseline pós REF-36).
- Modificados: `catalogoPresets.test.ts` (`toEqual` + campo), `calculos.test.ts` (5 mocks `DadosRJ`).
- `último npm run test`: **288 verdes** (18 arquivos).
- `npx tsc --noEmit`: 0. `npm run lint`: limpo.
- `rg "0.78|0.02" src --glob '!*.test.*'`: só `dados_rj.json` (fonte canônica).

## Revisão

### Modo
Auto-revisão IA (módulo 21), contra o código. Verificações: `tsc` 0; `test` 288; `lint` limpo; `rg` dos literais → só a fonte canônica.

### ✅ Bom
- **Duplicação real eliminada:** `dadosRJ.ts` é o único ponto de parse; 4 literais de negócio (`0.02` + 3× `0.78`) substituídos pela fonte validada.
- **Contrato schema↔tipo preservado:** estender o schema forçou (corretamente) espelhar `DadosRJ` e atualizar os testes — a rede da RNF-013 funcionou.
- **Sem mudança numérica:** alíquota e fator seguem idênticos; cálculos verdes.
- **D conduzido com honestidade:** em vez de maquiar o display, a investigação expôs o bug de modelagem e o encaminhou.

### 🔴 Bloqueante
Nenhum.

### 🟡 Importante
- **Bug de modelagem da Revisão Geral** (sempre amortizada; deveria ser ancorada à agenda fixa de 6.000 km). Não corrigido aqui (gated + muda custo) → **TASK-BG-022**.

### 🟢 Sugestão
- Ao adicionar novas constantes regionais a `dados_rj.json`, estendê-las no `dadosLocaisSchema` para manterem-se validadas e no `DadosRJ`.

### Veredito
**APROVADO.** Alinhamento de constantes concluído (A+B+C) com a rede de contratos intacta; D revalidado com encaminhamento correto do bug descoberto.

## Tarefas Geradas pela Revisão
- **TASK-BG-022:** Revisão Geral deve ser ancorada à agenda fixa (6.000 km), não amortizada — corrigir `calcularDetalhesRevisaoAnual`/`calcularEventosRevisaoNoAno` para usar `kmUltimaRevisao ?? kmAtual` como âncora; ajustar o display da frequência. Toca `calculos.ts` (gated) + provável ADR. (adicionada em `em-andamento.md`, planejada)

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma aqui. **Recomendada** para a TASK-BG-022 (modelo de custo: ancorado vs amortizado por tipo de item).

## Aprendizados Para o Projeto
- **Alinhar uma constante pode esticar a rede de contratos:** estender `dadosLocaisSchema` reverberou em `DadosRJ` + 2 testes — esperado quando há teste de paridade schema↔tipo (é a rede funcionando, não atrito).
- **Revalidação honesta > display bonito:** o item D parecia cosmético, mas investigar a origem do número revelou um bug de modelagem de custo. Não maquiar sintoma.

## Revisão Complementar (04/06/26)

### Modo
Revisão IA pós-conclusão, seguindo módulo 21/checklist 40, com foco nos arquivos efetivamente citados pela tarefa.

### Escopo Conferido
- `src/data/dadosRJ.ts`
- `src/data/dados_rj.json`
- `src/schemas/dadosLocaisSchema.ts`
- `src/types/calculos.ts`
- `src/pages/onboarding/passos/Passo3.tsx`
- `src/context/PerfilContext.tsx`
- `src/pages/PaginaInsumos.tsx`
- `src/components/detalhamento/DialogEdicaoCusto.tsx`
- `src/hooks/useCustos.ts`
- `docs/contexto-projeto-ai.md`
- `docs/dominio/_glossario.md`

### ✅ Bom
- `dadosRJ.ts` centraliza o parse do JSON regional e evita parse paralelo em `useCustos`.
- Os consumidores de IPVA/fator de etanol usam `dadosRJ`, não literais de negócio.
- `rg "0.78|0.02|autonomiaEtanolFatorReducao|aliquotaMotos"` confirmou que os literais restantes em produção ficam na fonte canônica (`dados_rj.json`) e que os demais usos passam pelo campo validado.
- `npm run test` passou com **294 testes** e `npm run lint` passou.

### 🔴 Bloqueante
Nenhum.

### 🟡 Importante
Nenhum.

### 🟢 Sugestão
- O tipo `DadosRJ` ainda é mantido manualmente em paralelo ao `dadosLocaisSchema`. A TASK preservou a paridade via teste de tipo, então não é problema atual; se o contrato regional crescer muito, considerar derivar o tipo do schema para reduzir manutenção dupla.

### Veredito
**APROVADO.** A revisão complementar não encontrou regressão nem drift funcional no escopo da TASK-CHORE-015.

### Tarefas Geradas pela Revisão Complementar
- Nenhuma.

### Observação de Verificação
- `npx tsc --noEmit`, `npm exec -- tsc --noEmit` e `./node_modules/.bin/tsc --noEmit` não rodaram neste ambiente por limitação WSL/Node (`WSL 1 is not supported` / `node: not found`). Isso não apareceu em `test`/`lint`, ambos verdes.
