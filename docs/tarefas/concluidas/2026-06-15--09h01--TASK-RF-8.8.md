# TASK-RF-8.8 - Bateria no card "últimas trocas/manutenções" + renomear título

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Desejável
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 15/06/26 08:45
- **Data-hora início:** 15/06/26 08:58
- **Data-hora conclusão:** 15/06/26 09:01
- **Dependências:** -
- **REQ/ADR/DT:** ADR-016 (bateria por tempo)
- **Observações:** A bateria ficava num card "BATERIA" separado (Vida útil em anos) abaixo de `SecaoUltimasManutencoes` (título "KM - últimas trocas/manutenções"). Pedido: trazer a bateria para o mesmo card e renomear o título para "Registro últimas trocas/manutenções". A bateria é por tempo (anos), não km — o seletor foi mantido como está, só realocado.

## Planejamento Aprovado
1. `CardBateria` vira bloco embutível (remove a `<section>` própria → `<div>`), mantendo título "Bateria" + Select de vida útil + texto.
2. `SecaoUltimasManutencoes`: novo prop `bateria`; título "KM - últimas trocas/manutenções" → "Registro últimas trocas/manutenções"; renderiza `CardBateria` embutido após a grade, separado por divisória (`border-t`).
3. `PaginaAjustes` e `Passo5Manutencoes`: deixam de renderizar `CardBateria` como card irmão e passam `bateria` para `SecaoUltimasManutencoes`.
4. Testes.

## Execução
- 08:58: Plano aprovado ("execute"). Pendentes → em-andamento.
- 08:59: `CardBateria` convertido em bloco embutível (`<section>`→`<div>`, sem chrome de card).
- 09:00: `SecaoUltimasManutencoes` ganhou prop `bateria`, título renomeado e `CardBateria` embutido com divisória; `PaginaAjustes` e `Passo5Manutencoes` passam `bateria` e param de renderizar o card separado (imports removidos).
- 09:01: Teste do `SecaoUltimasManutencoes` atualizado (prop `bateria` + caso do título novo e bloco da bateria embutido). Gates verdes.

## Decisões Tomadas
- **`CardBateria` vira bloco embutível em vez de duplicar o controle**: mantém uma fonte única do seletor de vida útil (reusado dentro da seção), sem card próprio. O componente segue testável isoladamente.
- **Bateria mantém o controle por anos** (não virou campo km): o pedido é só de realocação visual; a semântica de tempo da bateria (ADR-016/RF-8.7) é preservada, separada da grade de km por uma divisória.
- **Mesmo tratamento no onboarding** (`Passo5Manutencoes`) e em Ajustes, já que ambos reusam `SecaoUltimasManutencoes`.

## O Que NÃO Foi Feito (e Por Quê)
- Não renomeei o componente `CardBateria` (continua o nome, embora não seja mais um "card"): renomear arrastaria o arquivo/test/imports sem ganho real para esta tarefa.

## Testes
- `npx tsc --noEmit`: APROVADO.
- `npm run lint` (eslint + check-spacing-tokens): APROVADO.
- `npm run test -- SecaoUltimasManutencoes.test.tsx CardBateria.test.tsx App.smoke.test.tsx`: APROVADO (20 verdes).
- `SecaoUltimasManutencoes.test.tsx`: novo caso cobre o título "Registro..." (e ausência do antigo "KM - ...") + presença do seletor de vida útil da bateria embutido. `CardBateria.test.tsx` segue válido (bloco com "Bateria" + select).

## Revisão
- Auto-revisão: realocação visual + rename de título; reuso do controle existente; sem `any`; onboarding e Ajustes cobertos pelo mesmo componente. Smoke do onboarding (passo "Últimas manutenções") verde.

## Tarefas Geradas pela Revisão
- -

## Requisitos Gerados pela Revisão
- -

## ADRs Geradas pela Revisão
- -

## Aprendizados Para o Projeto
- `CardBateria` agora é um bloco embutível (sem chrome de card), renderizado dentro de `SecaoUltimasManutencoes`. O título canônico da seção é "Registro últimas trocas/manutenções".
