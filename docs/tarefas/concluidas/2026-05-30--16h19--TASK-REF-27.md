# TASK-REF-27 — `calcularCustoFinanciamentoAnual` com 5 parâmetros posicionais → objeto de opções

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/P
- **Data origem:** 30/05/26 (gerada pela revisão da TASK-RF-6.18)
- **Data início:** 30/05/26 16:16
- **Data conclusão:** 30/05/26 16:19
- **Dependências:** —
- **REQ/ADR/DT:** contexto em ADR-009 / TASK-RF-6.18
- **Observações:** Refatoração pura. A TASK-RF-6.18 deixou `calcularCustoFinanciamentoAnual` com 5 parâmetros posicionais (3 nuláveis do mesmo par de tipos `number | null` / `string | null`), o que impede o `tsc` de detectar troca de posição numa chamada futura. Convenção módulo 10 / checklist 40 §1: 5+ parâmetros → objeto de opções. Comportamento idêntico.

## Planejamento Aprovado

**O que muda:**
- `src/utils/calculos.ts:571` — interface nomeada `ParamsCustoFinanciamentoAnual` + assinatura `calcularCustoFinanciamentoAnual(params: ParamsCustoFinanciamentoAnual)`; corpo desestrutura no topo, lógica inalterada. ✅
- `src/utils/calculos.ts:716` — chamador `totalFinanciamento` (dentro de `calcularCustosPorCategoria`) passa objeto. ✅
- `src/utils/calculos.test.ts` (~1908) — 7 casos migram para chamada por objeto, mantendo os mesmos valores asseverados (6000 / 1750 / 0 / 6000 / 9600 / 10400 / 0). ✅

**Critérios de aceite:**
- Função aceita um único objeto; nenhuma chamada posicional restante no projeto. ✅
- Comportamento idêntico: 7 testes verdes com os mesmos valores; baseline 186 verdes mantida. ✅
- `npx tsc --noEmit` limpo e `npm run lint` limpo. ✅

**Impacto:** módulo `src/utils/calculos.ts` apenas. Sem impacto em schema, persistência ou UI.
**Riscos:** baixíssimo — único risco era typo na desestruturação, coberto por `tsc` + os 7 testes acoplados.
**Dependências novas:** nenhuma.

## Execução

- 30/05/26 16:16: Plano aprovado pelo humano. Baseline `npm test` = 186 verdes.
- 30/05/26 16:17: Adicionada interface `ParamsCustoFinanciamentoAnual` e convertida assinatura para desestruturação do objeto.
- 30/05/26 16:17: Chamador em `calcularCustosPorCategoria` (linha 716) atualizado para passar objeto.
- 30/05/26 16:18: 7 testes do `describe('calcularCustoFinanciamentoAnual ...')` migrados para chamada por objeto, mesmos valores.
- 30/05/26 16:19: `npm test` 186 verdes, `npx tsc --noEmit` limpo, `npm run lint` limpo. Auto-revisão concluída.

## Decisões Tomadas

- **Interface nomeada exportada (`ParamsCustoFinanciamentoAnual`) em vez de objeto inline anônimo:** dá nome ao contrato, permite reuso/import e melhora a leitura nos pontos de chamada. Era opção do plano; adotada.
- **Desestruturação na assinatura (não no corpo):** mantém a função enxuta e a lista de campos visível na própria assinatura.

## O Que NÃO Foi Feito (e Por Quê)

- **Nenhuma mudança de regra de negócio:** por definição da tarefa (refatoração pura). O afunilamento, os ramos de aluguel e `quitada → 0` permanecem idênticos.

## Testes

- `1º npm run test`: 186 verdes (baseline)
- **Novos:** — (nenhum)
- **Modificados:** `src/utils/calculos.test.ts` (7 casos do `describe('calcularCustoFinanciamentoAnual ...')` migrados de chamada posicional para objeto; valores asseverados inalterados)
- `último npm run test`: 186 verdes
- `npx tsc --noEmit`: limpo
- `npm run lint`: limpo (eslint + check-spacing-tokens)

## Revisão

### Modo
Auto-revisão IA + validação humana

### ✅ Bom
- Contrato da função agora nomeado e auto-documentado (`ParamsCustoFinanciamentoAnual`); ordem dos argumentos deixa de ser fonte de erro silencioso.
- Comportamento preservado bit a bit — mesmos 7 valores asseverados, baseline 186 verdes mantida.
- Escopo cirúrgico: definição + único chamador de produção + testes, na mesma passada.

### 🟡 Importante
- Nenhum.

### 🟢 Sugestão
- Nenhuma.

### Veredito
APROVADO — refatoração pura, sem ressalvas.

## Tarefas Geradas pela Revisão

- — (nenhuma tarefa gerada)

## Requisitos Gerados pela Revisão

- — (nenhum requisito gerado)

## ADRs Geradas

- — (nenhuma ADR gerada)

## Aprendizados Para o Projeto

- **Acoplar testes por posição é um sinal, não um defeito:** os 7 testes quebrarem juntos foi exatamente o que tornou a migração segura. Quando uma assinatura cresce para 5+ parâmetros do mesmo tipo, migrar para objeto de opções cedo evita troca de posição que o `tsc` não pega.
