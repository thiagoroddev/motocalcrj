# TASK-RF-6.32 - Onboarding "Valor de mão de obra" + toggle "Padrão / Estimado" (Strict)

- **Status:** Concluído
- **Modo:** Strict
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data início:** 07/06/26 20:30
- **Data conclusão:** 07/06/26 20:35
- **Dependências:** TASK-RF-6.28 (derivação de avulsos)
- **REQ/ADR/DT:** ADR-013, ADR-014, ADR-018, INV-CALC-2 (ver RF-6.31 item 9)
- **Observações:** Renomear o toggle global de estimativa de M.O. "Só valor real / Incluir ~estimativa"
  → **"Padrão / Estimado"** e entregar a **tela de onboarding** de M.O. (componente; wiring/ordem é da
  RF-6.31 — **opção A** aprovada pelo humano).

## Planejamento Aprovado (opção A)

**Semântica (a maior parte já existia):**
- **Padrão** (global OFF, default): por item, permite estimar (~) individualmente OU digitar valor; editável.
- **Estimado** (global ON): aplica ~ em todos os avulsos sem valor oficial, read-only. Valor oficial sempre prevalece.

**O que mudou:**
- Novo `components/mao-de-obra/ControleEstimativaMaoDeObra.tsx` (Segmentado "Padrão/Estimado" +
  explicação), reusado no popup do Detalhamento (`DialogEdicaoCusto`), em `SecaoPreferencias` (Ajustes)
  e na nova tela de onboarding. Rename num único lugar.
- `CardServico`: texto do modo global → "Modo Estimado".
- Helper `somarMaoDeObraEstimavel` em `maoDeObraEstimada.ts` para o exemplo com/sem.
- Nova tela `pages/onboarding/passos/PassoMaoDeObra.tsx` (PassoLayout + controle + exemplo). **Não
  roteada** nesta task (RF-6.31 faz o wiring na ordem final).
- ADR-013 e ADR-014 atualizadas (renomeação, controle único, passo no onboarding, semântica read-only).
- Smoke atualizado (popup "Padrão / Estimado").

## Execução

- 07/06/26 20:32: criado `ControleEstimativaMaoDeObra.tsx` (Segmentado **"Padrão / Estimado"** +
  explicação), dispatchando `SET_INCLUIR_ESTIMATIVA_MAO_DE_OBRA`.
- Reusado em `DialogEdicaoCusto` e `SecaoPreferencias` — `Segmentado` removido dos dois (rename num só
  lugar). `CardServico`: texto do modo global → "Modo Estimado".
- Helper `somarMaoDeObraEstimavel` em `maoDeObraEstimada.ts` (serviços sem tempário somam 0/não contam).
- Criado `PassoMaoDeObra.tsx` (PassoLayout + controle + exemplo "Padrão × Estimado" com a soma
  estimável). **Não roteado** — wiring/ordem é da RF-6.31 (opção A).
- ADR-013 e ADR-014 atualizadas (renomeação, controle único, passo no onboarding, read-only do Estimado).
- Testes: `ControleEstimativaMaoDeObra.test.tsx`, `somarMaoDeObraEstimavel`, `CardServico.test`
  ("Modo Estimado") e smoke ("Padrão / Estimado").

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 33 arquivos / **384 testes** verdes
  (Windows nativo).

## Revisão

### Modo
Auto-revisão IA + validação humana. Strict (toggle compartilhado + ADR).

### ✅ Bom
- **Rename em um só lugar:** o `Segmentado` saiu de `DialogEdicaoCusto` e `SecaoPreferencias`; ambos e o
  onboarding usam o `ControleEstimativaMaoDeObra`. Sem texto/estado duplicado.
- **Semântica preservada:** "Padrão"=off (editável/estimável por item), "Estimado"=on (read-only ~ em
  todos sem valor) — já existia no `CardServico`; só os rótulos/explicações mudaram. Sem mexer em
  `calculos.ts`.
- **Exemplo com fonte única:** a tela usa `somarMaoDeObraEstimavel` (mesmo `estimarMaoDeObra` do
  cálculo), não um número paralelo.
- **ADR atualizada** (013/014) com a renomeação e o passo no onboarding (revendo o "sem passo" da 013).

### 🔴 Bloqueante / 🟡 Importante
Nenhum.

### 🟢 Sugestão
- O chip do `CardTotalAnual` ainda diz "M.O. valor real / M.O. estimada ~" (status, não o toggle).
  Mantido por ser descritivo; alinhar a "Padrão/Estimado" é opcional.
- `PassoMaoDeObra` fica sem validação **em fluxo** até a RF-6.31 roteá-lo; partes lógicas (controle +
  soma) cobertas por teste.

### Veredito
APROVADO. Código, gate e ADRs OK. O toggle renomeado é o entregável visível; a tela de onboarding entra
em fluxo na RF-6.31.

### Requisitos/ADRs
- ADR-013 e ADR-014 atualizadas (histórico 07/06/26).

## Validação Visual

- **APROVADO (07/06/26):** humano notou que a tela nova de onboarding não aparece no fluxo — **esperado
  pela opção A** (a RF-6.31 fará o wiring) — e deu o ok ("então ok"). O toggle renomeado "Padrão /
  Estimado" (popup do Detalhamento + Ajustes) é o entregável visível desta task.
- **Pendente para a RF-6.31:** validar a tela `PassoMaoDeObra` quando estiver roteada na ordem final.
