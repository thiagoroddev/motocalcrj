# TASK-REF-32.3 - UI de Insumos original-only e grid compacto

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Crítico
- **Urgência:** Imediata
- **Esforço-H/IA:** M/G
- **Data origem:** 02/06/26 22:24
- **Data início:** 03/06/26 09:19
- **Data conclusão:** 03/06/26 09:22
- **Dependências:** TASK-REF-31, TASK-REF-32.1, TASK-REF-32.2
- **REQ/ADR/DT:** REV-001-A02, ADR-011, ADR-012, DT-18
- **Observações:** Executado o recorte MVP da aba Insumos: preço paralelo escondido da UI, campos futuros preservados no código/preset, cards compactados e lista limitada a peças/pneus relevantes fora dos pacotes fixos de revisão autorizada/concessionária.

## Planejamento Aprovado

**O que muda:**
- `src/pages/PaginaInsumos.tsx`: filtrar peças com `incluidoNaRevisaoAutorizada === true`, manter pneus como insumos de desgaste e renderizar peças/pneus em grid responsivo compacto. ✅
- `src/components/custos-pecas/CardItemPreco.tsx`: esconder input/label de `Paralela (R$)` no MVP, mantendo leitura/estado/handlers futuros sem expor ao usuário. ✅
- `src/components/detalhamento/DialogEdicaoCusto.tsx`: garantir que o editor de preço de peça no detalhamento use o mesmo card original-only. ✅
- `src/data/conteudoAjuda.ts`: atualizar ajuda de Insumos para o vocabulário original-only do MVP. ✅
- Testes afetados: adicionar cobertura de render para confirmar ausência de `Paralela (R$)` e ausência de peças cobertas pelo pacote fixo. ✅

**Critérios de aceite:**
- Aba Insumos não mostra inputs ou labels de peça paralela. ✅
- Cards de peças/pneus ocupam menos largura e aparecem em duas colunas quando houver espaço suficiente. ✅
- Peças cobertas por revisão autorizada/concessionária não aparecem como insumo avulso editável. ✅
- Pneus continuam aparecendo como insumos de desgaste. ✅
- Campos/cálculos futuros de paralela permanecem preservados no código. ✅
- `npm run test` e `npm run lint` verdes. ✅

**Impacto:** Insumos, card compartilhado de peça/preço, diálogo de detalhamento que reutiliza o card, ajuda contextual e testes.
**Riscos:** `CardItemPreco` é compartilhado com Detalhamento; mitigado preservando API e alterando apenas a apresentação MVP. `src/utils/calculos.ts` não foi alterado.
**Dependências novas:** nenhuma.

## Execução

- 03/06/26 09:19: Revisão da TASK-REF-32.2 lida; sem bloqueantes. Ressalvas de normalização/custo incompleto encaminhadas para TASK-REF-32.4.
- 03/06/26 09:19: Plano aprovado pelo comando humano "siga para a próxima tarefa"; iniciada TASK-REF-32.3.
- 03/06/26 09:20: Baseline `npm run test` verde antes das alterações.
- 03/06/26 09:21: `CardItemPreco` passou a esconder paralela por padrão; `PaginaInsumos` passou a filtrar peças cobertas por revisão e renderizar grid `sm:grid-cols-2`.
- 03/06/26 09:21: Adicionado smoke test para Insumos MVP: sem paralelas, sem óleo/vela/filtro cobertos por revisão, com kit relação e pneus preservados.
- 03/06/26 09:21: `npm run test` verde com 240 testes.
- 03/06/26 09:22: `npm run lint` verde.

## Decisões Tomadas

- **Não tocar `src/utils/calculos.ts`:** esta tarefa é UI/listagem; política de custo parcial fica para TASK-REF-32.4.
- **Paralela permanece no contrato:** esconder da UI não remove `precoParalela` nem overrides paralelos.
- **Card compartilhado por padrão original-only:** `CardItemPreco` mantém a opção futura `mostrarParalela`, mas nenhum fluxo do MVP passa essa opção.
- **Pneus seguem em Insumos:** pneus são desgaste/peça do usuário mesmo quando a concessionária não executa o serviço avulso.

## O Que NÃO Foi Feito (e Por Quê)

- **Aviso de custo incompleto/mão de obra desconhecida:** escopo da TASK-REF-32.4.
- **Renomear campos internos de paralela ou limpar presets:** descartado pela ADR-012; campos ficam como capacidade futura.
- **Alterar cálculo para forçar original globalmente:** fora do escopo desta UI e protegido pelo gate de `src/utils/calculos.ts`.

## Testes

- `npm run test`: passou antes das alterações, 14 arquivos e 239 testes.
- `npm run test`: passou após as alterações, 14 arquivos e 240 testes.
- `npm run lint`: passou, incluindo `check-spacing-tokens`.

## Revisão

### Modo
Auto-revisão IA

### Bom
- A aba Insumos agora bate com a ADR-012: só preço original visível e sem itens cobertos pelas revisões fixas.
- O layout reduz a largura dos cards em telas médias/grandes sem quebrar mobile estreito.
- O teste de smoke trava os pontos críticos do print: paralelas escondidas, vela/filtro/óleo fora e pneus ainda visíveis.

### Importante
- `CardItemPreco` preserva a capacidade futura de mostrar paralela; se o pós-MVP reativar esse modo, basta passar `mostrarParalela`.
- TASK-REF-32.4 ainda precisa lidar com custo parcial e testes de normalização apontados na segunda revisão da 32.2.

### Veredito
APROVADO - TASK-REF-32.3 concluída e próxima tarefa executável é TASK-REF-32.4.

### Segunda passada - revisão independente (03/06/26)

**Modo:** Auto-revisão IA (segunda passada a pedido do humano). Standard, sob ADR-012.

**Gate revalidado de forma independente:** `npx tsc --noEmit` verde; `npm run lint` verde; `npx vitest run` = 240 testes verdes (14 arquivos; +1 smoke de Insumos MVP).

**✅ Bom**
- Filtro `!incluidoNaRevisaoAutorizada` em `PaginaInsumos` alinhado à ADR-012 e consistente com o cálculo: peças do pacote fixo (óleo, vela, filtros) não viram insumo avulso; kit relação, freios, bateria, embreagem, cilindro e pneus permanecem.
- `CardItemPreco` ganhou `mostrarParalela` (default `false`): esconde input/label de Paralela preservando estado, handlers e contrato para o pós-MVP. Grid adapta para 1 coluna sem paralela; `temOverride` ignora override paralelo quando escondido (não oferece reset de campo invisível).
- Card compartilhado: Insumos e `DialogEdicaoCusto` herdam o default original-only sem mudança extra; `calculos.ts` intocado.
- Pneus preservados como insumo de desgaste (sem `incluidoNaRevisaoAutorizada`, sempre listados).
- Grid responsivo `grid-cols-1 sm:grid-cols-2` com `data-testid` para asserção.
- Ajuda contextual atualizada e honesta: Insumos "apenas preço original", sem aba Independente, Preferências sem modo de revisão.
- Smoke test novo de alto valor (render real): trava Paralela ausente, óleo/vela/filtro fora, kit relação + pneus presentes e o grid de 2 colunas. `inputMode`/`Label` preservados (a11y).

**🔴 Bloqueante:** nenhum.

**🟡 Importante:** nenhum introduzido pela 32.3. A normalização sem teste e o aviso de custo incompleto seguem encaminhados à TASK-REF-32.4.

**🟢 Sugestão**
- O smoke usa `toHaveClass('sm:grid-cols-2')` - asserção acoplada ao breakpoint Tailwind (teste de implementação). Em jsdom é um proxy aceitável para layout responsivo, mas quebra se o mecanismo de grid mudar sem regressão real.
- `CardItemPreco` ainda mantém `localParalela` + `useEffect` de ressincronização mesmo com `mostrarParalela=false` (estado morto enquanto escondido); inofensivo e intencional pela capacidade futura.

**Veredito:** APROVADO (sem bloqueantes nem ressalvas importantes; alinhado à ADR-012, gate verde, boa cobertura de render).

## Tarefas Geradas pela Revisão

- (nenhuma nova; seguir TASK-REF-32.4 e TASK-REF-32.5)

## Requisitos Gerados pela Revisão

- (nenhum requisito novo)

## ADRs Geradas

- (nenhuma ADR nova; execução baseada na ADR-012)

## Aprendizados Para o Projeto

- Esconder uma coluna de edição em card compartilhado é mais seguro quando a API permanece estável e a capacidade futura fica atrás de uma prop.
- A UI de Insumos deve seguir a mesma fronteira do cálculo autorizado: itens cobertos por pacote fixo não precisam de edição avulsa no MVP.
