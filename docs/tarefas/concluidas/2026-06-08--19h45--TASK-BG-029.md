# TASK-BG-029 - Categoria Outros: renomear, bloquear toggles filhos e corrigir Donut < 1%

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Correção / UX
- **Urgência:** ALTA
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 08/06/26 19:37
- **Data-hora início:** 08/06/26 19:41
- **Data-hora conclusão:** 08/06/26 19:45
- **Dependências:** -
- **REQ/ADR/DT:** -
- **Observações:** Três itens correlatos na categoria "Outros" (ex-Imprevistos).

## Planejamento Aprovado

### Correção A — Renomear "Imprevistos" → "Outros"
- `PaginaEstimativa.tsx` L28: label do Donut
- `SecaoImprevistos.tsx` L91, L128: textos da UI

### Correção B — Toggles filhos bloqueados quando global off
- `SecaoImprevistos.tsx`: adicionar `inativoPorPai={!categoriaAtiva}` nos dois blocos de `<Toggle>` filhos (sugeridos + gastosCustom), igualando ao padrão de `SecaoManutencao`.

### Correção C — Donut exibe categorias < 1%
- `PaginaEstimativa.tsx` L90: trocar `.filter((s) => s.porcentagem > 0)` por `.filter((s) => s.valorAnual > 0)`.

## Execução
- 19:41: Plano aprovado. Início da execução.
- 19:42: Correções A, B e C aplicadas nos dois arquivos.
- 19:44: `tsc --noEmit` verde.
- 19:45: `npm run test` verde (404/404).

## Decisões Tomadas
- Usar `valorAnual > 0` em vez de `porcentagem > 0` no filtro do Donut. Isso garante que qualquer categoria com custo real (mesmo que represente 0,1% do total) apareça no gráfico.
- Reutilizar a prop `inativoPorPai` que já existia no componente `Toggle`, sem criar nova lógica.

## O Que NÃO Foi Feito (e Por Quê)
- Auto-desligamento de toggles quando total = 0 (revertido em sessão anterior por bugs de lock). Não tentamos novamente — o usuário removeu esse requisito.

## Testes
- `npm run typecheck`: APROVADO
- `npm run test`: 404 verdes (36 arquivos) — APROVADO

## Revisão
- Revisão Standard: OK. Mudanças pontuais em 2 arquivos, sem novo estado, sem nova action, sem efeito colateral.

## Tarefas Geradas pela Revisão
- -

## Aprendizados Para o Projeto
- O filtro do Donut deve usar valor absoluto, não porcentagem arredondada, para não esconder categorias com custo real mas fatia visual muito pequena.
