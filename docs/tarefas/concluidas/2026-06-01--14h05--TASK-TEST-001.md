# TASK-TEST-001 — Testes do fipeService (BrasilAPI: parsing, rota rápida, fallback e erro)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/M
- **Data origem:** 31/05/26 12:20; extraída da antiga TASK-TEST-001 em 31/05/26
- **Data início:** 01/06/26 14:01
- **Data conclusão:** 01/06/26 14:05
- **Dependências:** —
- **REQ/ADR/DT:** —
- **Observações:** `src/services/fipeService.ts` faz parsing de valor BRL, decide entre rota rápida por `codigoFipe` e rota completa por marca/modelo/ano, e deve retornar `null` em erro/timeout.

## Planejamento Aprovado

**O que muda:**
- `src/services/fipeService.test.ts`: novos testes diretos do serviço FIPE com `global.fetch` mockado.
- Os testes isolam cache interno com `vi.resetModules()` e import dinâmico.

**Cenários cobertos:**
- Rota rápida `/preco/v1/{codigoFipe}` retorna preço e parseia `"R$ 9.999,00"` para `9999`. OK
- Rota rápida sem ano compatível cai para rota completa. OK
- Rota completa faz marcas → modelos → anos → preço e retorna `ResultadoFipe`. OK
- HTTP `ok: false` retorna `null`. OK
- `fetch` rejeitado retorna `null`. OK
- Timeout/abort retorna `null`, sem deixar timer pendurado. OK

**Critérios de aceite:**
- Fluxos principais e falhas do `fipeService` cobertos. OK
- Nenhum acesso real à BrasilAPI durante testes. OK
- `npm run test`, `npm run lint` e `npm run build` verdes. OK

**Impacto:** apenas testes de serviço; sem alteração de runtime.
**Riscos:** testes frágeis por acoplamento ao formato exato das rotas; mitigado porque as rotas são o contrato externo que o serviço monta.
**Dependências novas:** nenhuma.

## Execução

- 01/06/26 14:01: Plano aprovado pelo humano; iniciando baseline de testes.
- 01/06/26 14:02: Baseline `npm run test` verde com 227 testes.
- 01/06/26 14:03: Criado `src/services/fipeService.test.ts` com 6 cenários de serviço e `fetch` mockado.
- 01/06/26 14:03: `npm run test` verde com 233 testes.
- 01/06/26 14:04: `npm run lint` verde.
- 01/06/26 14:05: `npm run build` passou fora do sandbox. Dentro do sandbox falhou por limitação WSL/Node do ambiente, não por erro do projeto.

## Decisões Tomadas

- Usar `vi.resetModules()` antes do import do serviço para isolar os caches internos `_cacheMarca` e `_cacheModelo`.
- Mockar `global.fetch` via `vi.stubGlobal`, sem rede real.
- Simular timeout com `vi.useFakeTimers()` e abort via `AbortSignal`, cobrindo o comportamento real de `fetchJson`.

## O Que NÃO Foi Feito (e Por Quê)

- Não foi alterado `src/services/fipeService.ts`: os cenários passaram com o comportamento atual.
- Não foi criado teste de UI do Passo 3: isso pertence à `TASK-TEST-002`.
- Não foi adicionada dependência: Vitest já cobre mocks, timers e import dinâmico.

## Testes

- `1º npm run test`: 227 testes verdes antes da alteração.
- `2º npm run test`: 233 testes verdes após adicionar os testes do `fipeService`.
- `npm run lint`: verde.
- `npm run build`: verde fora do sandbox; aviso conhecido de chunk grande do Vite mantido.
- `git diff --check`: verde; apenas avisos existentes de CRLF em arquivos não relacionados.

## Revisão

### Modo
Auto-revisão IA (na conclusão) + revisão independente IA (a pedido do humano). Mudança só de teste (sem runtime); cobre o serviço FIPE. Checklist módulo 40 abaixo.

### Checklist essencial (módulo 40)
- [x] Testes / typecheck / lint passam — `fipeService.test.ts` 6 verdes (suíte 239), `tsc` 0, `lint` verde
- [x] Idioma PT; nomenclatura camelCase; sem `any` (`FetchMock` tipado, `as Response`, `unknown` p/ dados)
- [x] **Sem rede real** — `global.fetch` mockado via `vi.stubGlobal`; rota não mockada falha rápido
- [x] Caches de módulo (`_cacheMarca`/`_cacheModelo`) isolados entre testes via `vi.resetModules()` + import dinâmico
- [x] Testa comportamento (parse, escolha de rota, falhas), padrão AAA, nomes descritivos
- [x] Sem mudança de runtime nem dependência nova
- [N/A] UI / acessibilidade / storage — teste de serviço puro

### ✅ Bom
- Cobertura completa do contrato: parse BRL (`"R$ 9.999,00"` → 9999 e `"R$ 10.250,50"` → 10250.5, validando separador de milhar vs decimal), rota rápida por código, **fallback** rota-rápida→completa (5 chamadas), rota completa (4 chamadas, URLs assertadas na ordem) e as 3 falhas (HTTP não-ok, fetch rejeitado, timeout/abort).
- O teste de timeout usa `vi.useFakeTimers()` + `AbortSignal` e avança 8000ms — exercita o caminho real do `AbortController` do `fetchJson`, com `useRealTimers()` no cleanup.
- Isolamento de cache de módulo (`vi.resetModules()`) — sem isso, os testes de rota completa vazariam estado entre si. Detalhe fácil de esquecer.

### 🔴 Bloqueante
- Nenhum.

### 🟡 Importante
- Nenhum.

### 🟢 Sugestão
- Os testes acoplam às URLs exatas da BrasilAPI (`/veiculos/v1/motos/80/1234/2024-1`) — é o contrato externo que o serviço monta, então é aceitável (o teste guarda esse contrato).

### Nota de escopo
- A parte original de TEST-001 (testes da cadeia de migração v5→v23) ficou **obsoleta**: a REF-30 removeu `migracoes.ts`. A task foi corretamente reescopada só para o `fipeService` — sem testes órfãos de código deletado.

### Veredito
APROVADO — testes de serviço completos e bem isolados, sem rede; cobrem parsing, fallback e as 3 falhas. 6 verdes.

## Tarefas Geradas pela Revisão

- — nenhuma.

## Requisitos Gerados pela Revisão

- — nenhum.

## ADRs Geradas

- — nenhuma.

## Aprendizados Para o Projeto

- Serviços que encapsulam APIs externas precisam de testes sem rede real. Aqui, os testes protegem tanto o parsing BRL quanto a estratégia de fallback entre rotas da BrasilAPI.
