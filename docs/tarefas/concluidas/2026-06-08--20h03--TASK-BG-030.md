# TASK-BG-030 - Distribuição de Custos: porcentagens com 1 casa decimal em vez de arredondamento inteiro

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Correção / UX
- **Urgência:** ALTA
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 08/06/26 19:56
- **Data-hora início:** 08/06/26 19:57
- **Data-hora conclusão:** 08/06/26 20:03
- **Dependências:** TASK-BG-029
- **REQ/ADR/DT:** -
- **Observações:** Corrige arredondamento que impedia fatias < 1% de serem exibidas com valor legível.

## Planejamento Aprovado

### Pontos corrigidos
1. `DistribuicaoCustos.tsx` L41: filtro `>= 0.5` removido (filtragem já é por `valorAnual > 0` em PaginaEstimativa)
2. `DistribuicaoCustos.tsx` L82: `Math.round` → formatação com 1 casa decimal quando < 1%
3. `PaginaDetalhamento.tsx` L110-111: função `pct` com mesma regra decimal

### Regra de exibição
- `>= 1%` → inteiro (`"28%"`, `"4%"`)
- `< 1%` → 1 casa decimal com vírgula (`"0,5%"`, `"0,3%"`)

## Execução
- 19:57: Plano aprovado. Início da execução.
- 19:58: Correções aplicadas nos 2 arquivos.
- 20:02: `tsc --noEmit` verde.
- 20:03: `npm run test` verde (404/404).

## Decisões Tomadas
- Manter `Math.round` para valores >= 1% (legibilidade) e usar `toFixed(1)` apenas para < 1% (precisão sem poluição visual).
- Usar `replace('.', ',')` para formato brasileiro consistente com o restante da UI.

## O Que NÃO Foi Feito (e Por Quê)
- Não alteramos o `calcularBreakdownPercentual` em calculos.ts. Ele já retorna valores decimais puros (ex.: 0.496...). O arredondamento era puramente visual, nas views.

## Testes
- `npm run typecheck`: APROVADO
- `npm run test`: 404 verdes (36 arquivos) — APROVADO

## Revisão
- Revisão Standard: OK. Mudanças em 2 arquivos, sem nova lógica de cálculo, sem efeito colateral.

## Tarefas Geradas pela Revisão
- -

## Aprendizados Para o Projeto
- Formatação visual e filtragem visual devem operar sobre dados crus, não sobre dados já arredondados. Arredondar antes de filtrar cria "buracos" invisíveis.
