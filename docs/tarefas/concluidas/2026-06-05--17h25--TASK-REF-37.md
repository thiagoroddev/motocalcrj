# TASK-REF-37 - Higiene em calculos.ts: nomes neutros e remoção de wrapper morto

- **Status:** Concluído
- **Modo:** Standard (arquivo gated)
- **Valor:** Desejável
- **Urgência:** Imediata
- **Esforço-H/IA:** P/M
- **Data origem:** 05/06/26 16:20
- **Data início:** 05/06/26 17:11
- **Data conclusão:** 05/06/26 17:25
- **Dependências:** -
- **REQ/ADR/DT:** REV-002-A02, REV-002-A03, ADR-016, INV-CALC-2
- **Observações:** `src/utils/calculos.ts` foi alterado somente após aprovação explícita do plano pelo humano em 05/06/26 17:20.

## Planejamento Aprovado

### Decisão técnica

Remover `calcularCustoRevisaoAnual`, em vez de ampliar seu contrato. A função não tinha consumidor de produção, só devolvia `.total` de `calcularDetalhesRevisaoAnual` e divergia do caminho real por não aceitar `kmCicloRevisao`. Os testes seriam migrados para a API efetivamente usada em produção.

### O que muda

- `src/utils/calculos.ts`: neutralizar os identificadores multi-marca, remover o wrapper e preservar fórmulas, defaults, ordem das operações e retorno da API canônica.
- `src/utils/calculos.test.ts`: migrar as chamadas para `calcularDetalhesRevisaoAnual(...).total`, mantendo entradas, expectativas e quantidade de cenários.
- Documentação operacional: substituir o wrapper e o fallback Honda como contrato vigente.
- `REV-002`: resolver os achados A02 e A03 após validação.

### Critérios de aceite

- [x] Nenhum dos identificadores multi-marca antigos permanece em `src/`.
- [x] `calcularCustoRevisaoAnual` foi removido sem import ou chamada órfã.
- [x] `calcularDetalhesRevisaoAnual` permanece como API única do cálculo anual de revisão.
- [x] Os 152 testes de `calculos.test.ts` preservam os mesmos cenários e resultados.
- [x] A suíte completa permaneceu com 315 testes verdes.
- [x] Pop 110i mantém 36.000 km e os mesmos resultados; Factor 125i continua usando o ciclo do preset.
- [x] Documentos ativos refletem a API e os nomes atuais.
- [x] `npm run test`, `npm run lint` e `git diff --check` passaram.
- [~] Typecheck e build não iniciaram por limitação do launcher no WSL 1.

### Impacto

Restrito ao cálculo de revisão periódica, seus testes unitários e documentos que descrevem essa API. Não alterou UI, estado, persistência, schemas, presets ou dados.

### Dependências novas

Nenhuma.

## Execução

- 05/06/26 17:11: tarefa removida de `pendentes.md` e aberta para planejamento.
- 05/06/26 17:11: consumidores levantados; wrapper confirmado somente em `calculos.test.ts`.
- 05/06/26 17:11: baseline de 315 testes verdes em 25 arquivos; `calculos.test.ts` com 152 testes.
- 05/06/26 17:20: plano aprovado pelo humano; iniciada a implementação no arquivo gated.
- 05/06/26 17:22: identificadores neutralizados e wrapper removido; fórmulas e defaults preservados.
- 05/06/26 17:22: testes migrados para a API canônica; suíte específica passou com 152/152.
- 05/06/26 17:24: suíte completa passou com 315/315; lint e spacing guard passaram.
- 05/06/26 17:24: `npx tsc --noEmit` e `npm run build` abortaram antes dos compiladores pela limitação WSL 1.
- 05/06/26 17:25: auto-revisão concluída; REV-002-A02/A03 marcados como resolvidos.

## Decisões Tomadas

- **Remover o wrapper:** manter uma API exclusiva para testes aumentaria a superfície pública e permitiria nova divergência do contrato de produção.
- **Preservar o fallback em 36.000 km:** somente o nome foi neutralizado; o valor e seu comportamento permaneceram idênticos.
- **Preservar registros históricos:** tarefas concluídas e evidências da REV mantêm os nomes antigos quando descrevem fatos passados.

## O Que NÃO Foi Feito (e Por Quê)

- **Nenhuma fórmula ou expectativa numérica foi alterada:** esta é uma refatoração estrita, sem redesign.
- **Nenhuma nova ADR:** ADR-016 já define o comportamento; a tarefa apenas alinhou nomes e superfície de API.
- **Nenhuma limpeza adicional de `calculos.ts`:** fora dos achados A02/A03, para manter o toque gated mínimo.

## Testes

- Baseline: `npm run test` com 315 testes verdes em 25 arquivos.
- Específico: `npm run test -- src/utils/calculos.test.ts` com 152/152.
- Final: `npm run test` com 315/315.
- `npm run lint`: aprovado, incluindo `check-spacing-tokens`.
- `git diff --check`: aprovado.
- `npx tsc --noEmit` e `npm run build`: não iniciaram; launcher abortou com `WSL 1 is not supported` e `Could not determine Node.js install directory`.
- Aviso preexistente no smoke test: `DialogContent` sem `Description`/`aria-describedby`; não relacionado à tarefa.

## Revisão

### Modo

Auto-revisão IA pelo módulo 21 e checklist 40, com aprovação humana prévia do plano gated.

### Bom

- O diff em `calculos.ts` não altera operadores, constantes numéricas, defaults ou ordem das operações.
- Todos os testes do wrapper foram preservados na API canônica, com as mesmas entradas e expectativas.
- A superfície pública ficou menor e consistente com o caminho usado em produção.
- Documentação operacional, ADR e rastreabilidade da REV foram sincronizadas no mesmo ciclo.

### Bloqueante

Nenhum.

### Importante

Nenhum.

### Sugestão

Nenhuma.

### Veredito

APROVADO. Refatoração mecânica e localizada, sem alteração observável de cálculo.

## Tarefas Geradas pela Revisão

Nenhuma.

## Requisitos Gerados pela Revisão

Nenhum.

## ADRs Geradas

Nenhuma.

## Aprendizados Para o Projeto

- Helpers exportados usados apenas por testes tendem a divergir da API real; testar a função canônica reduz superfície e melhora a fidelidade da suíte.
- Em arquivo gated, renames devem ser revisados junto das expressões numéricas para demonstrar explicitamente que o comportamento permaneceu intacto.

## Revisão (Segunda passada)

### Modo

Auto-revisão IA independente (módulo 21 + checklist 40), sobre o diff já mergeado na working tree. Revisor distinto da execução original.

### Escopo verificado

- Diff de `src/utils/calculos.ts` (rename + remoção do wrapper) e de `src/utils/calculos.test.ts` (migração para a API canônica).
- Conferência das afirmações dos critérios de aceite contra o código e contra execução real.

### Bom

- O diff de `calculos.ts` é puro rename: nenhum operador, constante numérica (`36000`, `6000`, `3334.62`, `7`), default ou ordem de operação foi tocado. Confirmado linha a linha.
- A remoção de `calcularCustoRevisaoAnual` não deixou import nem chamada órfã — grep em `src/` por `calcularCustoRevisaoAnual`, `quantidadeRevisoesCicloHonda`, `KM_CICLO_REVISAO_HONDA` e `basePacoteHonda` retorna zero ocorrências.
- A migração dos testes preserva entradas e expectativas; cada `calcularCustoRevisaoAnual(...)` virou `calcularDetalhesRevisaoAnual(...).total` sem mudar números.
- As ocorrências remanescentes de "Honda"/"Yamaha" em `src/` são legítimas (campo `marca`, presets, fixtures e comentários de regra de domínio), não identificadores neutralizados pela tarefa.

### Verificação executada (fecha o `[~]` do typecheck/build)

- `npx vitest run src/utils/calculos.test.ts`: **152/152** verdes.
- `npx vitest run` (suíte completa): **315/315** verdes em 25 arquivos.
- `npx tsc --noEmit`: **exit 0, sem erros**. O typecheck que o launcher WSL 1 não conseguiu iniciar na execução original está limpo — a lacuna de verificação apontada no item `[~]` dos critérios de aceite está, portanto, fechada por esta passada.

### Bloqueante

Nenhum.

### Importante

Nenhum.

### Sugestão

**Comentários ainda citam a "constante Honda" após o rename** — ✅ RESOLVIDO em 05/06/26 (nesta passada).
- Onde: `src/utils/calculos.ts` linhas 832 e 837-838, na função `calcularCustosPorCategoria`.
- Problema: os comentários diziam "aplica overrides individuais ao ciclo Honda" e "não a constante Honda". A constante foi renomeada para `KM_CICLO_REVISAO_PADRAO`, então "a constante Honda" referenciava um identificador que não existe mais. Os exemplos "Honda 36.000, Yamaha 5.000×n" continuam válidos como ilustração de domínio; apenas a menção à *constante* ficou órfã.
- Correção aplicada: comentários reescritos de forma neutra — "ao ciclo da concessionária" e "(ex.: 36.000 numa marca, 5.000×n noutra), não o fallback fixo — corrige a amortização de presets com ciclo menor". `npm run lint` (eslint + check-spacing-tokens) e `npx tsc --noEmit` passaram após a edição. Comentários de regra de domínio legítimos (ex.: linha 335 sobre `concessionariaIncluiPeca`) foram preservados.

### Veredito

APROVADO. A segunda passada confirma a primeira: refatoração mecânica, sem alteração observável de cálculo, com suíte e typecheck verdes verificados de forma independente. Apenas uma sugestão cosmética de comentário.

### Tarefas Geradas pela Revisão

Nenhuma (a única sugestão não gera tarefa automaticamente, conforme módulo 21 §6.3).

### Requisitos Gerados pela Revisão

Nenhum.

### ADRs Geradas

Nenhuma.
