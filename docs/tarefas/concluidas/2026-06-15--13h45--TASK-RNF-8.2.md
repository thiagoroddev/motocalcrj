# TASK-RNF-8.2 - Lançamento web/PWA (adaptação responsiva, PWA, deploy Vercel, limpeza do Perfil)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Crítico
- **Urgência:** Imediata
- **Esforço-H/IA:** G/G
- **Data-hora origem:** 15/06/26 10:50
- **Data-hora início:** 15/06/26 10:50
- **Data-hora conclusão:** 15/06/26 13:45
- **Dependências:** - (a dependência original 8.2→8.1/Umami foi removida: PWA entregue sem analytics)
- **REQ/ADR/DT:** REV-004-A02 (registro retroativo); substitui o item RNF-8.2 do backlog
- **Observações:** Registro **retroativo** consolidando o sprint de lançamento (commits `6e9682d` largura, `6ee382e` limpeza, `d7f70f1`→`8932057` PWA/deploy/fixes). Formalizado pela TASK-DOC-017. O app foi publicado em `motocalcrj.vercel.app` (branch `motocalcrjv1`), confirmado pelo humano.

## Planejamento Aprovado
Tornar o app público hoje, sem depender da Play Store:
- App mobile-first não pode esticar 100% na web → coluna central limitada.
- Offline após 1º acesso + instalável → PWA (sem loja).
- Deploy estático na Vercel.
- Tela Perfil: decidir Idioma/Aparência/Termos antes de publicar.

## Execução
- **Largura web:** coluna central `max-w-3xl` (768px) em `App.tsx`; `NavBar.tsx` (fixed) centralizada e limitada à mesma largura; `sheet.tsx` (top/bottom) centralizado e limitado (`left-0 right-0 mx-auto max-w-3xl`, sem `translate`, p/ não brigar com o slide). Celular inalterado (sempre < limite → 100%).
- **PWA:** `vite-plugin-pwa` (devDep) em `vite.config.ts` (guarda VITEST); manifest "MotoCalcRJ" (`display: standalone`, theme/bg, ícones já existentes); precache Workbox do build. Build gera `sw.js`/`manifest.webmanifest`/`registerSW.js`.
- **Deploy:** `vercel.json` com rewrite SPA (BrowserRouter); `index.html` título "MotoCalcRJ" + descrição + tags Open Graph/Twitter; descrição corrigida para **custo operacional** (não só manutenção).
- **Limpeza do Perfil (`PaginaPerfil.tsx`):** removidas as linhas **Idioma** (PT-BR fixo) e **Aparência** (tema claro incompleto — infra `ThemeContext` mantida no código); **Privacidade e Termos** virou clicável abrindo dialog (app 100% local, sem coleta; estimativas/FIPE/concessionárias como referência e possivelmente desatualizadas); versão real 0.1.0.
- **README:** nome MotoCalcRJ, claim de PWA corrigido, seção Deploy.

## Decisões Tomadas
- **Aparência removida** (não só desabilitada): tema claro (`.light`) está incompleto; expor modo claro quebrado é pior que só escuro. Infra de tema preservada para acabamento futuro.
- **Idioma removido:** escopo RJ/PT-BR.
- **Largura 768px** (tablet retrato): escolha do humano após testar 480/672.
- **PWA sem Umami:** RNF-8.2 não dependia de analytics; dependência 8.2→8.1 removida (ver REV-004-A02). Umami (8.1) e TWA (8.3) seguem pendentes e independentes.

## O Que NÃO Foi Feito (e Por Quê)
- **Tema claro completo:** esforço M (paleta + QA em todas as telas); fora do escopo do lançamento. Infra pronta para retomar.
- **Code-splitting:** bundle único ~919 kB (REV-004-A03); pertence ao RNF-9.1 (perf).

## Testes
- tsc OK, lint OK, `npm run test` 466 verdes, `npm run build` OK (artefatos PWA gerados). Deploy Vercel confirmado pelo humano.

## Revisão
Auto-revisão (módulo 21): `localStorage` segue isolado; sem anti-padrões; dialogs/sheets centralizados; gates verdes. Limitação de borda (Sheet portado) resolvida nesta task.

## Aprendizados Para o Projeto
- Vercel publica só o `dist/` — docs/agents/outros não vão pro deploy; não precisa de branch "limpa" para deploy.
- PWA resolve offline+instalável sem Play Store.
- Elementos `fixed`/portados (NavBar, Sheet) não herdam a coluna central — precisam ser centralizados/limitados explicitamente (`max-w-3xl` em 3 lugares; ver REV-004-A04).
