# TASK-CHORE-010 — Remover aba Registros do menu inferior e desativar rota

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data origem:** 18/05/26
- **Data início:** 18/05/26
- **Data conclusão:** 18/05/26
- **Dependências:** ADR-003
- **REQ/ADR/DT:** ADR-003
- **Observações:** Consequência direta da ADR-003 (adiamento da tela de Registros). Mantém o arquivo `PaginaRegistros.tsx` intacto para uso futuro.

## Planejamento Aprovado

**O que muda:**
- `src/components/layout/NavBar.tsx`: remover o objeto `REGISTROS` do array `TABS` (5 itens → 4)
- `src/App.tsx`: remover `import { PaginaRegistros }` e `<Route path="/registros" ...>`

**O que NÃO muda:**
- `src/pages/PaginaRegistros.tsx`: arquivo mantido como inativo (não deletar)
- Catch-all `<Navigate to="/estimativa" replace />` já trata acesso direto à URL `/registros`

**Critérios de aceite:**
- NavBar exibe exatamente 4 abas: ESTIMATIVA, M. OBRA, AUTONOMIA, AJUSTES
- Rota `/registros` não existe mais na árvore de rotas (sem import, sem `<Route>`)
- Acesso direto a `/registros` no browser redireciona para `/estimativa`
- `npx tsc --noEmit` sem erros

**Impacto:** apenas `NavBar.tsx` e `App.tsx`. Nenhum outro módulo referencia a rota `/registros`.
**Riscos:** nenhum relevante. Mudança cirúrgica em 2 arquivos.
**Dependências novas:** nenhuma.

## Execução

- 18/05/26: Plano aprovado pelo humano.
- 18/05/26: Removido objeto `REGISTROS` do array `TABS` em `NavBar.tsx`.
- 18/05/26: Removidos `import PaginaRegistros` e `<Route path="/registros">` em `App.tsx`.
- 18/05/26: `npx tsc --noEmit` — zero erros. Tarefa concluída.

## Decisões Tomadas

- **Manter `PaginaRegistros.tsx`:** preservado para a fase futura (pós-MVP) sem custo de re-criar.

## O que NÃO foi feito (e por quê)

- **Deletar `PaginaRegistros.tsx`:** fora do escopo aprovado. Arquivo mantido inativo para reuso futuro.

## Conclusão

Tarefa de limpeza simples executada sem intercorrências. NavBar agora tem 4 abas (ESTIMATIVA, M. OBRA, AUTONOMIA, AJUSTES). A rota `/registros` foi removida da árvore do React Router; acesso direto redireciona para `/estimativa` pelo catch-all existente.

## Revisão

- [x] `npx tsc --noEmit` — zero erros
- [x] Critérios de aceite cumpridos
- [x] `PaginaRegistros.tsx` intacto (não deletado)
- N/A — sem testes unitários envolvidos (componente de navegação puro)

## Tarefas Geradas

— (nenhuma)

## ADRs Relacionadas

- ADR-003: motivação desta tarefa
