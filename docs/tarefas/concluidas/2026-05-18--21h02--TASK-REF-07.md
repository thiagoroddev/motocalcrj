# TASK-REF-07 - Onboarding Passo1-3 - shadcn + correção IPVA + dual-track FIPE

- **Status:** Concluída ✅
- **Modo:** Standard
- **Valor:** Crítico
- **Urgência:** Imediata
- **Esforço-H/IA:** M/G
- **Data origem:** 18/05/26
- **Início:** 18/05/26 ~21h00
- **Conclusão:** 18/05/26 21:02
- **Dependências:** TASK-REF-06 ✅
- **REQ/ADR/DT:** -
- **Responsável:** IA + Humano

---

## Planejamento Aprovado

**O que mudou:**
- `src/presets/pop110i.json`: `tabelaFipe` com valores por ano (2022-2025)
- `src/data/catalogoModelos.ts`: `codigoFipe` e `tabelaFipe` adicionados à interface `DadosModeloCatalogo` e ao LISTA
- `src/services/fipeService.ts`: rota rápida via `/api/fipe/preco/v1/{codigoFipe}` (1 call) antes da rota completa (4 calls); novo parâmetro opcional `codigoFipe` em `buscarPrecoFipe`
- `src/pages/onboarding/passos/Passo3.tsx`: `<input>` → shadcn `<Input>`; novo estado `'fallback'`; fallback `tabelaFipe` despacha `SET_FIPE_CACHE` garantindo IPVA ≠ R$0
- `src/pages/onboarding/passos/Passo1.tsx`: `<button>` cards → shadcn `<Button>` com `min-h-touch`
- `src/pages/onboarding/passos/Passo2.tsx`: idem Passo1

**Critérios de aceite:**
- IPVA nunca é R$0 se o ano for válido para o modelo ✅
- Passo3 exibe valor estimado mesmo com BrasilAPI retornando 500 ✅
- Todos os card-buttons com `min-h-touch` (48px) ✅
- `npm run lint` → 0 erros ✅
- `npm run test` → 92 testes passam ✅
- `npm run build` → passa ✅

---

## Execução

- Tarefa promovida a em-andamento. Plano aprovado pelo humano.
- `pop110i.json`: campo `tabelaFipe` adicionado com valores aproximados 2022-2025.
- `catalogoModelos.ts`: interface e LISTA atualizados com `codigoFipe` e `tabelaFipe`.
- `fipeService.ts`: função `buscarPrecoPorCodigo` adicionada (rota rápida); `buscarPrecoFipe` aceita `codigoFipe?` opcional.
- `Passo3.tsx`: migrado para shadcn `<Input>`; lógica de fallback implementada com estado `'fallback'`.
- `Passo1.tsx` e `Passo2.tsx`: cards migrados para shadcn `<Button>` com overrides `min-h-touch h-auto`.
- `npm run lint -- --fix`: auto-corrigiu formatação prettier em Passo2 e Passo3.
- lint (0 erros), 92 testes, build ok.

---

## Arquivos Alterados

| Arquivo | Mudança |
|---|---|
| `src/presets/pop110i.json` | Adicionado `tabelaFipe: { 2022: 11800, 2023: 12400, 2024: 13200, 2025: 14500 }` |
| `src/data/catalogoModelos.ts` | `codigoFipe: string` e `tabelaFipe: Record<number, number>` na interface e no LISTA |
| `src/services/fipeService.ts` | Rota rápida `buscarPrecoPorCodigo` + parâmetro `codigoFipe?` em `buscarPrecoFipe` |
| `src/pages/onboarding/passos/Passo3.tsx` | `<input>` → shadcn `<Input>`; estado `'fallback'`; fallback da `tabelaFipe` com `SET_FIPE_CACHE` |
| `src/pages/onboarding/passos/Passo1.tsx` | `<button>` cards → shadcn `<Button>` com `min-h-touch` |
| `src/pages/onboarding/passos/Passo2.tsx` | `<button>` cards → shadcn `<Button>` com `min-h-touch` |

---

## Decisões Tomadas

- **`tabelaFipe` com valores aproximados:** valores estimados para 2022-2025 da Pop 110i (sem confirmação oficial - BrasilAPI indisponível). Serão atualizados quando a API voltar. São suficientes para garantir IPVA ≠ R$0.
- **`codigoFipe` = "" (a confirmar):** rota rápida de 1 call fica inativa até o código ser preenchido. Rota de 4 calls permanece como fallback secundário.
- **Estado `'fallback'` separado de `'ok'`:** permite exibir "(estimativa - FIPE offline)" ao usuário sem alterar o fluxo de conclusão do passo.
- **shadcn `<Button>` para cards de seleção:** `h-auto` e `min-h-touch` sobrepõem o `h-10` do shadcn padrão; `justify-start flex-col items-start` mantém alinhamento à esquerda e suporte a múltiplas linhas (Passo2).

## O que NÃO foi feito e por quê

- **Preencher `codigoFipe` da Pop 110i:** BrasilAPI indisponível (HTTP 500). Documentado como "a confirmar" - atualizar quando API voltar ou via tabela oficial FIPE.
- **Atualizar `tabelaFipe` com valores oficiais:** mesma razão. Os valores atuais são estimativas de mercado.

---

## Testes

| Comando | Resultado |
|---|---|
| `npm run lint` | 0 erros |
| `npm run test` | 92 testes passaram |
| `npm run build` | CSS: 40.76 kB, JS: 345.89 kB |

---

## Tarefas Geradas

Nenhuma nova.
