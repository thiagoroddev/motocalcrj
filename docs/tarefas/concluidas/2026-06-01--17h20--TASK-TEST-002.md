# TASK-TEST-002 - Smoke tests de UI (onboarding→estimativa, Detalhamento) e perfilStorage

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data origem:** 31/05/26 12:20
- **Data início:** 01/06/26 16:57
- **Data conclusão:** 01/06/26 17:20
- **Dependências:** -
- **REQ/ADR/DT:** ADR-010, RNF-10
- **Observações:** proteger fluxos visuais principais e lacunas de `perfilStorage` com testes automatizados; testes de componente usam `// @vitest-environment jsdom`.

## Planejamento Aprovado

**O que muda:**
- `src/App.smoke.test.tsx`: novo smoke de UI com `jsdom`.
- `src/services/perfilStorage.test.ts`: completar lacunas do storage.
- Helpers locais no teste para criar perfil/preset válido e popular `localStorage`.

**Cenários cobertos:**
- Fluxo feliz de onboarding até `/estimativa`, sem rede real e sem "Modelo não encontrado". OK
- Persistência do preset no namespace `estimamoto:v1:*` após `COMMIT_ONBOARDING`. OK
- Render de `/estimativa` a partir de preset salvo. OK
- Render de `/estimativa/detalhamento` a partir de preset salvo. OK
- Toggle de `Alimentação` em Detalhamento altera o total e não quebra o CPK. OK
- `perfilStorage`: `setPresetAtivo(null)` remove a chave, `preservarCorrompido()` não lança quando `localStorage` falha e JSON inválido em presets retorna `[]`. OK

**Critérios de aceite:**
- Fluxo onboarding→estimativa coberto. OK
- `perfilStorage` coberto nas lacunas apontadas. OK
- Nenhum acesso real à BrasilAPI durante testes. OK
- `npm run test`, `npm run lint` e `npm run build` verdes. OK

**Impacto:** testes de UI/integração e testes de storage; sem alteração de runtime.
**Riscos:** smoke de UI ficar frágil por texto de tela; mitigação aplicada: validar marcos principais e rotas, não microcopy decorativa.
**Dependências novas:** nenhuma.

## Execução

- 01/06/26 16:57: Plano aprovado pelo humano; iniciando baseline de testes.
- 01/06/26 16:58: Baseline `npm run test` verde com 233 testes.
- 01/06/26 17:01: Adicionados reforços em `perfilStorage.test.ts` e smoke inicial `App.smoke.test.tsx`; primeira execução falhou por `localStorage` incompleto no jsdom/Node.
- 01/06/26 17:02: Isolado `localStorage` falso no smoke e ajustado seletor do card de total do Detalhamento.
- 01/06/26 17:02: `npm run test` verde com 239 testes.
- 01/06/26 17:19: Corrigido helper de teste para retornar `null` em vez de `undefined`, exigência capturada pelo `tsc`.
- 01/06/26 17:20: `npm run test`, `npm run lint`, `npm run build` e `git diff --check` verdes.

## Decisões Tomadas

- O smoke renderiza o `App` real com `BrowserRouter`, controlando a URL via `window.history.pushState`.
- A BrasilAPI é mockada em `src/App.smoke.test.tsx` via `vi.mock('./services/fipeService')`; não há rede real.
- O teste usa `localStorage` falso porque o ambiente jsdom/Node disponível não expõe a API completa de `localStorage`.
- A regressão direta de seguro da TASK-BG-017 não foi duplicada no smoke porque já há cobertura dedicada em `CampoSeguro.test.tsx`.

## O Que NÃO Foi Feito (e Por Quê)

- Não foi alterado código de produção: a tarefa era cobertura automatizada.
- Não foi criada dependência nova: `@testing-library/react`, `jsdom` e Vitest já estavam no projeto.
- Não foi coberto todo o onboarding com todas as ramificações de financiamento/aluguel; o objetivo aqui era smoke do fluxo feliz principal.

## Testes

- `1º npm run test`: 233 testes verdes antes da alteração.
- `2º npm run test`: falhou no smoke por `localStorage.clear` ausente no ambiente jsdom/Node.
- `3º npm run test`: falhou por seletor amplo demais para valores monetários no Detalhamento.
- `4º npm run test`: 239 testes verdes.
- `5º npm run test`: 239 testes verdes após ajuste de typecheck.
- `npm run lint`: verde.
- `npm run build`: verde fora do sandbox; no sandbox falha por limitação WSL/Node do ambiente. Aviso conhecido de chunk grande do Vite mantido.
- `git diff --check`: verde; apenas avisos existentes de CRLF em arquivos não relacionados.

## Revisão

### Modo
Auto-revisão IA (na conclusão) + revisão independente IA (a pedido do humano). Mudança só de teste (sem runtime); cobre fluxos visuais e storage. Checklist módulo 40 abaixo.

### Checklist essencial (módulo 40)
- [x] Testes / typecheck / lint passam - `npm run test` 239 verdes (App.smoke: 3, perfilStorage: 6), `tsc` 0, `lint` verde
- [x] Idioma PT; nomenclatura camelCase; sem `any` (`as PresetEntry[]`, `instanceof HTMLElement`, `Storage` tipado)
- [x] Ambiente correto - `// @vitest-environment jsdom`; guarda `VITEST`/`React.act` do vite.config aplica
- [x] **Sem rede real** - `fipeService` mockado via `vi.mock` + `mockResolvedValue`; `localStorage` falso isolado por teste
- [x] Sem mudança de runtime nem dependência nova (`@testing-library/react`/`jsdom`/Vitest já existiam)
- [x] Testa comportamento (rotas, persistência, toggle→total), padrão AAA, poucos pontos de asserção
- [x] Lacunas do `perfilStorage` cobertas - `setPresetAtivo(null)` remove chave, JSON inválido → `[]`, `preservarCorrompido` não lança quando `localStorage` falha

### ✅ Bom
- Test 1 **dirige o onboarding REAL** pelos passos (App + BrowserRouter) até `/estimativa`, com FIPE mockado, e assere persistência do preset + ausência de "Modelo não encontrado". É a cobertura ponta-a-ponta que faltava - agora pega regressões de onboarding (BG-019, o guard da RNF-012, etc.) automaticamente.
- Engenharia de ambiente sólida: mock do `fipeService` (zero BrasilAPI), `localStorage` falso (jsdom/Node não expõe a API completa), URL via `pushState`.
- Render dos dois caminhos protegidos (`/estimativa`, `/estimativa/detalhamento`) a partir de preset salvo - confirma `RotaProtegida` + `criarEstadoInicial` juntos.
- `perfilStorage` fecha as 3 lacunas exatas do escopo.

### 🔴 Bloqueante
- Nenhum.

### 🟡 Importante
- Nenhum.

### 🟢 Sugestão
- Seletores do smoke dependem da estrutura do DOM (`closest('div')`, `getAllByText(/^R\$/)`, card por texto) - frágeis a mudanças de UI. Aceitável para smoke; a auto-revisão já sugeriu rotular os toggles de categoria.
- O toggle de Alimentação (Test 3) checa que o total/CPK do Detalhamento muda - é integração; a regressão específica da BG-018 (CPK "sem alimentação" na Estimativa) segue no teste unitário de `calculos`, e a da BG-017 em `CampoSeguro.test`. Complementar, não duplicado.
- Test 1 leva ~1,45s real (debounce de 800ms do FIPE + waits); é o teste mais lento da suíte. Aceitável para um smoke.

### Veredito
APROVADO - smoke de UI real e de alto valor (fecha a lacuna de onboarding e-2-e), storage completo, sem rede, 239 verdes.

## Tarefas Geradas pela Revisão

- - nenhuma.

## Requisitos Gerados pela Revisão

- - nenhum.

## ADRs Geradas

- - nenhuma.

## Aprendizados Para o Projeto

- Smoke tests devem testar o app real o bastante para pegar regressões de rota/estado, mas com poucos pontos de asserção. A combinação `App` + storage falso + mock de serviço externo deu essa cobertura sem transformar o teste em replay frágil de cada detalhe visual.
