# TASK-BG-018 — Estimativa: "custo sem alimentação" subtrai alimentação fora do total filtrado

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/P
- **Data origem:** 31/05/26 12:20
- **Data início:** 31/05/26 13:26
- **Data conclusão:** 31/05/26 13:29
- **Dependências:** —
- **REQ/ADR/DT:** RF-6 (CPK da moto)
- **Observações:** correção restrita ao derivado `granularidadesMoto`; UI já esconde o segundo CPK quando os valores são iguais.

## Planejamento Aprovado

**O que muda:**
- `src/utils/calculos.ts`: subtrair alimentação de `totalMoto` somente quando o filtro de alimentação estiver ativo.
- `src/utils/calculos.test.ts`: cobrir `calcularResultado` com alimentação ligada e desligada.

**Critérios de aceite:**
- Com alimentação desligada, `granularidadesMoto.porKm === granularidades.porKm`. ✅
- Com alimentação ligada, `granularidadesMoto.porKm < granularidades.porKm`. ✅
- `calcularTotalFiltrado` continua respeitando os filtros. ✅
- Testes verdes. ✅

**Impacto:** cálculo da Estimativa.
**Riscos:** baixo; cuidado para não alterar breakdown/distribuição de custos.
**Dependências novas:** nenhuma.

## Execução

- 31/05/26 13:26: Plano aprovado pelo humano; iniciando baseline de testes.
- 31/05/26 13:26: Baseline `npm run test` verde com 208 testes.
- 31/05/26 13:27: `calcularResultado` ajustado para subtrair alimentação de `totalMoto` só quando `filtrosAtivos.alimentacao` estiver ativo.
- 31/05/26 13:27: Adicionados testes de regressão em `calculos.test.ts` para alimentação ligada/desligada.
- 31/05/26 13:28: `npm run test` verde com 210 testes.
- 31/05/26 13:28: `npm run lint` verde.
- 31/05/26 13:29: `npm run build` passou fora do sandbox; dentro do sandbox falhou por ambiente WSL/Node. Build manteve apenas aviso conhecido de chunk grande do Vite.

## Decisões Tomadas

- **Corrigir no orquestrador:** o helper `calcularCustoMotoAnual` permanece simples; a decisão de subtrair ou não depende dos filtros ativos em `calcularResultado`.

## O Que NÃO Foi Feito (e Por Quê)

- **Mudança em `PaginaEstimativa`:** fora do necessário; a tela já omite o segundo CPK quando os valores são iguais.
- **Alteração de breakdown/distribuição:** fora do escopo; `calcularTotalFiltrado`, `calcularBreakdownPercentual` e `calcularBreakdownValores` já respeitam filtros.

## Testes

- `1º npm run test`: 208 testes verdes antes da alteração.
- `npm run test`: 210 testes verdes após alteração.
- `npm run lint`: verde.
- `npm run build`: verde fora do sandbox; aviso de chunk grande do Vite mantido.

## Revisão

### Modo
Auto-revisão IA (na conclusão) + revisão independente IA (a pedido do humano). Toca cálculo de medida
(CPK) → atenção extra e validação humana recomendadas (módulo 21 §10). Checklist módulo 40 abaixo.

### Checklist essencial (módulo 40)
- [x] Testes / typecheck / lint passam — `npm run test` 210 verdes (212 após BG-019), `tsc` 0, `lint` verde
- [x] Idioma PT consistente; nomenclatura camelCase
- [x] Nenhum `any`
- [x] Arquivo certo — mudança em `utils/calculos.ts` (função pura); teste em `calculos.test.ts`
- [x] Invariantes respeitadas — contrato de `calcularCustoMotoAnual` inalterado; `calcularTotalFiltrado` segue respeitando filtros
- [x] Sem `console.log`
- [x] Comentário inline explica o *porquê* do condicional (adicionado após esta revisão)
- [x] Testes novos (2) — padrão AAA, testam comportamento, edge ON/OFF; o caso OFF reprova o código pré-fix
- [N/A] UI / acessibilidade / storage / segurança — mudança puramente de cálculo, sem componente nem IO

### ✅ Bom
- Correção na altitude certa (orquestrador `calcularResultado`), simétrica ao ponto que soma a alimentação ao total; helper `calcularCustoMotoAnual` mantido simples.
- Teste que de fato reprova o bug original: o caso OFF falharia em `totalMoto = total − food`.
- Completude verificada por `grep` — `granularidadesMoto`/`calcularCustoMotoAnual` só usados aqui + no display (`PaginaEstimativa.tsx`, `CardCpk.tsx`); sem ponto órfão subtraindo alimentação.

### 🔴 Bloqueante
- Nenhum.

### 🟡 Importante
- Nenhum.

### 🟢 Sugestão
- Comentário de 1 linha no `alimentacaoNoTotal` explicando o porquê do condicional — **feito** após esta revisão ([calculos.ts:991](../../../src/utils/calculos.ts#L991)).

### Veredito
APROVADO — sem bloqueante nem importante. Por tocar cálculo de medida, recomenda-se o "OK" humano sobre o
diff (1 linha efetiva em `calculos.ts` + 2 testes); a verificação acima está anexada.

## Tarefas Geradas pela Revisão

- — nenhuma.

## Requisitos Gerados pela Revisão

- — nenhum.

## ADRs Geradas

- — nenhuma.

## Aprendizados Para o Projeto

- Derivados de custo precisam considerar se o custo base já foi removido pelos filtros antes de aplicar novas subtrações.
