# TASK-REF-41.1 - Consolidar contrato profissional dos presets por ano

- **Status:** Concluído
- **Modo:** Strict
- **Valor:** Crítico
- **Urgência:** Imediata
- **Esforço-H/IA:** G/G
- **Data-hora origem:** 06/06/26 18:00
- **Data-hora início:** 06/06/26 19:58
- **Data-hora conclusão:** 06/06/26 23:21
- **Dependências:** TASK-REF-42 (concluída)
- **REQ/ADR/DT:** DT-18, ADR-018, ADR-011, ADR-014, INV-CALC-2
- **Observações:** primeira fatia executável da remoção de `PerfilUso`. Consolida os dados estáticos
  sem remover ainda o campo persistido nem os controles visuais, que pertencem às subtarefas 41.2
  e 41.3.

## ADR Vinculada

- **ADR-018:** um único padrão profissional/intenso editável por modelo e ano.
- **ADR-011:** preset JSON continua sendo a fonte única dos dados estáticos do modelo.
- **ADR-014:** vida útil canônica mora no serviço efetivo.
- **Status:** aceitas.

## Análise de Impacto

| Área | Mudança | Risco | Mitigação |
|---|---|---|---|
| Consumo | Dois campos viram referência profissional explícita por ano | Aplicar o consumo de outra geração | Resolver apenas pela chave exata modelo/ano, sem fallback |
| Peças | Remover `intervaloKmEntrega` | Fallback perder intervalo | Manter `intervaloKm` apenas onde ainda há fallback real |
| Pneus | Remover vida útil duplicada quando houver serviço | Mock/teste depender do campo | Serviço efetivo continua canônico; caracterizar ambos os modelos |
| UI transitória | `PerfilUso` ainda existe, mas não escolhe dados distintos | Controle temporariamente sem efeito | Remoção visual pertence à 41.3 e fica explicitamente registrada |
| Cálculo | Assinaturas ainda recebem finalidade nesta fatia | Confundir contrato transitório | Ignorar parâmetro com nome marcado e remover na 41.2 |
| Documentação | Fontes de pesquisa ainda citam dois modos | Apagar evidência histórica | Atualizar apenas docs operacionais; preservar documentos-fonte |

## Planejamento Aprovado

1. Fixar baselines profissionais atuais dos dois presets.
2. Substituir o consumo único por `consumoKmLPorAno`; Pop 2016-2024 usa 54 km/L, Pop 2025 usa
   49,1 km/L e Factor 2017-2025 usa 38 km/L.
3. Remover `intervaloKmEntrega` do tipo, schema e JSONs.
4. Manter `intervaloKm`/`vidaUtilKm` somente como fallback onde o contrato ainda exigir; não apagar
   `kit_revisao` nem driver temporal.
5. Fazer catálogo, onboarding, reducer e resets consumirem a referência exata do modelo/ano, sem
   remover ainda `PerfilUso`.
6. Remover `resolverConsumoEfetivo`, que ficou órfão, e manter `resolverIntervaloPeca` sem branch
   entrega/passageiro até a remoção de `PerfilUso`.
7. Atualizar contratos Zod, testes de catálogo/cálculo e documentação operacional.
8. Executar suíte, lint, typecheck, build e revisão do diff.

## Critérios de Aceite

- Presets possuem consumo profissional explícito por ano.
- `consumoKmLComBau` e `intervaloKmEntrega` não existem no contrato/runtime.
- Pop usa 54 km/L entre 2016-2024 e 49,1 km/L em 2025; Factor usa 38 km/L entre 2017-2025.
- Todo ano FIPE possui consumo e ano sem referência não conclui onboarding.
- Alterar o ano reaplica a autonomia padrão correspondente.
- Serviço efetivo continua sendo fonte canônica de vida útil.
- Componentes sem serviço ou com driver temporal preservam fallback necessário.
- `PerfilUso` continua presente apenas como dívida transitória para 41.2/41.3.
- Testes, lint, typecheck e build passam realmente.

## Execução

- 06/06/26 19:58: revisão da REF-42 aprovada; plano desta subtarefa ativado.
- 06/06/26 20:02: presets consolidados em uma referência profissional: Pop 33 km/L e Factor
  38 km/L; removidos `consumoKmLComBau` e `intervaloKmEntrega` de tipos, schema, catálogo e JSONs.
- 06/06/26 20:04: fallbacks `intervaloKm`/`vidaUtilKm` alinhados aos serviços efetivos dos dois
  modelos. Bateria preservada como driver temporal, sem fallback artificial em km.
- 06/06/26 20:06: onboarding, reducer, Insumos, Detalhamento e resets passaram a consumir a única
  referência. Assinaturas que ainda recebem `PerfilUso` foram mantidas transitoriamente, sem
  branch, para remoção na TASK-REF-41.2.
- 06/06/26 20:08: documentação operacional e histórico da ADR-018 sincronizados; documentos-fonte
  de pesquisa e tarefas concluídas foram preservados como evidência histórica.
- 06/06/26 20:12: regressões reforçadas para garantir autonomia idêntica nos dois valores legados,
  alinhamento fallback-serviço por modelo e exceção temporal da bateria.
- 06/06/26 20:59: decisão humana revisou o consumo da Pop 110i 2016-2024 para 54 km/L, com base em
  telemetria própria de 55 km/L numa Pop 2024 e margem conservadora. O preset continua único por
  modelo.
- 06/06/26 21:06: replanejamento Strict aprovado para diferenciar o consumo dentro do preset por
  ano, sem migration ou compatibilidade com dados locais anteriores.
- 06/06/26 21:10: `consumoKmLPorAno` implementado no tipo, schema, catálogo e presets. Pop
  2016-2024 ficou em 54 km/L, Pop 2025 em 49,1 km/L e Factor 2017-2025 em 38 km/L.
- 06/06/26 21:12: onboarding passou a exibir a referência do ano e bloquear ano ausente; Ajustes
  lista somente anos suportados e reaplica o padrão ao trocar o ano. Resets usam a mesma função
  canônica.
- 06/06/26 21:14: ADR-018, modelagem, glossário, dívida técnica e fontes do preset sincronizados.
  Nenhum caminho de migration foi criado, pois o produto ainda não possui usuários.
- 06/06/26 21:22: auto-revisão encontrou cache FIPE obsoleto ao trocar de um ano com valor para
  outro sem FIPE. Onboarding e `SET_ANO_MOTO` passaram a limpar/atualizar o cache; o cálculo agora
  ignora cache cuja marca, modelo ou ano não coincidam com a moto atual.
- 06/06/26 21:25: smoke passou a caracterizar 2024 → voltar → 2025, garantindo autonomia 49,1 e
  `fipeCache = null`.

## Testes

- `npm run test`: passou, 31 arquivos e 374 testes após a auto-revisão.
- Testes focados finais: 4 arquivos e 79 testes passaram.
- `npm run lint`: passou, incluindo `check-spacing-tokens`.
- TypeScript: zero diagnósticos pela API local `typescript`, usada porque os launchers Linux e
  `cmd.exe` ficaram indisponíveis no WSL 1 desta sessão.
- Build de produção: passou pela API local do Vite; permaneceu apenas o aviso não bloqueante de
  chunk maior que 500 kB.
- `git diff --check`: passou.
- Contrato caracterizado: cobertura de todos os anos FIPE, Pop 2024/2025, Factor 2017/2025,
  rejeição de ano ausente e reaplicação do padrão ao alterar ano.

## Revisão Pendente

- Nova revisão independente do replanejamento estrutural por ano.
- Validação visual do seletor de modelo, referência no passo de ano e lista de anos em Ajustes.

## Revisão Humana (Segunda passada)

Revisão independente solicitada em 06/06/26 20:16, pelo módulo 21. Strict + toca preset, schema e
cálculo → leitura do diff real (presets, tipos, schema, consumo, fallbacks). Tarefa mantida em
andamento, sem conclusão.

### ✅ Bom (confirmado contra o código)

- **Consolidação não-silenciosa:** cada `intervaloKm` colapsa para o antigo `intervaloKmEntrega`
  (caminho profissional) e o consumo para o antigo `comBau` — Pop 36→33, Factor 42→38, pneus Yamaha
  30000/20000→22500/15000. Os números são os valores de uso intenso já existentes, não escolha nova;
  as notas dos presets foram reescritas para "referência profissional", preservando contexto.
- **Limpeza completa do contrato:** `consumoKmLComBau` e `intervaloKmEntrega` saíram de tipo
  (`PecaPreset`/`PresetMoto`), `presetSchema`, `catalogoModelos` e dos dois JSONs. Busca no código
  vivo não acha mais `consumoKmLComBau`, `intervaloKmEntrega` nem `usaComBau`.
- **Seeding coerente ponta a ponta:** ao escolher o modelo, o reducer grava
  `autonomia = modeloDados.consumoKmL` (perfilReducer.ts:103-121), agora 33/38. O cálculo de
  combustível usa o consumo profissional; o `perfilDefaults.autonomia: 36` é só placeholder
  pré-modelo, sobrescrito na seleção. INV-CALC-2 do caminho profissional preservada.
- **Escopo transitório respeitado:** `PerfilUso` foi mantido de propósito (sem branch) para 41.2/41.3,
  e isso está explícito. `resolverIntervaloPeca(_tipoUso)` é param morto numa função viva, removido
  quando `PerfilUso` sair — coberto pela 41.2.
- Driver temporal da bateria preservado (sem fallback artificial em km). Gates: 366/366 testes,
  typecheck e lint verdes (rodados de novo aqui).

### 🔴 Bloqueante

Nenhum.

### 🟡 Importante

**`resolverConsumoEfetivo` ficou órfão (código morto introduzido por esta fatia)**
- Onde: `src/utils/calculos.ts:56` (definição) e `src/utils/calculos.test.ts:210`.
- Problema: a 41.1 inlinou o consumo em `PaginaInsumos` (`catalogo?.consumoKmL`) e no reducer, mas a
  função `resolverConsumoEfetivo(preset, _usaBau)` continua exportada **sem nenhum caller de
  produção** — só o teste a chama. Seu único papel era escolher baú-vs-base, conceito que está sendo
  eliminado, então não tem futuro. O teste (`(presetMock, true|false) === 36`) só documenta que o
  argumento é ignorado: cobertura de comportamento morto, dá falsa sensação de segurança.
- Recomendação: remover a função e o teste agora (trivial) ou, seguindo a convenção do projeto de
  absorver cleanup na task dependente, dobrar a remoção dentro da TASK-REF-41.2 — registrando
  explicitamente no escopo dela. Não deixar como dívida implícita.

### 🟢 Sugestão

- `perfilDefaults.autonomia: 36` não corresponde mais a nenhum modelo (Pop é 33). É inócuo
  (placeholder sobrescrito na seleção), mas vale alinhar ou comentar que é um valor neutro pré-modelo.

### Verificação objetiva (re-executada nesta revisão)

- `npm run typecheck` (`tsc --noEmit`): **verde** — rodou normalmente neste ambiente.
- `npm run test`: **366/366 verdes** em 31 arquivos.
- `npm run lint`: verde, incluindo `check-spacing-tokens`.
- Busca por `consumoKmLComBau`/`intervaloKmEntrega`/`usaComBau` no código vivo: nenhuma ocorrência.

### Veredito

APROVADO COM RESSALVAS. Sem bloqueantes. A consolidação está correta, caracterizada e com seeding
coerente. A única ressalva (🟡) é o código morto `resolverConsumoEfetivo`: remover agora ou absorver
explicitamente na TASK-REF-41.2 antes de fechar o pacote da DT-18. Tarefa permanece **em andamento**
aguardando a decisão humana de conclusão.

### Tarefas Geradas pela Revisão

- Nenhuma nova como tarefa independente. O cleanup de `resolverConsumoEfetivo` deve ser absorvido
  pela TASK-REF-41.2 (ou corrigido na própria 41.1 antes de concluir), não virar fix-up separado.

### Resolução das Ressalvas

- 06/06/26 20:40: a ressalva importante foi confirmada. `resolverConsumoEfetivo` não possuía
  consumidor de produção e foi removida de `calculos.ts`, junto com os dois testes que apenas
  exercitavam o parâmetro morto e sua referência em `calculos-visao.md`.
- A sugestão sobre `perfilPadrao.financeiro.combustiveis.*.autonomia` foi resolvida por
  documentação no código. O valor `36` permanece como fallback positivo pré-modelo, necessário
  para o perfil respeitar o schema; `COMMIT_ONBOARDING` o substitui pela referência profissional
  do preset escolhido.
- Nenhuma pendência técnica da revisão permanece. A TASK-REF-41.2 não precisa mais absorver esse
  cleanup.
- Decisão posterior do proprietário substituiu o baseline de 33 km/L da Pop por 54 km/L. As
  menções a 33 km/L na leitura independente acima registram o estado revisado naquele momento; não
  são mais o valor canônico.
- Gates após a correção: `npm run test` com 364/364 testes, `npm run lint`, `npm run typecheck`,
  `npm run build` e `git diff --check` passaram. O build manteve apenas o aviso não bloqueante de
  chunk maior que 500 kB.

### Veredito Após Resolução

APROVADA tecnicamente, sem bloqueantes ou ressalvas de código. Permanece apenas a validação visual
humana já prevista para a alteração textual do onboarding.

### Reabertura após o veredito

Em 06/06/26 21:06, a decisão humana de suportar autonomia distinta por ano reabriu a subtarefa e
substituiu o contrato de consumo único por modelo. O veredito acima continua válido apenas para o
diff anterior; a implementação por ano aguarda nova revisão independente e validação visual.

## Auto-revisão do contrato por ano

### Modo

Auto-revisão IA completa, Strict, em 06/06/26 21:25. Foram revisados contrato Zod, catálogo,
onboarding, reducer, cache FIPE, cálculo, presets, ADR e testes.

### ✅ Bom

- `consumoKmLPorAno` exige chaves anuais válidas, números positivos e cobertura de todo ano FIPE.
- Resolução usa a chave exata modelo/ano, sem fallback silencioso.
- Pop 2016-2024, Pop 2025 e Factor 2017-2025 estão caracterizadas em testes.
- Alteração de ano reaplica o padrão correspondente, mantendo a edição posterior disponível.
- Não foi criada migration nem compatibilidade para perfis inexistentes.

### 🔴 Bloqueante resolvido

**FIPE de outro ano podia continuar incidindo no IPVA**

- Onde: `Passo3.tsx`, `perfilReducer.ts`, `calculos.ts`.
- Problema: escolher 2024 gravava FIPE; voltar e escolher 2025 não limpava o cache. O cálculo lia
  apenas `fipeCache.valor`, permitindo calcular IPVA de 2025 com a FIPE de 2024.
- Resolução: `SET_FIPE_CACHE` aceita `null`; onboarding limpa FIPE indisponível; `SET_ANO_MOTO`
  atualiza ou limpa o cache; o cálculo só aceita cache com marca, modelo e ano coincidentes.
- Regressão: testes de reducer, cálculo e smoke do fluxo 2024 → voltar → 2025.

### 🔴 Bloqueante resolvido por decisão humana

**Pop 2025 está selecionável com base técnica de outra geração**

- Onde: `pop110i.json`, `catalogoModelos.ts`, `Passo3.tsx` e todos os consumidores de
  `obterPreset(perfil.moto.modelo)`.
- Evidência interna: `_fonte.manual` e `_fonte.anoModelo` limitam peças, revisões e manutenção da
  Pop ao manual 2022-2024, mas `consumoKmLPorAno` torna 2025 selecionável para o preset inteiro.
- Evidência externa: a Honda descreve a Pop 110i ES 2025 como nova geração, com motor inédito,
  partida elétrica e transmissão semiautomática:
  `https://saladeimprensa.honda.com.br/releases/honda-pop-110i-es-2025-ainda-mais-pratica-nova-geracao-traz-motor-inedito-com-partida`.
- Decisão de 06/06/26 22:05: peças, pneus e serviços avulsos são compartilhados por qualquer ano.
  Não haverá preset mecânico separado para 2025.
- A diferença real fica nas revisões fixas Honda, selecionadas por faixa: 2016-2024, 2025-2026 e
  2027. Yamaha mantém uma tabela única para todos os anos.
- Tratamento: TASK-REF-44 criada para modelar a seleção por faixa após validação final do
  levantamento em `docs/dominio/informacoes-modelos-motos/pop110i/revisoes-pop110i.md`.
- Pendências de fonte que não serão resolvidas por suposição:
  - o primeiro título ainda diz `2017-2024`, enquanto a decisão humana define `2016-2024`;
  - na faixa 2025-2026, a revisão de 1.000 km registra peças `R$ 120,44`, mão de obra gratuita e
    total `R$ 120,64`.

### Veredito

APROVADA PARA PROMOÇÃO. O defeito de FIPE foi corrigido, os gates passaram e a decisão humana
eliminou a necessidade de variante mecânica por ano. A seleção de revisões fixas por faixa foi
separada na TASK-REF-44 porque depende da validação final do levantamento e não bloqueia a remoção
de `PerfilUso`.


## Conclusão (arquivamento)

- **Arquivada em** 06/06/26 23:21 após gates verdes confirmados na sessão de conclusão:
  `npm run typecheck`, `npm run build` (só aviso de chunk), `npm run test` (373/373) e
  `npm run lint` passaram.
- A revisão independente do contrato por ano foi feita junto com a TASK-REF-41.2 (mesma working
  tree): consumo por `consumoKmLPorAno`, Pop 54/49,1 fundamentado, fix de FIPE obsoleta e baseline
  de combustível caracterizado — ver o arquivo da TASK-REF-41.2.
- A ressalva da primeira passada (`resolverConsumoEfetivo` órfão) foi resolvida nesta própria fatia.

## Aprendizados Para o Projeto

- Mudança de baseline de cálculo (consumo) deve ser caracterizada e fundamentada no dado, não
  escolhida em silêncio; as `consumoNota`/`consumoFontes` do preset cumprem esse papel.
- Cache derivado de ano/modelo (FIPE) precisa ser invalidado quando a chave muda, senão um valor de
  outra seleção contamina o cálculo.

## DT / Tarefas

- DT-18 permanece aberta até a TASK-REF-41.3 (remoção do Passo 4 informativo da rota).
- Trabalho diferido rastreado: TASK-REF-44 (revisões fixas por faixa de ano) e pendências de fonte
  do levantamento da Pop em `docs/dominio/informacoes-modelos-motos/pop110i/revisoes-pop110i.md`.
