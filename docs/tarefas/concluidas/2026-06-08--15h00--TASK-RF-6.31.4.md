# TASK-RF-6.31.4 - Confirmação (ordem do resumo + linha de M.O.) + regra "último passo" do PassoLayout

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P
- **Data início:** 08/06/26
- **Data conclusão:** 08/06/26 15:00
- **Dependências:** RF-6.31.3
- **REQ/ADR/DT:** ADR-020
- **Observações:** Delta pequeno (links "Editar" já feitos na 6.31.3). (1) PassoLayout: trocar
  `passo === '9'` por `'Próximo'`; (2) resumo: Alimentação antes de Internet; (3) linha "Mão de obra".

## Execução

- 08/06/26: `PassoLayout` — `labelBotao = textoBotao ?? 'Próximo'` (removido o literal morto
  `passo === '9'`; `passo` saiu do destructure por ficar sem uso).
- `PassoConfirmacao` — resumo reordenado: **Alimentação antes de Gastos operacionais (Internet)**
  (casa com o fluxo Seguro→Alimentação→Internet); adicionada a seção **"Mão de obra"** mostrando
  `Padrão`/`Estimado` (editar → `mao-de-obra`). KM última revisão mantida sob Quilometragem (decisão do humano).

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 33 arquivos / 386 testes verdes.

## Revisão

### Modo
Auto-revisão IA + validação humana. UI de revisão + rótulo de botão (sem lógica de cálculo).

### ✅ Bom
- Remove um literal de rota morto (`'9'`) — a confirmação já controla o próprio texto via `textoBotao`.
- Resumo coerente com a ordem do fluxo; nova linha de M.O. reflete o estado real (`incluirEstimativaMaoDeObra`).

### 🔴 Bloqueante / 🟡 Importante
Nenhum.

### Veredito
APROVADO. Código, gate e validação visual humana OK.

## Validação Visual

- **APROVADO (08/06/26):** humano confirmou ("tudo ok"). Ordem e linha de M.O. corretas; botão final
  "Concluir configuração".
