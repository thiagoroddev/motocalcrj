# TASK-BG-038 - Refinamentos pós-BG-037 (clamp km revisão + rótulo "periódica" + popup de ancorados ocultos)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 15/06/26 10:20
- **Data-hora início:** 15/06/26 10:20
- **Data-hora conclusão:** 15/06/26 10:45
- **Dependências:** TASK-BG-037
- **REQ/ADR/DT:** Débito residual do BG-037 (absorvido em task dependente); REV-004-A01 (registro retroativo)
- **Observações:** Registro **retroativo** — o trabalho foi commitado (`0208dd3 TASK-BG-038 concluida`) durante o sprint de lançamento sem o arquivo de conclusão; formalizado pela TASK-DOC-017 (ver REV-004-A01).

## Planejamento Aprovado
Resíduo do BG-037 e refinamentos pedidos em uso:
- Guard de km da última revisão periódica (não aceitar km > kmAtual), igual ao card de trocas.
- Renomear o rótulo do campo para "...periódica".
- Popup "Custos ancorados" mais claro: remover o exemplo confuso, ancorar na rodagem anual e mostrar quando cai a próxima troca.

## Execução
- Clamp de `kmUltimaRevisao` ao `moto.kmAtual` (só quando `kmAtual > 0`) em `SecaoVeiculo.tsx` (Ajustes) e `Passo5Manutencoes.tsx` (onboarding); `max` no input.
- Rótulo "KM última revisão" → "KM última revisão periódica" em `SecaoVeiculo.tsx`, `Passo5Manutencoes.tsx` ("...geral" → "...periódica") e `PassoConfirmacao.tsx`.
- Popup "Custos ancorados" (`SecaoManutencao.tsx`): removido o bloco de exemplo; frase curta com a rodagem anual ("No seu ritmo de ~X km/ano…"); nova coluna **"Faltam"** com `~X meses` (helper `formatarMesesAteTroca` = (próxima troca − km atual) ÷ km/ano × 12, com singular "mês").

## Decisões Tomadas
- Clamp só com `kmAtual > 0` para não travar digitação quando o km atual não foi informado.
- Coluna "Faltam" em meses (escolha do humano) por ser mais acionável que "última troca" ou "vida útil"; não exigiu dado novo no view-model (usa `proximaTrocaKm` + kmAtual/kmAnual).

## Testes
- Novos: `SecaoVeiculo.test.tsx` (clamp ao kmAtual; não capa com kmAtual 0); `SecaoManutencao.test.tsx` (coluna "~13 meses" + item oculto fora da lista).
- Suíte: verde no momento da entrega (parte da contagem que chegou a 466 ao fim do sprint).

## Revisão
Auto-revisão (módulo 21): sem mudança de cálculo; guard espelha o do card de trocas; gates verdes.

## Aprendizados Para o Projeto
- Quando o filtro de exibição esconde algo configurado pelo usuário, ofereça discoverability (onde/quando volta) em vez de reexibir com valor 0.
