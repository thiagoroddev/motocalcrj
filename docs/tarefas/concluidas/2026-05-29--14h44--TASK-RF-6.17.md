# TASK-RF-6.17 — Distribuição de custos: melhorar UI e mostrar R$ por categoria

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/M
- **Data origem:** 25/05/26 08:33
- **Data início:** 29/05/26
- **Data conclusão:** 29/05/26 14:44
- **Dependências:** —
- **REQ/ADR/DT:** —
- **Tasks absorvidas:** TASK-BG-007 (label "Gastos extras" → "Imprevistos").
- **Observações:** Reforma do card `DistribuicaoCustos` da Estimativa: mais apresentável (mobile 375px) + valor em R$ por categoria. Decisões do humano: (1) seletor de período no card, só **Ano · Mês · Sem** (default Mês); (2) layout em lista (donut em cima, legenda full-width embaixo com `label · R$ · %`); (3) absorver BG-007.

## Planejamento Aprovado

**O que muda:**
- `src/utils/calculos.ts`: `calcularBreakdownValores(custos, filtros)` (R$ anual por categoria) e `converterAnualParaPeriodo(anual, periodo, diasAno, horasDia)`. + testes.
- `src/components/SeletorPeriodo.tsx` (movido de `detalhamento/`): compartilhado, prop opcional `periodos?: Periodo[]` (default = 5; Detalhamento inalterado).
- `src/pages/PaginaDetalhamento.tsx`: import de `SeletorPeriodo`/`Periodo`; usar `converterAnualParaPeriodo` (remove função local duplicada).
- `src/components/estimativa/DonutChart.tsx`: `SegmentoDonut` ganha `valorAnual`.
- `src/components/estimativa/DistribuicaoCustos.tsx`: novo layout; estado `periodo` (default 'mes') + `SeletorPeriodo` com `['ano','mes','sem']`; R$ por período.
- `src/pages/PaginaEstimativa.tsx`: `valorAnual` via `calcularBreakdownValores`; passar `diasAno`/`horasDia`; label 'Gastos extras' → 'Imprevistos' (BG-007).

**Critérios de aceite:**
- [x] Cada categoria mostra label, R$ e %, legíveis em 375px.
- [x] Seletor (Ano/Mês/Sem) altera só os R$; donut e % permanecem.
- [x] Default "Mês".
- [x] Label "Imprevistos" no lugar de "Gastos extras".
- [x] Detalhamento idêntico após a extração.
- [x] Lint, testes (com novos do breakdown de valores) e tsc (arquivos da task) verdes.

**Impacto:** card de Estimativa; `calculos.ts`; `SeletorPeriodo` (move) e Detalhamento/CardTotalAnual (import + dedupe). **Riscos:** tocar Detalhamento ao extrair o conversor — mitigado por comportamento idêntico + testes. **Dependências novas:** nenhuma.

## Implementação

- `Periodo` movido para `src/types/calculos.ts` (camada de tipos), evitando que `calculos.ts` dependa de um componente. `SeletorPeriodo` re-exporta o tipo para manter os imports existentes.
- `calcularBreakdownValores` virou a fonte única dos numeradores em R$; `calcularBreakdownPercentual` passou a derivar dela (eliminou a lógica de filtro duplicada entre as duas).
- `converterAnualParaPeriodo` extraída da cópia local do Detalhamento; ganhou guarda `divisor > 0` (evita `Infinity` quando `diasAno === 0` nos períodos dia/hora — melhoria sobre o comportamento anterior).
- `SeletorPeriodo` promovido a componente compartilhado com prop `periodos`; o card de Estimativa exibe só Ano/Mês/Sem, o Detalhamento segue com os cinco.
- Card reescrito: donut centralizado, seletor de período, legenda em lista full-width com `label · R$ · %` (R$ em destaque, % esmaecido, `tabular-nums`).
- BG-007 absorvida no `CATEG_CONFIG` da `PaginaEstimativa`.

## Execução

- 29/05/26: Plano aprovado (seletor só Ano/Mês/Sem). Task movida de `pendentes.md` para andamento.
- 29/05/26: Implementado (tipos, calculos, SeletorPeriodo move, card, página) + 5 testes.
- 29/05/26: `npm run test` 166 verdes; `npm run lint` verde; `npx tsc --noEmit` sem erros no código da task.
- 29/05/26: Validação visual aprovada pelo humano. Ciclo fechado.

## Decisões Tomadas

- **Seletor de período no próprio card** (em vez de período fixo): escolha do humano; só Ano/Mês/Sem para um card pequeno.
- **`calcularBreakdownPercentual` deriva de `calcularBreakdownValores`**: single source of truth dos numeradores, em vez de duplicar a lógica de filtro.
- **`Periodo` na camada de tipos**: respeita a direção de dependência (utils não dependem de componentes).
- **Guarda de divisor 0** no conversor: pequena melhoria de robustez ao extrair (antes podia gerar Infinity).

## O que NÃO foi feito (e por quê)

- **Corrigir os erros de `tsc` em `src/utils/calculos.test.ts`** (`posicao: string` vs union): pré-existentes (TASK-BG-016), fora do escopo. Meus testes apenas deslocaram as linhas dos erros.

## Testes

- `npm run lint`: verde.
- `npm run test`: 166 testes verdes (+5: `calcularBreakdownValores`, `converterAnualParaPeriodo`).
- `npx tsc --noEmit`: sem erros no código da task; restam só os 6 pré-existentes de `calculos.test.ts` (BG-016).

## Tarefas Geradas

Nenhuma.
