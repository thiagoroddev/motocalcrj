# TASK-RF-8.2 — Bateria nos presets (completo Honda / incompleto Yamaha+CB) + tirar do card KM

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante · **Urgência:** Normal · **Esforço-H/IA:** M/M
- **Data-hora origem:** 15/06/26 02:15 · **Data-hora início:** 15/06/26 · **Data-hora conclusão:** 15/06/26 02h35
- **Dependências:** TASK-RF-8.1 (motor) · **REQ/ADR/DT:** ADR-014; parte 2 do épico **TASK-RF-8**

## Planejamento Aprovado

Ligar o motor da RF-8.1 aos presets: o valor **oficial completo** (peça + M.O.) da troca de bateria nos 6 presets Honda, e tirar a bateria do card "KM - últimas trocas" (passa a ter card próprio na RF-8.3).

## Execução

- **6 presets Honda completo** (patch cirúrgico descartável `_patch-bateria-oficial.mjs`, removido): `troca-bateria` → `precoTotalAutorizada` = oficial, `statusPrecoAutorizada: informado`, `concessionariaIncluiPeca: true`. Pop **577,74** / Start **564,74** / Fan-Titan **571,74** / Bros **563,34** / XRE **495,37** (servicos-extras-*.md).
- **Card KM:** removida a `bateria` de `MAPA_PECA_PARA_KM_ULTIMA_TROCA` (`calculos.ts`) e de `LABELS_KM_ULTIMA_TROCA` (`SecaoUltimasManutencoes.tsx`). Bateria é driver temporal — sem âncora em km.
- **CB250F + 7 Yamaha:** mantidos `nao_informado` → **incompletos por status** (sem flag, sem mudança); o motor (RF-8.1) já trata: peça em Insumos + M.O. estimável (0,2h).

## Decisões Tomadas

- **Incompleto via status, não via flag:** com `nao_informado`, `concessionariaInformaPrecoCompleto` é false (→ subseção Incompleto) e `ehPecaCobertaPorServicoCompleto` é false (→ peça aparece). Adicionar `concessionariaIncluiPeca:false` seria redundante/inerte aqui — só seria necessário se a bateria virasse `informado` (aí vale a regra "incompleto = false explícito", ver [[reference-completo-incompleto-concessionaria]]).
- **`kmUltimaTrocas.bateria` permanece** no tipo/schema (legado), só sai da UI/MAPA.

## O Que NÃO Foi Feito (e Por Quê)

- **UI** (card próprio de bateria em Ajustes/onboarding, bateria em Serviços Extras com seletor de anos, próxima troca/atraso no Detalhamento) + **docs/ADR**: é a **TASK-RF-8.3**. Até lá, a bateria amortiza pelo default 3 anos (não editável na UI ainda) — estado consistente.

## Testes

- **Suíte completa: 442 verdes, 0 falhas/skips.** `tsc`/`lint`/`build`: **APROVADO**. Nenhum teste quebrou ao remover a bateria do card KM nem ao setar os oficiais Honda.
- Check end-to-end (vitest temp, removido): bateria fora de `chavesKmUltimaTrocaDoPreset`; Honda completo → `ehPecaCobertaPorServicoCompleto('bateria')` true (peça escondida); Yamaha incompleto → false (peça aparece); revisão Honda inclui `troca-bateria` com `custoAnual ≈ 564,74/3` e `eventosNoAno ≈ 1/3`.

## Revisão

Auto-revisão (Standard): mudança de **dados** (6 presets) + 2 remoções pontuais de UI/MAPA, reusando o motor já testado da RF-8.1. Sem risco de cálculo novo.

## Tarefas Geradas

- **TASK-RF-8.3** (em pendentes): UI da bateria (card Ajustes/onboarding + Serviços Extras + Detalhamento) + docs/ADR-014/016.

## Aprendizados Para o Projeto

- **Patch cirúrgico por string** (regex ancorada em `"id": "troca-bateria"`) edita N presets sem reler cada JSON nem reserializar o arquivo inteiro (diff mínimo) — alternativa ao `JSON.parse`→`stringify` (que reformata tudo).
