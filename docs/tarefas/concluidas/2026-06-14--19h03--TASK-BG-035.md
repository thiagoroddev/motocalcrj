# TASK-BG-035 — Onboarding (passo M.O.): subseções Valor Completo/Incompleto

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante · **Urgência:** Normal · **Esforço-H/IA:** M/M
- **Data-hora origem:** 14/06/26 16:43 · **Data-hora início:** 14/06/26 · **Data-hora conclusão:** 14/06/26 19h03
- **Dependências:** - · **REQ/ADR/DT:** TASK-BG-033, TASK-DOM-5; pré-requisito da fase 2 Honda (TASK-DOM-6)

## Planejamento Aprovado

Fazer o passo "Valor de mão de obra" do onboarding mostrar as subseções **Valor Completo / Valor Incompleto** como a aba Mão de Obra, extraindo um componente compartilhado (DRY). Decisão do humano: manter o card de total **abaixo do toggle**, somando só os incompletos.

## Execução

- 14/06/26: Plano aprovado. Extraídos `src/components/mao-de-obra/ListaServicos.tsx` (+ `BotaoRestaurarTudo`, com `ocultarIntervalo`) e `SecaoServicosExtras.tsx` (subseções Completo/Incompleto + toggle + `slotAposToggle`). `PaginaMaoDeObra` usa o componente (visual idêntico). `PassoMaoDeObra` reescrito (subseções + total dos incompletos via slot, `mostrarTitulo={false}`).
- 14/06/26: Ajustado `PassoMaoDeObra.test.tsx` (toggle agora na subseção Incompleto). Gates verdes (test 445). Validação visual aprovada ("ok"). Concluída.

## Decisões Tomadas

- **Componente compartilhado `SecaoServicosExtras`** (DRY) usado pela aba M.Obra e pelo onboarding — "resolver em um resolve em todos".
- **Total no onboarding:** abaixo do toggle, somando só os incompletos (decisão do humano).
- **`mostrarTitulo={false}`** no onboarding (o PassoLayout já dá o título).

## O Que NÃO Foi Feito (e Por Quê)

- Bug dos pneus em "Últimas manutenções" (detectado na validação) → **TASK-BG-036** (tarefa dedicada, junto do tratamento de M.O. do pneu).

## Testes

- `npm run test`: **445 verdes** — APROVADO. tsc/lint/build: **APROVADO**.
- Validação visual: **APROVADA** (onboarding mostra subseções; aba M.Obra inalterada).

## Revisão

Auto-revisão (Standard): refactor DRY sem mudança visual na aba M.Obra (smoke cobre); onboarding ganhou as subseções via o mesmo componente.

## Tarefas Geradas

- **TASK-BG-036**: pneus não aparecem em "Últimas manutenções" (Ajustes/onboarding) — regressão da RF-6.39 (`chavesKmUltimaTrocaDoPreset` ignora `preset.pneus`) — + tratamento de M.O. do pneu (Honda × Yamaha) + rename Excepcional→Independente.

## Aprendizados Para o Projeto

- `SecaoServicosExtras` é o ponto único das subseções Completo/Incompleto — qualquer ajuste vale para aba M.Obra e onboarding.
