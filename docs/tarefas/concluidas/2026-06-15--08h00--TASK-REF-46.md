# TASK-REF-46 - Ícone de "Alternar predefinição" unificado e compartilhado

- **Status:** Concluído
- **Modo:** Standard (registro retroativo; execução foi rápida/Light)
- **Valor:** Desejável (consistência visual + dedup)
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 15/06/26 07:55
- **Data-hora início:** 15/06/26 07:55
- **Data-hora conclusão:** 15/06/26 08:00
- **Dependências:** TASK-RF-9 (botão no header)
- **REQ/ADR/DT:** -
- **Observações:** Logo após a RF-9, o usuário pediu que o botão do header usasse **exatamente o mesmo ícone** do botão "Alternar predefinição" do Perfil (as setas "repeat"), e não o `IconTrocar` (swap-circle) que a RF-9 tinha usado. O ícone alvo existia apenas como função local `IcTrocar` dentro de `PaginaPerfil.tsx`. Registro retroativo a pedido do usuário ("eu esqueci, registre conforme o ciclo, já como concluída").

## Planejamento Aprovado
1. Elevar o SVG das setas "repeat" (local `IcTrocar` de `PaginaPerfil`) para o módulo compartilhado `components/icons` como `IconAlternar`, parametrizado por `className`.
2. Usar `IconAlternar` em `BotaoAlternarPredefinicao` (header) no lugar do `IconTrocar`.
3. Trocar o `<IcTrocar />` em `PaginaPerfil` por `<IconAlternar className="w-4 h-4" />` e remover a função local duplicada.

**Critérios de aceite:**
- Header e Perfil exibem o mesmo ícone (setas "repeat").
- Fonte única do ícone (sem SVG duplicado).
- `tsc --noEmit` + lint + testes verdes.

## Execução
- 07:55: Pedido do usuário para igualar o ícone ao do Perfil.
- 07:57: `IconAlternar` adicionado a `components/icons/index.tsx` (stroke-based, `strokeLinecap/Linejoin="round"`, aceita `className`).
- 07:58: `BotaoAlternarPredefinicao` passou a importar/usar `IconAlternar`; `PaginaPerfil` migrado para o ícone compartilhado e função local `IcTrocar` removida.
- 07:59: Lint apontou linha em branco dupla deixada pela remoção da função; corrigida.
- 08:00: Gates verdes.

## Decisões Tomadas
- **Novo `IconAlternar` em vez de reaproveitar o `IconTrocar` existente**: são desenhos diferentes (repeat vs swap-circle); o usuário quer o "repeat" do Perfil. O `IconTrocar` antigo foi mantido no módulo (pode estar em uso futuro), mas o `BotaoAlternarPredefinicao` deixou de usá-lo.
- **Migrar `PaginaPerfil` para o ícone compartilhado**: agora justificado (consistência pedida); elimina a duplicação do SVG, que na RF-9 eu deixei de fora por estar fora de escopo.

## O Que NÃO Foi Feito (e Por Quê)
- Não removi o `IconTrocar` (swap-circle) do módulo de ícones — pode ser usado em outro ponto; removê-lo seria fora de escopo.

## Testes
- `npx tsc --noEmit`: APROVADO.
- `npm run lint` (eslint + check-spacing-tokens): APROVADO.
- `npm run test -- BotaoAlternarPredefinicao.test.tsx App.smoke.test.tsx`: APROVADO (18 verdes). Os testes da RF-9 não dependem do desenho do ícone, então seguem válidos sem alteração.

## Revisão
- Auto-revisão: troca puramente visual + extração de ícone para fonte única; sem `any`; sem mudança de comportamento. Header coberto pelo smoke.

## Tarefas Geradas pela Revisão
- -

## Requisitos Gerados pela Revisão
- -

## ADRs Geradas pela Revisão
- -

## Aprendizados Para o Projeto
- O módulo `components/icons` tem dois ícones de troca próximos: `IconTrocar` (swap-circle, Material) e `IconAlternar` (setas "repeat", Feather/lucide). Para "alternar predefinição", o canônico do produto é o `IconAlternar`.
