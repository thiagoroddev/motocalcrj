# TASK-RF-6.30 - Onboarding "Qual o ano da moto?": selecionar + corrigir modelo de dados de consumo/ano

- **Status:** Concluído
- **Modo:** Strict (cresceu: schema/preset/reducer + ADR-018)
- **Valor:** Importante
- **Urgência:** Imediata
- **Data-hora origem:** 06/06/26 22:56
- **Data-hora início:** 07/06/26 06:30
- **Data-hora conclusão:** 07/06/26 14:56
- **Dependências:** -
- **REQ/ADR/DT:** ADR-015, ADR-018, RNF-09, INV-CALC-2
- **Observações:** começou como "trocar input livre de ano por seletor" e cresceu para uma correção
  de modelo de dados (decisão humana 07/06/26): consumo é do **modelo** (não por ano), os **anos**
  vêm da **FIPE**, e a **Pop 110i ES 2025 é outro modelo** (removida da Pop).

## Execução

- 07/06/26 06:30: seletor implementado; humano validou visualmente e apontou 3 pontos: (1) altura do
  select fora do padrão; (2) Pop não deveria ter 2025 (ES é outro modelo); (3) consumo não deveria
  ser "por ano". Conceito esclarecido e aplicado por inteiro.
- **Seletor de ano (`Passo3.tsx`):** usa o mesmo `Select` (Radix/shadcn) do Ajustes (`SecaoVeiculo`),
  resolvendo a altura e a consistência. Anos via `obterAnosModelo` (chaves de `tabelaFipe`, desc);
  default = ano gravado se suportado, senão o mais recente. Removidos `ANO_MIN`/`ANO_MAX` e os avisos
  inalcançáveis. Painel Consumo/FIPE/IPVA preservado.
- **Consumo por modelo (reverte o `consumoKmLPorAno` da 41.1):** `PresetMoto.consumoKmL: number`
  (tipo, schema, catálogo, JSONs). Pop = 54, Factor = 38. `obterConsumoKmLPorAno` → `obterConsumoKmL`;
  novo `obterAnosModelo`. Removido o `superRefine` que exigia consumo por ano FIPE.
- **Pop 2025 removida:** `consumoKmLPorAno {...2025:49.1}` saiu; `consumoNota`/`consumoFontes`
  reescritas (Pop 110i ES é outro modelo). `tabelaFipe` da Pop já era 2016-2024.
- **`SET_ANO_MOTO` simplificado:** trocar o ano não recalcula autonomia (consumo é do modelo); só
  atualiza o ano e a FIPE (que é por ano). `COMMIT_ONBOARDING` semeia a autonomia do `consumoKmL` do
  modelo.
- **Consumidores ajustados:** `PaginaInsumos`, `DialogEdicaoCusto`, `onboardingGuards`, `SecaoVeiculo`.
- **Bug compartilhado do Select corrigido:** o `SelectContent` (`components/ui/select.tsx`) usava
  `bg-popover`/`text-popover-foreground` — tokens do shadcn padrão que este tema (Tailwind v4) **não
  define** → dropdown transparente (só borda) desde o início do projeto, em TODOS os selects.
  Trocado por `bg-card text-card-foreground` + `border-muted`. Corrige também o ano em Ajustes.
- **Docs:** ADR-018 (item 10 + histórico corretivo), entidade-preset-moto, bloco-moto, dados_rj nota.
- **Testes:** smoke (aceita o ano default; sem 2025), catalogoPresets (consumo por modelo + anos via
  FIPE sem 2025), presetSchema (consumoKmL), calculos (presetMock + baseline), PerfilContext
  (SET_ANO_MOTO preserva autonomia; COMMIT independe do ano).

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 32 arquivos / 377 testes verdes.
- **APROVADO:** `npm run build`.
- `obterAnosModelo('pop110i')` = [2024..2016] (sem 2025); `obterConsumoKmL('pop110i')` = 54.

## Validação Visual (humana)

- 07/06/26: humano aprovou ("ficou ok"). Antes disso, dois achados corrigidos na validação:
  (1) seletor nativo trocado pelo `Select` (Radix) do Ajustes, resolvendo a altura/consistência;
  (2) **bug compartilhado** — todos os dropdowns estavam transparentes (tema sem token `popover`),
  corrigido no `components/ui/select.tsx` (`bg-card`), valendo para o app inteiro.

## Decisões Tomadas

- **Consumo é do modelo, não por ano:** `preset.consumoKmL` (valor único). Reverte o `consumoKmLPorAno`
  da 41.1 — consumo vem de manual/INMETRO/relato, não da FIPE; não varia entre anos do mesmo modelo.
- **Anos do modelo vêm da `tabelaFipe`:** a FIPE precifica cada ano-modelo, então é a fonte natural
  de "quais anos existem". `obterAnosModelo` centraliza.
- **Pop 110i ES 2025 é outro modelo:** removida do preset da Pop 110i (2016-2024). Entra como preset
  próprio no futuro.
- **Seletor = `Select` do design system** (não nativo): consistente com o Ajustes e sem o problema de
  altura.

## Aprendizado Para o Projeto

- "Fonte de dado" é por grandeza: FIPE = valor (por ano); consumo = modelo; anos do modelo = a própria
  FIPE. Não acoplar consumo a ano nem usá-lo como fonte de anos.
- Bug latente em componente de design só aparece quando o componente passa a ser usado de fato — o
  `Select` estava transparente desde o início porque mal era usado.

## Relacionado

- **TASK-REF-44** atualizada: trata só preços de revisão fixa Honda por faixa de ano; "Pop 2025" não
  é faixa, é outro modelo (Pop 110i ES), que entra como preset próprio no futuro.
