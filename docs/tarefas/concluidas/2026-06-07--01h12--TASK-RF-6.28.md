# TASK-RF-6.28 - Passo 4 vira editor de vida útil dos serviços avulsos

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Data-hora origem:** 06/06/26 17:42
- **Data-hora início:** 07/06/26 00:20
- **Data-hora conclusão:** 07/06/26 02:38
- **Dependências:** - (a TASK-REF-41.3 foi cancelada; o Passo 4 não é removido)
- **REQ/ADR/DT:** DT-18, ADR-014, ADR-018, RNF-09
- **Observações:** decisão humana de 07/06/26 — o Passo 4 não é removido; passa a mostrar os cards de
  vida útil das peças avulsas. Encerra a DT-18 após a validação visual.

## Execução

- 07/06/26 00:20: plano aprovado; implementação iniciada.
- Novo componente `src/components/onboarding/CardVidaUtilOnboarding.tsx`: card enxuto (ícone + nome +
  um input de km). Estado local string + commit no `onBlur` (BG-017). Só marca
  `intervaloKmInformadoUsuario: true` quando o valor muda; valor inválido (<=0) reverte sem despachar.
  Despacha a mesma fonte da Mão de Obra (`SET_SERVICO_INDEPENDENTE`, REF-42).
- `src/pages/onboarding/passos/Passo4.tsx`: deixou de ser o card "Uso profissional e intenso"; agora
  deriva os avulsos via `normalizarPerfilMvp(perfil, preset).servicosIndependentes` filtrados por
  `!ehExcepcional && !incluidoNaRevisaoAutorizada && intervalKm > 0` (mesma regra da aba Mão de Obra),
  com aviso de que são estimativas ajustáveis ali ou depois na Mão de Obra. Passo NÃO removido.
- `src/pages/onboarding/onboardingUtils.ts`: corrigidos os labels/percentuais inconsistentes do
  `CONFIG_PASSOS` (limpeza herdada da 41.3 cancelada): `'6/responsabilidade'` deixou de ser
  "PASSO 8 DE 9" e virou "PASSO 6 DE 9"; percentuais dos sub-passos 6 tornados monotônicos
  (68/68/70). Contagem mantida em 9 passos.
- `src/App.smoke.test.tsx`: o fluxo agora interage com o Passo 4 "Vida útil das peças" em vez do
  aviso; asserções obsoletas de entrega/passageiro removidas.
- Novo `src/components/onboarding/CardVidaUtilOnboarding.test.tsx`: 4 casos (render mínimo; edição
  despacha com a flag; sem mudança não despacha; valor inválido reverte).

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — exit 0, 32 arquivos / 377 testes verdes.
- **APROVADO:** `npm run build` — só o aviso pré-existente de chunk > 500 kB.
- **APROVADO:** `npm run lint` com `check-spacing-tokens`.

## Validação Visual (humana)

- 07/06/26: humano validou o Passo 4 por captura de tela — aprovado ("ficou bom").
- Ajuste 1 pedido: faltava a unidade "km" visível nos inputs. Adicionado sufixo "km" sobreposto
  (`pr-9` + span absoluto); o `aria-label` já dizia km.
- Ajuste 2 pedido: o último input (kit cilindro, 102000) cortava o número. Largura do campo
  aumentada de `w-28` para `w-36`. Revalidado: gates verdes.

## Decisões Tomadas

- **Card enxuto dedicado** em vez de reusar o `CardServico` (pesado: preço, estimativa de M.O.,
  reset, avisos). Single-responsibility para o contexto de onboarding.
- **Passo 4 mantido** (9 passos) — a remoção (TASK-REF-41.3) foi cancelada por retrabalho.
- **Só marca procedência quando o valor muda** (guarda `num === servico.intervalKm`), para não
  marcar como editados os avulsos que o usuário só tabula sem alterar.

## DT

- **Encerra a DT-18** (último item da dívida técnica vigente). O onboarding passou a expor os campos
  editáveis de vida útil, que era o resíduo da DT-18 após a remoção do `PerfilUso`.

## Aprendizados Para o Projeto

- Reaproveitar a mesma fonte de dados de uma feature existente (avulsos da Mão de Obra via
  `normalizarPerfilMvp` + flag da REF-42) evita estado paralelo e mantém onboarding e telas em sincronia.
