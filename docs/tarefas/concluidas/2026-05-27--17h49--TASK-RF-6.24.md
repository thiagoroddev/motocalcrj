# TASK-RF-6.24 - Revisar modelagem do Kit revisão: consumíveis/juntas variáveis por ciclo

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Imediata
- **Esforço-H/IA:** M/M
- **Data origem:** 27/05/26
- **Data início:** 27/05/26 17:43
- **Data conclusão:** 27/05/26 17:49
- **Dependências:** TASK-RF-6.13
- **REQ/ADR/DT:** ADR-006
- **Observações:** Usuário confirmou que o kit revisão representa juntas/anéis/vedações necessários em revisões periódicas com verificação de válvulas. Deve entrar automaticamente no modo independente; no modo autorizado permanece fora do cálculo por peça para não duplicar o pacote Honda. Como não é última troca editável, sai dos Ajustes.

## Planejamento Aprovado

**O que muda:**
- `src/presets/pop110i.json`: manter `kit_revisao` como custo recorrente de 6.000 km, com nota mais clara: juntas/vedações de revisão, sem vela/filtro de ar porque esses já são peças próprias.
- `src/utils/calculos.ts`: remover `kit_revisao` de `MAPA_PECA_PARA_KM_ULTIMA_TROCA`, para ele nunca ser ancorado por km informado em Ajustes.
- `src/components/ajustes/SecaoUltimasManutencoes.tsx`: remover "Kit revisão" da seção "Quilometragem das últimas trocas/manutenções".
- `src/types/perfil.ts` e `src/context/PerfilContext.tsx`: limpar o campo `kitRevisao` do modelo/defaults, com migration v19->v20 para descartar dado legado.
- `src/utils/calculos.test.ts` e `src/context/PerfilContext.test.ts`: atualizar testes para garantir que o kit é automático no independente, excluído no autorizado, e ignorado mesmo se houver `kitRevisao` legado salvo.
- Docs vivos: ajustar `docs/contexto-projeto-ai.md`, `docs/arquitetura/estado_inicial.md` e o registro da task para refletir que `kit_revisao` não é mais rastreável em Ajustes.

**Critérios de aceite:**
- ✅ Modo independente inclui `kit_revisao` automaticamente a cada 6.000 km.
- ✅ Modo autorizado continua sem somar `kit_revisao` por fora.
- ✅ "Kit revisão" não aparece em Ajustes -> "Quilometragem das últimas trocas/manutenções".
- ✅ `kmUltimaTrocas.kitRevisao` legado não altera cálculo.
- ✅ Vela e filtro de ar continuam separados e sem dupla contagem no autorizado.
- ⚠️ Testes e lint verdes; typecheck/build não executaram por limitação WSL/Node do ambiente atual.

**Impacto:** cálculo de manutenção, Ajustes, migration de perfil e documentação viva.
**Riscos:** confundir "kit revisão" como peça de desgaste editável; mitigado removendo a ancoragem por km e documentando como consumível recorrente de revisão. A exceção da revisão de 1.000 km fica fora do modelo atual, porque o cálculo independente trabalha por ciclo regular de 6.000 km, não por agenda exata da primeira revisão.
**Dependências novas:** nenhuma.

## Execução

- 27/05/26 17:43: Plano aprovado pelo humano.
- 27/05/26 17:43: Baseline `npm run test`: 157 testes verdes.
- 27/05/26 17:45: Removido `kit_revisao` do mapa de ancoragem por `kmUltimaTrocas` e da UI de "Quilometragem das últimas trocas/manutenções".
- 27/05/26 17:46: Schema atualizado para v20; migration v19->v20 remove `kitRevisao` legado preservando os demais campos.
- 27/05/26 17:47: Preset `pop110i` documentado como base de juntas/anéis/vedações recorrentes; vela e filtro seguem separados.
- 27/05/26 17:48: Testes atualizados para cobrir kit automático no independente, exclusão no autorizado e legado `kitRevisao` ignorado.
- 27/05/26 17:49: Docs vivos sincronizados: `contexto-projeto-ai.md`, `estado_inicial.md`, glossário/modelagem/requisitos e `pendentes.md`.

## Decisões Tomadas

- **Kit revisão não é última troca editável:** representa consumíveis recorrentes de revisão (juntas/anéis/vedações), então fica automático no cálculo independente e sai do card de Ajustes.
- **Modo autorizado preservado:** `incluidoNaRevisaoAutorizada: true` continua evitando dupla contagem com os pacotes Honda.
- **Schema v20:** remove `kitRevisao` de `KmUltimaTrocas`; migration limpa dados legados sem afetar demais campos.

## O que NÃO foi feito (e por quê)

- **Agenda exata da primeira revisão de 1.000 km:** fora do modelo atual. O cálculo independente usa ciclo regular de 6.000 km.
- **Typecheck/build:** não executaram por falha de ambiente WSL/Node antes da compilação.

## Testes

- `1o npm run test`: 157 verdes.
- `2o npm run test`: 159 verdes.
- `npm run lint`: verde.
- `npx tsc --noEmit`: não executou no ambiente atual (`WSL 1 is not supported. Could not determine Node.js install directory`).
- `./node_modules/.bin/tsc --noEmit`: não executou porque o shim local não encontrou `node` no PATH Linux.
- `npm run build`: não executou pelo mesmo erro de ambiente WSL/Node.

## Revisão

- **Resultado:** APROVADO com ressalva ambiental.
- **Achados bloqueantes:** nenhum.
- **Risco residual:** typecheck/build não foram executados por limitação do ambiente. Testes unitários e lint passaram.
