# TASK-RF-6.29 - Onboarding "Últimas manutenções do veículo" (km atual destacado + card editável)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data origem:** 07/06/26 (REDEFINIDA)
- **Data início:** 07/06/26 19:00
- **Data conclusão:** 07/06/26 19:20
- **Dependências:** TASK-RF-6.28 (concluída)
- **REQ/ADR/DT:** ADR-014, ADR-018, ADR-006, RF-6.3.4 (ver RF-6.31 item 10)
- **Observações:** Tela 10 do onboarding (bloco C). Substitui o checklist `Passo5Trocas.tsx`
  ("O que foi trocado?") por uma tela de edição de km: km atual destacado + card da última revisão
  geral (0 por padrão) + o card de Ajustes `SecaoUltimasManutencoes` reusado. Remover do card os
  itens que já vêm nos pacotes de revisão fixa (vela de ignição, filtro de ar); manter o óleo.

## Planejamento Aprovado

**O que muda:**
- `Passo5Trocas.tsx` → renomeado para `Passo5Manutencoes.tsx`: (A) km atual destacado + input "KM da
  última revisão geral" (`SET_KM_ULTIMA_REVISAO`, vazio = null); (B) explicação ciclo ancorado vs
  amortizado; (C) `<SecaoUltimasManutencoes>` reusado. `podeContinuar` sempre true.
- `SecaoUltimasManutencoes.tsx`: remover `velaIgnicao` e `filtroAr` de `COMPONENTES_TROCA` (vêm nos
  pacotes de revisão fixa); manter óleo. Vale para onboarding e Ajustes.
- `FluxoOnboarding.tsx`: import + element da rota `5trocas` para `Passo5Manutencoes` (path mantido;
  rename de rota é da RF-6.31).
- Cleanup absorvido: remover `MARCAR_TROCAS_REVISAO` (órfão) do reducer, do tipo `PerfilAction` e das
  docs (`estado_inicial.md`, `perfil-usuario.md`).
- `App.smoke.test.tsx`: título "O que foi trocado?" → "Últimas manutenções do veículo".

**Critérios de aceite:**
- Tela mostra km atual destacado + campo de km da última revisão (0 padrão) + explicação + card reusado.
- Vela/filtro de ar fora do card (onboarding e Ajustes); óleo mantido.
- Editar km da última revisão habilita a réplica por item.
- Nenhum passo bloqueia o avanço; `MARCAR_TROCAS_REVISAO` sem referências.
- `npm run verify` verde.

**Impacto:** tela do onboarding (slot 5trocas), `SecaoUltimasManutencoes` (Ajustes também), reducer,
smoke + docs. **Riscos:** validação `comPerfil` nas actions de km (perfil já válido no ponto; smoke
confirma); remoção de ação com 1 consumidor. **Dependências novas:** nenhuma.

## Execução

- 07/06/26 19:00: plano aprovado ("sim"); tarefa promovida.
- 07/06/26 19:10: `Passo5Trocas.tsx` (checklist "O que foi trocado?") removido; criado
  `Passo5Manutencoes.tsx` — (A) km atual destacado + input "KM da última revisão geral"
  (`SET_KM_ULTIMA_REVISAO`, vazio = `null`); (B) explicação ciclo ancorado vs amortizado;
  (C) `<SecaoUltimasManutencoes>` reusado. `podeContinuar` sempre true.
- 07/06/26 19:12: `SecaoUltimasManutencoes` — removidos `velaIgnicao` e `filtroAr` do
  `COMPONENTES_TROCA` (vêm nos pacotes de revisão fixa); óleo mantido. Vale p/ onboarding e Ajustes.
- 07/06/26 19:13: `FluxoOnboarding` aponta a rota `5trocas` para `Passo5Manutencoes` (path mantido).
- 07/06/26 19:15: cleanup absorvido — `MARCAR_TROCAS_REVISAO` (órfão após sair o checklist) removido
  do reducer e do tipo `PerfilAction`; docs `estado_inicial.md` e `perfil-usuario.md` atualizadas.
- 07/06/26 19:16: smoke do onboarding atualizado (título "Últimas manutenções do veículo").
- 07/06/26 19:18: **correção na validação visual** — o texto explicativo citava "(ADR-006)" para o
  usuário (erro grosseiro: ADR é interno). Reescrito sem citar ADR ("...o app distribuir o desgaste
  ao longo do tempo."). Gate re-executado verde.

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 32 arquivos / **379 testes** verdes
  (Windows nativo). A exaustividade do reducer (`never` no default) confirma que `MARCAR_TROCAS_REVISAO`
  saiu sem referências; nenhum teste o citava.
- `SecaoUltimasManutencoes.test.tsx` segue verde após a remoção de vela/filtro (não os citava).

## Revisão

### Modo
Auto-revisão IA + validação humana. Toca estado/onboarding/componente compartilhado → revisão obrigatória.

### ✅ Bom
- **Reuso real, sem duplicação:** a tela compõe o `SecaoUltimasManutencoes` de Ajustes; a parte
  específica do onboarding (km atual + km da última revisão) fica só na tela, sem vazar para o Ajustes
  (onde `kmUltimaRevisao` já é editado em `SecaoVeiculo`).
- **Remoção de vela/filtro é segura:** o cálculo ainda mapeia `vela_ignicao`/`filtro_ar` em
  `kmUltimaTrocas` (âncora), mas sem UID ficam em 0 → ciclo amortizado — coerente com estarem nos
  pacotes de revisão fixa. Sem mexer no schema/migração.
- **Cleanup absorvido na task dependente:** `MARCAR_TROCAS_REVISAO` saiu por completo (reducer, tipo,
  docs), sem deixar ação morta — a checagem `never` garante.
- **Acessibilidade:** input com `inputMode="numeric"`, label associada via `useId`.

### 🔴 Bloqueante
Nenhum.

### 🟡 Importante
Nenhum.

### 🟢 Sugestão
- O `case 'SET_KM_ULTIMA_TROCA'`/`SET_KM_ULTIMA_REVISAO` passam por `comPerfil` (valida o perfil). No
  onboarding o perfil já é válido neste ponto (o smoke exercita o fluxo até a estimativa). Mantido como
  está; só registro o ponto.
- Rota ainda chamada `5trocas` (nome legado). O rename de rota/posição é da RF-6.31.

### Veredito
APROVADO. Sem bloqueantes nem importantes. Gate verde (379 testes) e validação visual humana confirmada
(após a correção do texto que citava ADR).

### Tarefas Geradas pela Revisão
- **TASK-BG-023** (validação visual, real-use): remover o texto residual "Consumo profissional definido
  pelo ano" do `Passo2.tsx` e qualquer outro resíduo do modelo antigo de consumo-por-ano na UI. Consumo
  é do modelo (hardcoded no preset, vindo de manuais). Registrada em `em-andamento.md`.

## Validação Visual

- **APROVADO (07/06/26):** humano validou a tela ("o resto está ok"), com um único achado corrigido na
  hora: o texto explicativo citava "(ADR-006)" ao usuário — removido. A tela mostra km atual destacado,
  campo de km da última revisão, explicação sem jargão interno, e o card reusado sem vela/filtro de ar.
