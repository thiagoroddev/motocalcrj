# TASK-REF-30 — Resetar schema pré-lançamento e remover migrations históricas

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data origem:** 31/05/26 12:20; reformulada em 31/05/26 após decisão humana de descartar dados pré-lançamento
- **Data início:** 31/05/26 19:39
- **Data conclusão:** 31/05/26 20:00
- **Dependências:** —
- **REQ/ADR/DT:** ADR-010
- **Observações:** app ainda não lançado; dados de teste no localStorage podem ser descartados. O contrato correto agora é schema/storage atual ou fallback limpo.

## Planejamento Aprovado

**O que muda:**
- `src/services/perfilStorage.ts`: trocar namespace para uma versão pública inicial, `estimamoto:v1:*`, deixando `motocalc:v5:*` ignorado.
- `src/context/perfilDefaults.ts`: resetar `schemaVersion` atual para `1` e criar/usar uma constante de versão atual.
- `src/schemas/perfilSchema.ts`: validar que `schemaVersion` é exatamente a versão atual.
- `src/context/PerfilContext.tsx`: remover o fluxo `carregar → migrar → validar`; ficará `carregar → validar`. Se falhar, preserva como `.corrupted` e volta para onboarding.
- `src/services/migracoes.ts`: remover o contrato histórico de migração v5→v23.
- `src/main.tsx` e `src/fixtures/usuario_teste.json`: atualizar fixture/dev loader para o novo namespace e `schemaVersion: 1`.
- Testes: remover testes históricos de `migrarPerfil` e substituir por testes do novo contrato de storage/schema.
- Docs: atualizar documentação ativa que ainda promete namespace `motocalc:v5` e migração v5→v23.

**Critérios de aceite:**
- Nenhum código depende de `migrarPerfil`. OK
- `schemaVersion` atual passa a ser baseline público inicial. OK
- Dados antigos não são migrados; são ignorados/rejeitados com fallback seguro. OK
- `motocalc:v5:*` não é lido pelo app. OK
- `npm run test`, `npm run lint` e `npm run build` verdes. OK

**Impacto:** persistência local, inicialização do perfil, schema runtime, fixture dev, testes de contexto/schema e docs de modelagem/ADR.
**Riscos:** remover testes demais e deixar buraco em reducer/storage; docs ficarem contraditórias; fixture dev não carregar se namespace e chaves não forem atualizados juntos.
**Dependências novas:** nenhuma.

## Execução

- 31/05/26 19:39: Plano aprovado pelo humano; iniciando baseline de testes.
- 31/05/26 19:40: Baseline `npm run test` verde com 242 testes.
- 31/05/26 19:45: Implementado namespace `estimamoto:v1:*`, `schemaVersion` baseline `1`, validação direta sem `migrarPerfil` e remoção dos testes históricos de migração.
- 31/05/26 19:46: `npm run test` verde com 226 testes após o reset do contrato.
- 31/05/26 19:48: Docs ativas de storage/schema/ADR atualizadas para o contrato pré-lançamento.
- 31/05/26 19:48: `npm run lint` verde.
- 31/05/26 19:49: `npm run test` final verde com 226 testes.
- 31/05/26 19:49: `npm run build` passou fora do sandbox; dentro do sandbox falhou por ambiente WSL/Node. Build manteve apenas aviso conhecido de chunk grande do Vite.

## Decisões Tomadas

- Baseline público inicial = `schemaVersion: 1`.
- Namespace novo = `estimamoto:v1:*`, ignorando completamente `motocalc:v5:*`.
- Remover `src/services/migracoes.ts` em vez de manter função vazia, para não sugerir contrato histórico inexistente.
- `perfilSchema` valida versão exata; versões antigas/futuras caem no fallback recuperável.

## O Que NÃO Foi Feito (e Por Quê)

- Não foi criada migração de dados antigos: decisão explícita de pré-lançamento.
- Não foi criado ADR novo: a decisão atualiza o escopo da ADR-010, sem trocar tecnologia ou política de validação/fallback.
- Não foram alterados arquivos históricos em `docs/tarefas/concluidas/`.

## Testes

- `1º npm run test`: 242 testes verdes antes da alteração.
- `2º npm run test`: 226 testes verdes após a alteração.
- `3º npm run test`: 226 testes verdes após docs.
- `npm run lint`: verde.
- `npm run build`: verde fora do sandbox; aviso de chunk grande do Vite mantido.

## Revisão

### Modo
Auto-revisão IA (na conclusão) + revisão independente IA (a pedido do humano). Toca persistência/estado e
remove código (migrations) — atenção extra §10. Checklist módulo 40 abaixo.

### Checklist essencial (módulo 40)
- [x] Testes / typecheck / lint passam — `npm run test` 226 verdes, `tsc` 0, `lint` verde
- [x] Idioma PT; sem `any`; `VERSAO_SCHEMA_ATUAL = 1` centralizado em `types/perfil` e usado por schema (`z.literal`), defaults e tipo (fonte única)
- [x] `migracoes.ts` removido; **zero** referências a `migrarPerfil`/`migracoes` em `src/` (confirmado por grep)
- [x] Storage `estimamoto:v1:*`; `motocalc:v5:*` não é lido — teste dedicado garante o ignore
- [x] `criarEstadoInicial` = carregar → validar, com fallback `preservarCorrompido` + estadoPadrao (ADR-010)
- [x] Schema rejeita versão ≠ 1; fallback coberto por testes novos (schemaVersion 23 → fallback, 999 → fallback, lixo total)
- [x] Fixture dev (`estimamoto:v1:*`, `schemaVersion: 1`) + `main.tsx` alinhados — **verificado** que a fixture valida no schema atual
- [x] Storage isolado em serviço; sem `console.log`; sem segredo

### ✅ Bom
- Reset de contrato bem-feito: uma constante (`VERSAO_SCHEMA_ATUAL`) é a fonte única da versão, consumida por tipo, schema e defaults.
- Remoção limpa: `migracoes.ts` deletado em vez de virar função vazia; nenhum import órfão.
- **Cobertura preservada, não perdida** (verificado): os 21 testes removidos eram TODOS de `migrarPerfil` (v8→v11 … v22→v23) — testavam código deletado. O comportamento que importa hoje (versão antiga/futura/lixo → fallback recuperável) foi RE-coberto por testes novos em `criarEstadoInicial.test.ts` e `perfilStorage.test.ts`.
- Efeito colateral positivo: a fixture corrigida (fipeCache com marca/modelo) fecha o bug pré-existente flagrado na verificação da RNF-012; verifiquei que ela agora valida no schema atual.

### 🔴 Bloqueante
- Nenhum.

### 🟡 Importante
- Nenhum.

### 🟢 Sugestão
- O comentário de `IPerfilStorage.preservarCorrompido` (`perfilStorage.ts`) ainda diz "validação/migração" — não há mais migração; trocar por "validação". Cosmético.
- `limpar()` e o ignore não apagam as chaves antigas `motocalc:v5:*` do localStorage real (ficam órfãs, inofensivas pois nunca são lidas). Aceitável; fica o registro.

### Veredito
APROVADO — contrato histórico removido, baseline público v1 definido, fallback recuperável intacto e cobertura
relevante preservada. Verificações independentes (grep de `migrarPerfil`, ignore de `motocalc:v5`, fixture valida) confirmam.

## Tarefas Geradas pela Revisão

- — nenhuma.

## Requisitos Gerados pela Revisão

- — nenhum.

## ADRs Geradas

- — nenhuma.

## Aprendizados Para o Projeto

- Antes do lançamento público, é mais saudável resetar contratos internos do que carregar uma cadeia de compatibilidade para dados de teste. Depois que houver usuários reais, cada bump de schema volta a exigir decisão explícita de migração.
