# TASK-RF-6.14 - Adicionar Bateria, Kit Embreagem, Kit Revisão e Kit Cilindro como itens de cálculo

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data origem:** 25/05/26 10:29
- **Data início:** 26/05/26
- **Data conclusão:** 27/05/26 00h10
- **Dependências:** TASK-RF-6.22 (concluída - modelo ADR-007 disponível)
- **REQ/ADR/DT:** ADR-004, ADR-006, ADR-007

## Planejamento Aprovado (recapitulação)

**O que mudou:**

- [src/types/calculos.ts](../../src/types/calculos.ts) `PecaPreset`: `intervaloKm` e `intervaloKmEntrega` agora opcionais; novo campo `intervaloMeses?: number` para peças com driver temporal.
- [src/utils/calculos.ts](../../src/utils/calculos.ts):
  - `resolverIntervaloPeca` devolve `0` quando peça omite intervalo em km (caller trata como sinal de driver temporal).
  - `calcularCpkPorPeca` ganha ramo para peça temporal: `trocasNoAno = 12 / intervaloMeses`, `custoAnual = trocasNoAno × preco`, `cpk = kmAnual > 0 ? custoAnual / kmAnual : 0` (proteção contra divisão por zero).
  - `MAPA_PECA_PARA_SERVICO` ganha 3 ligações: `bateria` → `troca-bateria`, `kit_embreagem` → `troca-kit-embreagem`, `kit_cilindro` → `troca-kit-cilindro`.
  - `calcularDetalhesRevisaoAnual` ramo independente filtra `intervalKm > 0` antes de dividir (evita Infinity para `troca-bateria` com `intervalKm: 0`). Débito técnico: M.O. da bateria (~R$ 25/ano em modo independente) fica fora do cálculo de revisão; peça em si entra normalmente via `calcularCpkPorPeca`.
- [src/presets/pop110i.json](../../src/presets/pop110i.json): 4 peças novas em `pecas[]` (`bateria` 24m driver temporal, `kit_embreagem` 40k km, `kit_revisao` 6k km com `incluidoNaRevisaoAutorizada: true`, `kit_cilindro` 100k km); `revisaoAutorizada[1].precoPecas` e `precoTotal` 127,88 → 152,83 (reflete preço real do Kit revisão Honda).
- [src/context/PerfilContext.tsx](../../src/context/PerfilContext.tsx):
  - `SERVICOS_INDEPENDENTES_PADRAO` ganha 3 entradas: `troca-bateria` (`intervalKm: 0`, M.O. indep R$ 50, total Honda R$ 567,34), `troca-kit-embreagem` (40k km, R$ 50, R$ 450,33), `troca-kit-cilindro` (100k km, R$ 50, R$ 510,83). Todas com `incluidoNaRevisaoAutorizada: false`, `ativo: true`, `ehExcepcional: false`.
  - Reducer: mutual exclusion `kit_cilindro` ↔ `retifica-completa` em ambos os toggles. `TOGGLE_IMPREVISTO_SUGERIDO` para `retifica-completa` ativando zera `kit_cilindro` em `manutencaoPorPeca` (false explícito); `TOGGLE_MANUTENCAO_POR_PECA` para `kit_cilindro` ativando zera `retifica-completa` em `imprevistosSugeridosAtivos` (false explícito). Aviso/toast fica na UI.
  - Migration v17 → v18 idempotente: adiciona apenas serviços faltantes pelo id (preserva edições). Bump `perfilPadrao.schemaVersion` 17 → 18.
- [src/fixtures/usuario_teste.json](../../src/fixtures/usuario_teste.json): `schemaVersion` 17 → 18 em ambos os presets + 3 serviços novos inline (mantém fixture autossuficiente sem depender da migration).
- [src/components/detalhamento/DialogEdicaoCusto.tsx](../../src/components/detalhamento/DialogEdicaoCusto.tsx) e [src/pages/PaginaInsumos.tsx](../../src/pages/PaginaInsumos.tsx): adicionado `?? 0` na leitura de `intervaloKm`/`intervaloKmEntrega` para acomodar peças temporais sem quebrar UI.

**Defaults aprovados pelo usuário (25/05/26):**

| id | `intervalKm` | `precoMOIndep.` | `precoTotalAutoriz.` | Observação |
|---|---|---|---|---|
| troca-bateria | 0 (temporal) | 50 | **567,34** | Valor Honda exato fornecido pelo usuário; driver `intervaloMeses: 24` na peça. |
| troca-kit-embreagem | 40000 | 50 | **450,33** | peça 300,33 + M.O. 150 |
| troca-kit-cilindro | 100000 | 50 | **510,83** | peça 360,83 + M.O. 150; alternativa à Retífica completa |

**Decisões aprovadas:**

- Risco 1 - **(A)**: `troca-bateria` usa `intervalKm: 0`. Driver temporal vive só na peça (`PecaPreset.intervaloMeses`).
- Risco 2 (mutex): toggle marca explicitamente como `false` (não `delete`), porque `manutencaoPorPeca` default é "ativo se ausente".
- Renomear "Kit Revisão 6000km" → **"Kit revisão"** (intervalo é autoexplicativo via `intervaloKm: 6000`).
- 7 testes (5 em `calculos.test.ts` + 2 em `PerfilContext.test.ts`).

**Critérios de aceite cumpridos:**

- ✅ 4 peças contribuem ao CPK no modo independente; `kit_revisao` é absorvida no modo autorizado (ADR-006).
- ✅ Bateria gera `custoAnual` correto mesmo com `kmAnual: 0` e cpk = 0 (sem divisão por zero).
- ✅ Modo autorizado soma `precoTotalAutorizada` de `troca-kit-embreagem` ao ciclo Honda; peça `kit_embreagem` é excluída (ADR-007, evita dupla contagem).
- ✅ Mutual exclusion `kit_cilindro` ↔ `retifica-completa` via reducer (testado em ambos os sentidos).
- ✅ Migration v17 → v18 idempotente; preserva edições do usuário; adiciona apenas ids faltantes.
- ✅ `npm run test` 151/151 passa; `npx tsc --noEmit` verde; `npm run lint` verde.

**Riscos mitigados:**

- Divisão por zero em `calcularDetalhesRevisaoAnual` quando `intervalKm: 0` (bateria): proteção `intervalKm > 0` em ambos os ramos (independente e autorizado). Débito técnico documentado: M.O. da bateria (~R$ 25/ano) fica fora do cálculo de revisão; o peso da peça em si entra pelo caminho de manutenção via `calcularCpkPorPeca`. Custo negligível diante do CPK total.
- Cascade de migration estendida: 11 testes existentes que afirmavam `schemaVersion === 17` atualizados para `18` (assertam o estado final da cascade, comportamento intencional).
- Tipo `intervaloKm` opcional: dois consumers (`DialogEdicaoCusto.tsx`, `PaginaInsumos.tsx`) ajustados com fallback `?? 0` - preço temporal continua editável sem regressão na UI.

## Execução

- 26/05/26 22:00 - Plano apresentado e aprovado pelo usuário com 4 decisões: Risco 1=A; valores defaults conforme tabela; "Kit Revisão 6000km" → "Kit revisão"; 7 testes completos.
- 26/05/26 23:30 - Implementação: tipos, calculos.ts, preset, PerfilContext + fixture, testes.
- 27/05/26 00:05 - `tsc` reportou 2 erros em consumers de `intervaloKm` (agora opcional): ajustados com `?? 0`. `vitest` reportou 12 falhas (11 migrations cascade + 1 teste de revisão autorizada que replicava fórmula sem filtro `intervalKm > 0`): todos atualizados em sintonia com a mudança intencional.
- 27/05/26 00:10 - `npm run test` 151/151 verde; `npm run lint` verde; `npx tsc --noEmit` verde.

## Tarefa concluída
