# TASK-REF-09 — Onboarding Passo6, 6Financiamento, 6Aluguel, 6Responsabilidade — shadcn + min-h-touch

- **Status:** Concluída ✅
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** P/M
- **Data origem:** 18/05/26
- **Início:** 18/05/26 ~21h20
- **Conclusão:** 18/05/26 21:24
- **Dependências:** TASK-REF-06 ✅
- **REQ/ADR/DT:** —
- **Responsável:** IA + Humano

---

## Execução

- `Passo6.tsx`: `<button>` cards → shadcn `<Button>` com `min-h-touch h-auto flex-col items-start justify-start`.
- `Passo6Financiamento.tsx`: 2× `<input>` → shadcn `<Input>` com `min-h-touch rounded-input`; className extraído em constante.
- `Passo6Aluguel.tsx`: `<input>` → shadcn `<Input>`; 2 toggle buttons (`h-11`) → shadcn `<Button>` com `min-h-touch`.
- `Passo6Responsabilidade.tsx`: 9 toggle buttons (`h-10`) → shadcn `<Button>` com `min-h-touch`.

---

## Arquivos Alterados

| Arquivo | Mudança |
|---|---|
| `src/pages/onboarding/passos/Passo6.tsx` | `<button>` cards → shadcn `<Button>` + `min-h-touch` |
| `src/pages/onboarding/passos/Passo6Financiamento.tsx` | 2× `<input>` → shadcn `<Input>` + `min-h-touch` |
| `src/pages/onboarding/passos/Passo6Aluguel.tsx` | `<input>` → shadcn `<Input>`; 2 toggle `<button>` (`h-11`) → shadcn `<Button>` + `min-h-touch` |
| `src/pages/onboarding/passos/Passo6Responsabilidade.tsx` | 9 toggle `<button>` (`h-10`) → shadcn `<Button>` + `min-h-touch` |

---

## Decisões Tomadas

- **Toggle buttons (Aluguel + Responsabilidade):** padrão estabelecido para botões primários ativos: `bg-primary text-foreground hover:bg-primary/90`; inativos: `bg-card border border-muted text-muted-foreground hover:bg-muted/50`. Consistente com o override `text-foreground` documentado na TASK-REF-06.

---

## Testes

| Comando | Resultado |
|---|---|
| `npm run lint` | 0 erros |
| `npm run test` | 92 testes passaram |
| `npm run build` | CSS: 40.93 kB, JS: 345.82 kB |

---

## Tarefas Geradas

Nenhuma.
