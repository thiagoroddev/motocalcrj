# TASK-RF-6.31.5 - Matriz de navegação do onboarding + sincronização de docs

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data início:** 08/06/26
- **Data conclusão:** 08/06/26 15:30
- **Dependências:** RF-6.31.3, RF-6.31.4, RF-6.31.6
- **REQ/ADR/DT:** ADR-020
- **Observações:** Última subtarefa do épico RF-6.31. Teste da matriz de navegação (3 ramos + voltar +
  modo edição) e sincronização de docs com a ordem/rotas finais. Sem mudança de UI.

## Execução

- **Helper testável:** extraído `proximoEmEdicao(passo, situacao)` em `onboardingUtils.ts` (centraliza a
  regra "editar volta à Confirmação, exceto sub-rota de Situação"); `FluxoOnboarding` e `Passo6` passaram
  a usá-lo (remove a duplicação que existia nos dois).
- **Teste:** novo `src/pages/onboarding/onboardingUtils.test.ts` — matriz de `getProximoPasso`/
  `getPassoAnterior` nos 3 ramos (quitada linear; financiada; alugada) incluindo **voltar**; `proximoEmEdicao`
  (passo normal → confirmação; Situação financiada/alugada percorre as sub-rotas e então volta); `parsePasso`.
- **Docs sincronizadas** com a ordem/rotas semânticas (ADR-020 = fonte única):
  - `requisitos/funcionais.md`: RF-ON-02/03/04/05/07 atualizados; o "Fluxo Detalhado" antigo (Passo 1-9,
    "PASSO X DE 9") foi substituído por um resumo apontando para a ADR-020 (sem duplicar a ordem).
  - `contexto-projeto-ai.md`: `/onboarding/1` → `/onboarding/modelo`; descrição do fluxo (10 passos +
    Confirmação, Rodagem inline, editar-volta).
  - `dominio/_glossario.md`: Onboarding (10 passos + ADR-020), Situação (passo de Situação + sub-rotas),
    FIPE Cache / FIPE (passo de Ano), Catálogo de Modelos (consumo do modelo `consumoKmL` — ADR-019; não
    mais "consumo por ano"; autonomia semeada no passo de Km e preservada no commit — RF-6.33).

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 34 arquivos / **397 testes** verdes
  (Windows nativo). `onboardingUtils.test.ts`: 11 testes.

## Revisão

### Modo
Auto-revisão IA. Teste + docs (sem UI). N/A validação visual.

### ✅ Bom
- **Matriz cobre os 3 ramos + voltar + modo edição** numa suíte pura e rápida (testa `onboardingUtils`
  direto, sem render).
- **Refator com ganho:** `proximoEmEdicao` elimina a duplicação da regra de edição entre `FluxoOnboarding`
  e `Passo6`, e torna o comportamento testável — o gate confirmou que nada quebrou.
- **Docs sem duplicação tóxica:** o anexo de fluxo aponta para a ADR-020 (fonte única) em vez de repetir
  a ordem passo-a-passo, que voltaria a divergir.

### 🔴 Bloqueante / 🟡 Importante
Nenhum.

### Veredito
APROVADO. Fecha o épico RF-6.31.

## Relacionado

- **Épico RF-6.31 (reformulação do onboarding) — COMPLETO:** 6.31.1 (ADR-020) · 6.31.2 (PassoModelo) ·
  6.31.3 (rotas/reorder) · 6.31.4 (confirmação) · 6.31.6 (editar-volta + Rodagem inline) · 6.31.5 (matriz
  + docs). Telas de M.O.: 6.32 + 6.32.1.
