# TASK-RF-6.31.2 - Unificar Marca + Modelo numa única etapa (PassoModelo)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M
- **Data conclusão:** 07/06/26 22:35
- **Dependências:** TASK-RF-6.31.1 (ADR-020)
- **REQ/ADR/DT:** ADR-020 (passo 1), registro mestre RF-6.31
- **Observações:** Subtarefa 2 da RF-6.31. Entrega o **componente** da etapa única de marca+modelo; o
  wiring (rotas/maps/remoção do Passo1/Passo2) é da **RF-6.31.3**.

## Execução

- 07/06/26: criado `src/pages/onboarding/passos/PassoModelo.tsx` — uma só tela com:
  - seletor de **marca** (`getMarcasDisponiveis`) no topo;
  - lista de **modelos** da marca selecionada (`getModelosPorMarca`), card só com o nome (sem consumo —
    coerente com a BG-023);
  - grava `moto.marca` + `moto.modelo` via `SET_ONBOARDING_CAMPO` no avançar;
  - `podeContinuar` só com modelo selecionado;
  - auto-seleção: marca inicial quando há só uma marca; modelo quando a marca tem só um; ao trocar de
    marca, o modelo é limpo (ou auto-selecionado se único).
- `Passo1.tsx` e `Passo2.tsx` foram **mantidos** — a RF-6.31.3 os substitui no roteamento e remove.

## Testes

- **APROVADO:** `npm run verify` (typecheck + lint + test) — 33 arquivos / 384 testes verdes
  (Windows nativo). O componente compila e passa no lint.

## Revisão

### Modo
Auto-revisão IA. Componente de onboarding (sem lógica de cálculo). Validação visual em fluxo deferida.

### ✅ Bom
- Reaproveita os helpers do catálogo (`getMarcasDisponiveis`/`getModelosPorMarca`) e o padrão visual de
  `Passo1`/`Passo2`, sem duplicar lógica de dados.
- Mantém a auto-seleção (marca/modelo únicos) já existente, evitando fricção.
- Card de modelo sem consumo, consistente com a consolidação da BG-023.

### 🔴 Bloqueante / 🟡 Importante
Nenhum.

### 🟢 Sugestão
- `PassoModelo` fica **não roteado** até a RF-6.31.3; por isso não há teste/validação em fluxo nesta
  subtarefa (será coberto pelo smoke da RF-6.31.5). As partes lógicas são triviais (seleção + dispatch).

### Veredito
APROVADO. Código e gate OK. Validação visual em fluxo ocorre quando a RF-6.31.3 rotear o componente.

## Relacionado

- Próxima: **TASK-RF-6.31.3** (reordenar rotas/progresso/wiring) — roteia `PassoModelo` e `PassoMaoDeObra`,
  reescreve `onboardingUtils`/`FluxoOnboarding` e remove `Passo1`/`Passo2`.
