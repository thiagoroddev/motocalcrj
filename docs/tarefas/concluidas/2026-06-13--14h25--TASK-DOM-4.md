# TASK-DOM-4 — Criar preset do FZ15 (pioneiro do freio traseiro a disco)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** M/G
- **Data origem:** 13/06/26 13h00
- **Data início:** 13/06/26 13h00
- **Data conclusão:** 13/06/26 14h25
- **Dependências:** TASK-DOM-3 (dados), TASK-RF-6.38 (tempário/ícones/testes)
- **REQ/ADR/DT:** ADR-013, ADR-014, ADR-019, ADR-021; `como-criar-preset.md`
- **Observações:** Escopo reduzido ao **FZ15** (decisão do humano: pioneiro a disco). Os 250s são DOM-4.2/4.3/4.4. O FZ15 estreia o freio traseiro a disco → adicionou o `MAPA` que a RF-6.38 deixou pendente.

## Planejamento Aprovado

Criar `src/presets/fz15.json` completo (identidade, FIPE, peças com freio a disco diant+tras, pneus, `revisaoAutorizada` de `revisoes-fz15.md`, serviços) + adicionar ao `MAPA_PECA_PARA_SERVICO` as entradas `disco_freio_traseiro: 'troca-disco-traseiro'` e `pastilha_freio_traseiro: 'troca-pastilha-traseira'`. CATALOGO/onboarding auto-derivam dos presets. `consumoKmL` = 35 (humano).

## Execução

- 13/06/26 13h00: Movida de pendentes (escopo FZ15).
- 13/06/26: Consumos do humano (FZ15 35; FZ25 30; Lander 29; Fazer 250 35). Aprovado.
- 13/06/26: `fz15.json` criado (disco diant+tras, sem sapata; preços FZ15; `precoParalela=original`; 7 revisões somando R$ 2.712). `MAPA` atualizado. `npm run fipe:update` → 827124-0 (2023/2024).
- 13/06/26 14h04: Gates verdes; runtime check (vitest temp) ok.
- 13/06/26 14h25: Validação visual aprovada pelo humano. Concluída.

## Decisões Tomadas

- **`precoParalela = precoOriginal`** em todas as peças/pneus: decisão do humano (estes modelos não têm/terão cotação paralela).
- **Óleo/filtros/vela herdados da família Yamaha 125/150** (reusados do Factor 150); disco/pastilha/kit relação/caixa com preço específico do FZ15.
- **Freio traseiro a disco** (sem sapata): primeiro preset do tipo; adicionou o `MAPA_PECA_PARA_SERVICO` (validado pela guarda generalizada na RF-6.38).
- **Rateio peça×M.O. estimado** nos pacotes de revisão "M.O.+Peças" (totais exatos do doc; split aproximado, nota "Rateio estimado").
- **consumoKmL 35** informado pelo humano (nota "validar" no `_fonte`).

## O Que NÃO Foi Feito (e Por Quê)

- **Presets dos 250cc** (FZ25, Fazer 250, Lander 250): **DOM-4.2/4.3/4.4** em pendentes — reusam a estrutura do FZ15, com consumos já registrados.
- **Km-âncora de disco/pastilha em Ajustes:** é a TASK-RF-6.39 (disco/pastilha do FZ15 entram amortizados por ora).

## Testes

- `1º npm run test`: 433 (baseline pós-RF-6.38).
- Check de runtime: vitest temporário (`__fz15_check.test.ts`, removido após uso — convenção do projeto) validou: FZ15 no CATALOGO (Yamaha, consumo 35); freio a disco diant+tras presente e sem sapata; `MAPA` resolve; M.O. estimada do disco/pastilha traseiro bate (0,7/0,5 × 110 × 1,15); soma das revisões = 2.712.
- `último npm run test`: **433 verdes** — APROVADO (a guarda generalizada do `MAPA_PECA_PARA_SERVICO` agora valida o `disco_freio_traseiro` via fz15.json).
- `npx tsc --noEmit`: **APROVADO** (0).
- `npm run lint`: **APROVADO**.
- `npm run build`: **APROVADO**.
- Validação visual: **APROVADA** pelo humano.

## Revisão

Auto-revisão (Standard): preset baseado no Factor 150 (Yamaha 150), trocando o conjunto de freio traseiro (tambor→disco) e os preços específicos do FZ15. Schema validado na carga (PRESETS) — gate de teste garante. Nenhum modelo existente afetado (mudança aditiva: novo arquivo + 2 linhas no `MAPA`).

## Tarefas Geradas

- **DOM-4.2** (FZ25), **DOM-4.3** (Fazer 250), **DOM-4.4** (Lander 250): em pendentes, reusam a estrutura.

## Aprendizados Para o Projeto

- **Catálogo de modelos (onboarding) é auto-derivado dos presets** (`catalogoModelos.ts` mapeia `LISTA_PRESETS`) — criar o JSON já registra o modelo, sem mexer em catálogo.
- O **primeiro preset de um novo tipo de freio** (disco traseiro) é o que "destrava" o `MAPA_PECA_PARA_SERVICO`: capacidade (RF-6.38: tempário/ícones/teste generalizado) + dados (DOM-4: preset + MAPA) fecham o ciclo sem mapeamento órfão.
