# Handoff TASK-DOM-1

**Data de conclusão:** 2026-05-10
**Agente executor:** modelador-dominio
**Status:** ✅ CONCLUÍDO

---

## Task

**TASK-DOM-1 Atualização da modelagem de domínio**

Revisar código atual do projeto e atualizar `docs/dominio/` (glossário, diário de trabalho, value objects, aggregate do perfil e ajustes de referências) para refletir o que está implementado.

---

## Contexto da execução

Esta task surgiu após identificação de que a modelagem inicial de `docs/dominio/` (criada em rodadas anteriores) havia sido feita **por inferência sobre documentação**, sem leitura do código real. Resultado: vários arquivos com estruturas incorretas, atributos inventados, e divergências silenciosas entre modelagem e implementação.

A execução foi feita com acesso direto ao filesystem do projeto (Claude Code), permitindo leitura efetiva dos arquivos `.ts` e `.tsx`.

---

## Arquivos consultados (código real lido)

- `src/types/perfil.ts`
- `src/types/calculos.ts`
- `src/utils/calculos.ts`
- `src/context/PerfilContext.tsx`
- `src/services/perfilStorage.ts`
- `src/services/fipeService.ts`
- `src/data/catalogoModelos.ts`
- `src/data/dados_rj.json`
- `docs/dominio/*` (estado anterior)

---

## O que foi feito

### Arquivos criados
- `docs/dominio/aggregate-perfil.md` novo aggregate do armazenamento local (presets + ativo). Separação semântica de `aggregate-preset.md` (que representa um `PresetEntry` individual).
- `docs/dominio/README.md` README correto (substituiu `READM.md` que tinha typo no nome).

### Arquivos reescritos
- `docs/dominio/value-objects.md` reescrito com VOs reais (e declaração explícita de que `HabitosUso` e `CustosFixos` **não existem** no código atual).
- `docs/dominio/diario-trabalho.md` reescrito com `DiarioEntry` real e regras do reducer.
- `docs/dominio/entidade-moto.md` alinhado ao bloco real do perfil.
- `docs/dominio/entidade-preset.md` alinhado ao `PresetEntry` real.
- `docs/dominio/historico-manutencao.md` alinhado ao reducer e ao adapter real (`adaptarHistoricoParaRegistros`).

### Arquivos atualizados (ajustes pontuais)
- `docs/dominio/_glossario.md` atualizado com referências v5 (corrigido depois em TASK-DOM-2 para v6) e novos termos.
- `docs/dominio/bloco-configuracao-display.md` confirmada ausência de sincronização com `modoRevisao`.
- `docs/dominio/bloco-financeiro.md` confirmado uso de `SET_ONBOARDING_CAMPO` para campos sem action dedicada (origem da DT-14, registrada em TASK-DOM-2).
- `docs/dominio/bloco-perfil-manutencao.md` confirmada ausência de actions dedicadas.
- `docs/dominio/bloco-trabalho.md`, `bloco-moto.md`, `aggregate-preset.md`, `perfil-usuario.md` referências v5 (corrigidas em TASK-DOM-2).
- `docs/dominio/overrides.md` regra `anoFimOriginal força paralela` (RN-11) marcada como **não implementada** no código.
- `docs/dominio/divida-tecnica.md` referências v5 e DT-12 confirmada (Diário × Histórico não sincronizam).

---

## Decisões tomadas

1. **Padronizar referências para `Requisitos_MotoCalc_RJ_v5.md`** ⚠️
   - **Esta decisão foi posteriormente identificada como incorreta** em TASK-DOM-2 (a versão ativa do projeto é v6, não v5). Corrigida em rodada seguinte pelo `agente-evolutivo`.

2. **Criar aggregate do armazenamento local separado** ✅
   - `aggregate-perfil.md` representa o conjunto "presets + ativo" (o contêiner do localStorage).
   - `aggregate-preset.md` representa um `PresetEntry` individual.
   - Justificativa: INV-PRESET-1 (consistência do `presetAtivoId`) é invariante **entre** Presets, não dentro de um. Ter aggregate dedicado torna explícito.

---

## O que NÃO foi feito (e por quê)

- **Não alterei `contexto-base.instructions.md`** sem mudanças estruturais em `src/`, e mudanças no contexto-base são responsabilidade do `agente-evolutivo`.
- **Não validei numeração RN-xx dentro dos Requisitos v5** mantidas as referências existentes (RN-25, RN-26, etc.). Validação completa exigiria leitura linha-a-linha do documento de requisitos, fora do escopo desta task.

---

## Pendências resolvidas (das 5 que existiam)

1. ✅ **`PerfilAction` completo** leitura de `PerfilContext.tsx` confirmou as actions reais. Várias actions usadas fora do Onboarding usam `SET_ONBOARDING_CAMPO` como atalho (origem da DT-14).
2. ✅ **INV-OVR-4 (`anoFimOriginal` força paralela)** confirmado que **NÃO está implementado** no código. Documentado em `overrides.md`.
3. ✅ **Sync Diário × Histórico** confirmado que **NÃO sincronizam**. DT-12 atualizada.
4. ✅ **`modoOficinDisplay` vs `perfilManutencao.modoRevisao`** confirmado que existem separados, sem sync. Documentado em `bloco-configuracao-display.md`.
5. ❓ **INV-DIARIO-5 (`ADD_DIA_TRABALHO` atualiza `kmAtual`)** pendência remanescente. Possivelmente resolvida (não houve menção contrária), mas não confirmada explicitamente. Migrada para TASK-INVEST-1 no backlog.

---

## Testes executados

**N/A** mudanças apenas em documentação (`docs/dominio/`). Nenhum código de produção foi tocado.

---

## Alertas para próximos agentes

- **Modo personalizado** substitui automaticamente com 1+ registros (não 5+ como pede RN-25). Registrado em DT-7.
- **`modoOficinDisplay`** não altera `perfilManutencao.modoRevisao` no reducer são campos independentes.
- **Registros do diário e histórico de abastecimento** não sincronizam (DT-12 confirmada com evidência da fixture).
- **`SET_ONBOARDING_CAMPO`** é usado fora do Onboarding com `valor: unknown`, bypassando type-safety (origem da DT-14, registrada em TASK-DOM-2).
- **Referências v5 ainda presentes** em vários arquivos corrigidas em TASK-DOM-2.

---

## Próximo agente recomendado

**`agente-evolutivo`** para varredura de coerência entre a nova modelagem e:
- `contexto-base.instructions.md` (afirmações que podem estar desatualizadas)
- Instruções de outros agentes (suposições contraditadas)
- Decisão sobre criar DT nova para `SET_ONBOARDING_CAMPO`